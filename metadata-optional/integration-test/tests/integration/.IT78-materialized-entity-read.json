{
  "fields": {
    "TypeID": "@lookup:MJ: Test Types.Name=Integration Test",
    "Name": "IT78 - Materialized Entity Base-View Read Redirect",
    "Description": "Live-DB proof that RunView(DataSource:'Materialized') on an entity is served from the entity's physical snapshot via resolveEffectiveBaseView, not the live base view (the higher-blast-radius redirect that can affect RunView for any entity). Fabricates an EntityBaseView MaterializedResult (SourceEntityID) + a snapshot carrying a sentinel row absent from the live view; EMR1 asserts a Materialized RunView returns the sentinel, EMR2 (negative control) asserts the same read Live returns it nowhere. Server transport. Self-cleaning fixtures; SQL-Server only (skips when no mssql pool).",
    "InputDefinition": {},
    "ExpectedOutcomes": {
      "summary": "A Materialized RunView on the entity is served from the snapshot (returns the sentinel) while the same read Live returns the sentinel nowhere. 2 checks (EMR1-EMR2)."
    },
    "Configuration": {
      "tier": "deterministic",
      "transport": "server",
      "checks": [
        {
          "type": "materialized-entity-read"
        }
      ]
    },
    "Status": "Active"
  },
  "primaryKey": {
    "ID": "6D9C5995-1DFE-44C1-B286-5E84255F8EE4"
  },
  "sync": {
    "lastModified": "2026-09-05T23:43:05.335Z",
    "checksum": "f8bb6ece8a5f30b678c92f1b79aa1103eddf9f6fdf44c1ecc88543d8d1ce2048"
  }
}