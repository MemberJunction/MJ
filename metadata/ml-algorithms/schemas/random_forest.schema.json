{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Random Forest Hyperparameters",
  "type": "object",
  "properties": {
    "n_estimators": {
      "type": "integer",
      "minimum": 10,
      "maximum": 2000,
      "default": 200,
      "description": "Number of trees in the forest."
    },
    "max_depth": {
      "type": ["integer", "null"],
      "minimum": 1,
      "maximum": 100,
      "default": null,
      "description": "Maximum depth of each tree; null lets trees grow until leaves are pure."
    },
    "min_samples_split": {
      "type": "integer",
      "minimum": 2,
      "maximum": 100,
      "default": 2,
      "description": "Minimum number of samples required to split an internal node."
    },
    "min_samples_leaf": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 1,
      "description": "Minimum number of samples required to be at a leaf node."
    },
    "max_features": {
      "enum": ["sqrt", "log2", null],
      "default": "sqrt",
      "description": "Number of features considered per split; null uses all features."
    }
  }
}
