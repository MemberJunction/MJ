{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "ApiKey": {
      "type": "string",
      "title": "API Key",
      "description": "Wild Apricot admin API key (used as Basic auth username on the OAuth token endpoint).",
      "isSecret": true,
      "order": 0
    },
    "AccountId": {
      "type": "string",
      "title": "Account ID",
      "description": "Optional Wild Apricot account (tenant) ID. Auto-discovered via GET /v2/accounts on first auth when omitted.",
      "order": 1
    }
  },
  "required": [
    "ApiKey"
  ]
}
