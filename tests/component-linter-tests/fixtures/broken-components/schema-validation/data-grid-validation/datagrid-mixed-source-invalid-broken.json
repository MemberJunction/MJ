{
  "name": "DataGridMixedSourceInvalidBroken",
  "type": "generic",
  "title": "DataGrid Mixed Source Invalid Field - Broken Pattern",
  "description": "Tests linter detection of invalid field when mixing entity and computed fields",
  "code": "function DataGridMixedSourceInvalidBroken({ utilities, components }) {\n  const [members, setMembers] = useState([]);\n  const { DataGrid } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Members' });\n      if (result?.Success) {\n        // Adding computed field\n        const enhanced = result.Results.map(m => ({\n          ...m,\n          FullName: `${m.FirstName} ${m.LastName}`\n        }));\n        setMembers(enhanced);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Members</h3>\n      {/* ❌ MemberLevel doesn't exist in entity or computed */}\n      <DataGrid \n        data={members} \n        columns={[\n          {field: 'FullName', title: 'Name'},\n          {field: 'Email', title: 'Email'},\n          {field: 'MemberLevel', title: 'Level'}\n        ]} \n      />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Members",
      "fields": ["ID", "FirstName", "LastName", "Email", "MembershipType"]
    }]
  }
}
