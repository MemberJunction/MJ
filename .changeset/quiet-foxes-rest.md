---
"@memberjunction/integration-connectors": patch
---

Update YourMembership connector watermark tests to match simplified FetchMemberBatch that delegates pagination to the engine instead of filtering client-side.
