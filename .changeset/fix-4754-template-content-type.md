---
'@memberjunction/ng-core-entity-forms': patch
---

Template form: a content-type (or text) change made in the Template Editor is now saved — the form saves through the editor that owns the content rows instead of a separately loaded copy, and content load/save failures are reported instead of swallowed (#4754).
