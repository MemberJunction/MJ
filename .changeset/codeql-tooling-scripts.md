---
"@memberjunction/ai-agents": patch
---

The agent-memory smoke harness (`scripts/memory-write-smoke.ts`) decides TLS for Azure SQL with an anchored host check (`endsWith`) instead of a substring match. Not part of the published output; recorded for the release notes.
