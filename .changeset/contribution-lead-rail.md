---
"@memberjunction/ng-base-forms": patch
---

Honor L1 `inclusion` on form contributions. Primary contributions (Overview) are their own rail items and sort in a lead band before Details. The left-nav only applies a persisted user section order — the generated form.sections fallback no longer parks Overview after baked related grids.
