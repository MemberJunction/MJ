---
"@memberjunction/global": patch
---

Add canonical `EscapeSQLString` utility to `@memberjunction/global` for safe escaping of string literals in SQL statements, clauses, and `ExtraFilter` predicates. Replaced duplicate ad-hoc implementations across core packages.
