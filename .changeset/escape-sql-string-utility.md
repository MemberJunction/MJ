---
"@memberjunction/global": patch
---

Add canonical `EscapeSQLString` (and `EscapeSQL`, `EscapeSqlString` aliases) utility to `@memberjunction/global` for safe escaping of string literals in SQL statements, clauses, and `ExtraFilter` predicates.
