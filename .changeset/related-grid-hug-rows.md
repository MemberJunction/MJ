---
"@memberjunction/ng-base-forms": patch
---

Related left-nav grids hug toolbar + header + rows with no 200px floor. Optional MaxHeight (default 560) caps the box so AG Grid scrolls inside; omit it to grow with the rows.
