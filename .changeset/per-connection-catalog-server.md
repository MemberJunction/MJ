---
"@memberjunction/server": minor
---

Per-connection integration catalog, server half: the overflow promoter and `enforceSchemaLimits` write the connection's own rows, newly discovered objects arrive Active but unselected (`autoEnableNewObjects` defaults to false again) while still reaching key inference and the migration, and every catalog-touching mutation runs in the connection's catalog scope.
