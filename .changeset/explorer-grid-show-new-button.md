---
"@memberjunction/ng-base-forms": patch
---

`mj-explorer-entity-data-grid` now forwards `ShowNewButton` to the inner entity grid, so a related list can keep search/refresh/export and still hide New.
