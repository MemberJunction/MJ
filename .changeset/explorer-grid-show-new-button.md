---
"@memberjunction/ng-base-forms": patch
---

`mj-explorer-entity-data-grid` now forwards the inner grid's toolbar chrome (`ShowToolbar`, `ShowSearch`, and the `Show*Button` / `ShowRecycleBin` flags) so a related list can hide New, search, refresh, export, or the whole bar without losing the rest.
