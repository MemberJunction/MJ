---
"@memberjunction/ng-base-forms": patch
---

Left-nav form chrome can collapse to a thin current-section strip. Pin is the default and is stored per user per entity; unpinned rails hide again after you pick another section.
