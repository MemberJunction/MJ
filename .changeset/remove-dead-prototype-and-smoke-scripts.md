---
"@memberjunction/ai-agents": patch
"@memberjunction/actions": patch
---

Remove two unreferenced smoke scripts (`contextcrush-smoke.ts`, `entity-action-workflow-integration.ts`) from the packages' `scripts/` folders. Neither is compiled or published; the published output is unchanged.
