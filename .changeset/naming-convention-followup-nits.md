---
"@memberjunction/ng-core-entity-forms": patch
---

The Record Process form template binds the PascalCase members `RawJsonExpanded`, `RawJsonCopied`, `ToggleRawJson` and `CopyRawJson` directly rather than through their deprecated camelCase stubs. Behavior is unchanged.
