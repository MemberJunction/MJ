---
"@memberjunction/ng-conversations": patch
---

Keep the message header gear icon right-anchored after agent completion by moving the right-anchor onto the whole right-side cluster, and surface the frozen live-elapsed value as a fallback when neither the agent run nor message timestamps yield a duration.
