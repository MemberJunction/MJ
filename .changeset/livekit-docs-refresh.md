---
"@memberjunction/ai-bridge-livekit": patch
---

Docs: point the LiveKit provider README at the now-shipped `@memberjunction/ai-bridge-livekit-native` room client (the `@livekit/rtc-node` wrapper) and note the `NativeModuleSpecifier` / `LIVEKIT_NATIVE_MODULE` deployment knob.
