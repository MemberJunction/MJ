---
"@memberjunction/db-auto-doc": patch
"@memberjunction/codegen-lib": patch
---

Add enum/value-list detection for databases without CHECK constraints — deterministic pre-filter gates identify candidate columns, LLM verdicts are emitted in additionalSchemaInfo Fields[], and CodeGen consumes them to populate EntityFieldValue rows (patch)
