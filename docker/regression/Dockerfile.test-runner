# MemberJunction Test Runner — Regression Testing
# Runs the MJ Testing Framework with ComputerUseTestDriver against MJExplorer.
#
# Based on Playwright's official image (includes Chromium).
# Copies the full monorepo since the test driver depends on many packages:
#   TestingFramework, ComputerUse, MJComputerUse, AI packages, Core, etc.
#
# Usage: the docker-compose test stack starts this after mjexplorer is healthy.
#
# Layer order (DR-C1): package manifests → npm ci (BuildKit-cached) → sources →
# build. A source edit no longer invalidates the dependency install.

# ─── Manifests-only stage (see Dockerfile.db-setup for the rationale) ───────
FROM node:24-slim AS manifests
WORKDIR /m
COPY packages/ ./packages/
RUN find packages -type f ! -name package.json ! -path '*/bin/*' -delete \
    && find packages -type d -empty -delete

# ─── Runner image ───────────────────────────────────────────────────────────
FROM mcr.microsoft.com/playwright:v1.58.1-noble

# socat forwards localhost:4200 → mjexplorer:4200 so the browser sees a secure origin
RUN apt-get update && apt-get install -y --no-install-recommends socat && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Dependencies first: root manifests + the package.json skeleton, then a
# cache-mounted reproducible install keyed only on manifest content.
COPY package.json package-lock.json turbo.json tsconfig.angular.json tsconfig.server.json ./
COPY --from=manifests /m/packages/ ./packages/
RUN --mount=type=cache,target=/root/.npm npm ci

# Sources after install (COPY merges over the skeleton; nested node_modules
# created by npm ci are preserved). Metadata is baked for standalone use —
# the compose stack bind-mounts the live tree over it at runtime.
COPY packages/ ./packages/
COPY metadata/ ./metadata/
# The 380-test regression suite + membership live in the opt-in metadata-optional
# sibling root (kept out of the base metadata tree, like integration-test). The
# entrypoint pushes them via `mj sync push --dir=metadata-optional/regression-test`,
# so the directory must exist in the container — the runtime :ro mount in
# docker-compose.test.yml shadows this for the live-mount flow, and this COPY keeps
# a plain `docker run` (no compose volumes) working too. Without it the suite push
# finds nothing and the DR-E1 preflight gate aborts the run.
COPY metadata-optional/ ./metadata-optional/

# Build all packages (turbo handles dependency order)
# --concurrency=2 limits parallel tsc processes to avoid OOM in Docker's memory-constrained environment
RUN npx turbo build --concurrency=2 --filter="@memberjunction*"

# Copy MJ config
COPY docker/MJAPI/docker.config.cjs ./mj.config.cjs

# Bootstrap: imports ComputerUseTestDriver so @RegisterClass fires before CLI runs
RUN echo "import '@memberjunction/computer-use-engine';" > /app/bootstrap.mjs

# Create results directory
RUN mkdir -p /app/test-results

VOLUME ["/app/test-results"]

# Default: run the regression suite
# Use the workspace-local CLI (not global) to ensure correct module resolution
CMD ["npx", "mj", "test", "suite", "--name", "MJ Explorer Regression Suite", "--format", "json", "--output", "/app/test-results/results.json"]
