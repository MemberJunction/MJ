# MJ Agentic Test Runner — Architecture

Deep-dive reference for the Docker-based, LLM-driven regression test
infrastructure that lives under [`docker/regression/`](.) and the published
runner image at [`docker/agentic-test-runner/`](../agentic-test-runner/).

**Audience:** engineers and AI agents extending the stack, debugging failures,
or adopting it in their own project. For getting-started material, see
[`REGRESSION_TESTING.md`](REGRESSION_TESTING.md). For known gaps + follow-ups,
see [`LIMITATIONS.md`](LIMITATIONS.md).

---

## Contents

1. [Overview](#1-overview)
2. [The Four Run Modes](#2-the-four-run-modes)
3. [Docker Image Topology](#3-docker-image-topology)
4. [Compose Profiles](#4-compose-profiles)
5. [Container Inventory](#5-container-inventory)
6. [Entrypoint Dispatch](#6-entrypoint-dispatch)
7. [Control Flow — A Single Run](#7-control-flow--a-single-run)
8. [Target Profile System](#8-target-profile-system)
9. [Variable Substitution](#9-variable-substitution)
10. [Custom Oracles](#10-custom-oracles)
11. [Suite-Level Application Context](#11-suite-level-application-context)
12. [Browser Context Isolation](#12-browser-context-isolation)
13. [Archive Flow](#13-archive-flow)
14. [CLI Surface](#14-cli-surface)
15. [Init Templates](#15-init-templates)
16. [File Inventory](#16-file-inventory)
17. [Diagnostic Outputs](#17-diagnostic-outputs)
18. [Extension Points](#18-extension-points)
19. [Glossary](#19-glossary)

---

## 1. Overview

The MJ Agentic Test Runner is an **end-to-end browser-test harness** that
drives real applications with a headless Chromium instance controlled by an
LLM (the **Computer Use** engine). Tests are authored as MJ metadata JSON
files describing a natural-language *goal* (e.g. "log in and verify the
dashboard loads"). At runtime an LLM controller decides what to click, type,
or scroll based on screenshots of the current page; a second LLM judge
periodically evaluates whether the goal has been achieved; and deterministic
oracles (URL match, step count, custom plugins) produce a scored result.

Two callers consume this infrastructure:

- **The MemberJunction monorepo itself**, where it gates releases by
  running 25 canonical tests against a self-contained 5-container MJ stack
  every time `next` is built.
- **External adopters** — customers, internal teams, or any project — who
  point the published [`memberjunction/agentic-test-runner`][docker-hub-image]
  image at their own deployed app or bring up their app via a docker-compose
  overlay.

Both callers share the **same image** and **same testing framework**. The
difference is purely orchestration:

| Concern | MJ Monorepo | External Adopter |
|---|---|---|
| Source of test definitions | `metadata/tests/regression/*.json` | A scaffolded `metadata/tests/` dir generated by `init` |
| Stack being tested | Local docker MJ Explorer | A remote URL, or an app brought up by their own overlay |
| Results recording DB | Ephemeral docker SQL Server | The same or an external "runs DB" |
| Comparison baseline | Per-run `results.json` artifacts | Optionally an archive MJ instance |

The two key abstractions that enable the same code to serve both callers:

1. **Target profiles** (§8) — JSON files describing *where to run* (URL,
   auth, suite name, archive destination). Decouple "what to test" from
   "where to test it."
2. **Variable substitution** (§9) — Tests reference `{{baseUrl}}` /
   `{{authUsername}}` placeholders instead of hardcoded values. The same
   test JSON works across Mode A/B/C/D without modification.

[docker-hub-image]: https://hub.docker.com/r/memberjunction/agentic-test-runner

---

## 2. The Four Run Modes

The runner supports four distinct modes. They share the **same image**, the
**same TestingFramework**, and the **same compose project**; they differ in
which containers boot, where `startUrl` points, and where results land.

```
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│  Mode A — Self-Contained MJ Regression                                     │
│    Full 5-container stack: sqlserver + db-setup + mjapi + mjexplorer       │
│    + test-runner. Tests target localhost:4200 via socat. Results in        │
│    ephemeral DB + run-*/results.json. Used in CI to gate releases.         │
│    Profile: `full`. Entrypoint: test-runner-entrypoint.sh.                 │
│                                                                            │
│  Mode B — Remote MJ Target                                                 │
│    Test-runner alone (with a results DB). Targets a remote MJ URL          │
│    (staging / customer / production). Reuses the canonical MJ Explorer     │
│    Regression Suite tests with `{{baseUrl}}` substitution. Results in      │
│    JSON; optionally pushed to a "results archive" MJ instance.             │
│    Profile: `full` (default — local DB) or `remote-target` (--no-local-stack). │
│    Entrypoint: test-runner-remote-entrypoint.sh.                           │
│                                                                            │
│  Mode C — Generic Web App                                                  │
│    Same compose shape as Mode B. Targets any non-MJ URL. Tests authored    │
│    per-app, no MJ assumptions. Custom oracles plug in via                  │
│    `--oracles-module`. Results JSON only.                                  │
│    Profile: `full` or `remote-target`.                                     │
│    Entrypoint: test-runner-remote-entrypoint.sh.                           │
│                                                                            │
│  Mode D — Bring-Your-Own-App                                               │
│    User supplies a docker-compose overlay that boots their app alongside   │
│    the test-runner inside the regression network. Same target profile     │
│    shape as Mode C; the only addition is `--overlay=path/to/file.yml`     │
│    on the CLI invocation. Runner reaches the app at its compose service   │
│    name (e.g. http://my-app:3000).                                         │
│    Profile: `full` (local DB for results). Entrypoint chosen by env.       │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### Mode selector

The CLI infers the mode from the flags passed to `mj test regression`:

| Command | Mode |
|---|---|
| `mj test regression up` | A |
| `mj test regression remote --target=<remote-mj>` | B |
| `mj test regression remote --target=<generic-web>` | C |
| `mj test regression remote --target=<x> --overlay=<x>.yml` | D |

Under the hood, all four invoke `docker compose --profile <p> up` with
appropriate env vars and (for B/C/D) the remote entrypoint dispatcher.

---

## 3. Docker Image Topology

There are **two distinct images** in this stack with a deliberate dependency
direction:

```
  ┌──────────────────────────────────────────────────────────┐
  │   memberjunction/agentic-test-runner:v<MJServer-version> │
  │   (PUBLISHED — docker/agentic-test-runner/Dockerfile)    │
  │                                                          │
  │   • Playwright + Chromium                                │
  │   • Full monorepo `dist/` (mj CLI, TestingFramework,     │
  │     Computer Use, ComputerUseTestDriver, oracles, ...)   │
  │   • Dispatcher entrypoint (run | import-bacpac | export) │
  │   • (init templates ship with @memberjunction/cli)       │
  │   • No MJ-specific config or socat shim                  │
  └──────────────────────────────────────────────────────────┘
                              │  consumed by
                              ▼
  ┌──────────────────────────────────────────────────────────┐
  │   docker/regression/Dockerfile.test-runner               │
  │   (MJ-SPECIFIC — used by the regression compose project) │
  │                                                          │
  │   Adds:                                                  │
  │   • socat (localhost:4200 → mjexplorer:4200 shim)        │
  │   • MJ-specific entrypoint (test-runner-entrypoint.sh)   │
  │   • MJ test-user provisioning + AssociationDB setup      │
  │                                                          │
  │   Result: the test-runner container in the 5-service     │
  │   Mode A compose stack.                                  │
  └──────────────────────────────────────────────────────────┘
```

> **Current state:** the published image's Dockerfile exists at
> [`docker/agentic-test-runner/Dockerfile`](../agentic-test-runner/Dockerfile),
> and the CI workflow at [`.github/workflows/docker.yml`](../../.github/workflows/docker.yml)
> is staged to publish it alongside `memberjunction/api`. As of the most
> recent change, the image has **not yet been published**, and
> `Dockerfile.test-runner` is **still self-contained** (not yet a thin
> `FROM memberjunction/agentic-test-runner:v<X>` overlay). The dependency
> flip is the final step gated on the first successful image publish — see
> [LIMITATIONS.md §8](LIMITATIONS.md#8-future-phases-not-yet-implemented).

### What the published image includes

| Inside `/app/` | Purpose |
|---|---|
| `dist/` of every `@memberjunction/*` package | Built TS → JS for the runtime |
| `mj` CLI on `$PATH` (via `npx`) | Entry point for `mj test suite`, `mj test compare`, `mj sync` |
| `bootstrap.mjs` | One-line `import '@memberjunction/computer-use-engine'` so `@RegisterClass(BaseTestDriver, 'ComputerUseTestDriver')` fires before the CLI runs |
| `mj.config.cjs` | Default config copied from [`docker/MJAPI/docker.config.cjs`](../MJAPI/docker.config.cjs); can be overridden by mount |
| `/usr/local/bin/agentic-test-runner` | Dispatcher script (entrypoint) — supports `run`, `import-bacpac`, `export`, `exec`, `help` |

---

## 4. Compose Profiles

Every service in [`docker-compose.test.yml`](docker-compose.test.yml) is
**profile-gated**. There is no "default" — running `docker compose up` with
no profile starts zero services (by design — a hard signal that the user
should go through `mj test regression *`).

| Profile | Services included | Mode |
|---|---|---|
| `full` | sqlserver, db-setup, mjapi, mjexplorer, test-runner | A; also B/C/D when the local stack records results |
| `remote-target` | test-runner only | B/C with `--no-local-stack` (external results DB) |
| `gen-forms` | sqlserver, form-generator | One-shot Angular form-codegen run |

`sqlserver` is in both `full` and `gen-forms` because both need a database
to bootstrap. `test-runner` is in both `full` and `remote-target` because
those are the only modes that run a suite.

### Why fully profile-gated

Before Phase 4 the services had no profiles — `docker compose up` brought
the whole stack online. That made it ambiguous which mode you were in if a
flag was missing. With everything profile-gated:

- The CLI is the single source of truth for "which profile to activate."
- Adding a new mode is one new profile string; no service-classification
  refactor required.
- A bare `docker compose up` failing fast (zero services) is a much
  better error than silently entering the wrong mode.

### `depends_on` semantics across profiles

`test-runner` has `depends_on: { mjapi, mjexplorer }` with
`required: false`. When the active profile is `remote-target` (where
`mjapi`/`mjexplorer` aren't in the profile), compose **skips the
dependency** instead of erroring. This is what lets the same service
definition serve both Mode A (waits for MJExplorer) and Modes B/C/D
(MJExplorer isn't running — proceed directly).

---

## 5. Container Inventory

### sqlserver

- **Image:** `mcr.microsoft.com/mssql/server:2022-latest` (platform: `linux/amd64`)
- **Host port:** 11433 → container 1433
- **Profile:** `full`, `gen-forms`
- **Healthcheck:** `sqlcmd … SELECT 1` every 10s
- **Volume:** *(none — DB is ephemeral; wiped on `down -v`)*
- **SA password:** from `TEST_SA_PASSWORD` env (default `TestPassword123!`)

### db-setup

- **Dockerfile:** [`Dockerfile.db-setup`](Dockerfile.db-setup) (FROM `Dockerfile.api`-shape)
- **Profile:** `full`
- **Lifecycle:** one-shot. Exits 0 on success, then mjapi can start.
- **Entrypoint:** [`db-setup-entrypoint.sh`](db-setup-entrypoint.sh)
- **Steps:**
  1. `bootstrap-db.cjs` — creates `MemberJunction_Test`, installs AssociationDB demo schema (58 tables, 10K+ rows)
  2. `mj migrate` — runs Flyway migrations to create `__mj` schema
  3. `mj codegen` — generates entity classes, views, stored procs, GraphQL resolvers, EntityField metadata
  4. `mj sync push` of `metadata/applications/`
  5. `mj sync push` of `metadata/prompts/` — picks up Computer Use controller/judge prompts
- **Bacpac mode** (`BACPAC_FILE` set, via `--bacpac` + the `docker-compose.bacpac.yml` overlay): the entrypoint branches to [`import-bacpac.cjs`](scripts/import-bacpac.cjs) (SqlPackage `/Action:Import` into `MemberJunction_Test`) → optional `mj migrate` + `mj codegen` (gated by `BACPAC_UPGRADE`, default on; guarded against mis-baselining a DB with no `flyway_schema_history`) → push prompts. AssociationDB + the standard app/test seeding are skipped; the user supplies their suite via `EXTRA_METADATA_DIRS`. The overlay pins db-setup to `linux/amd64` so the x64 SqlPackage build runs.
- **Why a separate container?** Migration + codegen + AssociationDB install takes ~2-3 minutes. Keeping them out of MJAPI's Dockerfile keeps the runtime image lean and lets `service_completed_successfully` block dependents until the DB is fully ready.

### mjapi

- **Dockerfile:** [`Dockerfile.api`](Dockerfile.api)
- **Host port:** 14000 → container 4000
- **Profile:** `full`
- **Depends on:** `db-setup` (with `condition: service_completed_successfully`)
- **Entrypoint:** runs Express/Apollo via `npm start`
- **Restart policy:** `on-failure:5` — auto-restart on OOM under heavy suite load
- **Build-time customization:** [`scripts/patch-test-api-config.cjs`](scripts/patch-test-api-config.cjs) flips `autoCreateNewUsers`, `newUserRoles`, and `CreateUserApplicationRecords` for the test environment
- **Healthcheck:** `curl /healthcheck` every 10s

### mjexplorer

- **Dockerfile:** [`Dockerfile.explorer`](Dockerfile.explorer) (multi-stage)
- **Host port:** 4200 → container 4200
- **Profile:** `full`
- **Build args:** `AUTH0_DOMAIN`, `AUTH0_CLIENTID` (Angular AOT build bakes them in)
- **Runtime:** nginx:alpine serving the built Angular app + reverse-proxying `/api/` to `http://mjapi:4000/`
- **nginx config:** [`nginx.conf`](nginx.conf) — uses a `map $http_upgrade $connection_upgrade` block to conditionally set the `Connection` header so GraphQL POSTs don't hang on a phantom WebSocket upgrade
- **Build-time customization:** [`scripts/gen-environment-ts.cjs`](scripts/gen-environment-ts.cjs) generates `src/environments/environment.ts` from build args

### test-runner

- **Dockerfile:** [`Dockerfile.test-runner`](Dockerfile.test-runner)
- **Base:** `mcr.microsoft.com/playwright:v1.58.1-noble` (Playwright + Chromium)
- **Profile:** `full`, `remote-target`
- **Depends on:** `mjapi` (healthy), `mjexplorer` (started) — both `required: false` so the runner can run alone in `remote-target` profile
- **Entrypoint:** **selected by env var** at compose-parse time — `entrypoint: ["/bin/bash", "/app/${TEST_RUNNER_ENTRYPOINT:-test-runner-entrypoint.sh}"]`
- **Volumes** (bind-mounted from the host):
  - `./test-results:/app/test-results` (RW — output dir)
  - `./test-runner-entrypoint.sh:/app/test-runner-entrypoint.sh:ro`
  - `./test-runner-remote-entrypoint.sh:/app/test-runner-remote-entrypoint.sh:ro`
  - `./scripts:/app/docker/regression/scripts:ro` — `.cjs` helpers
  - `./archive:/app/docker/regression/archive:ro` — archive entity-cascade configs
  - `./targets:/app/docker/regression/targets:ro` — target profiles
  - `../../metadata:/app/metadata` (RW)
  - `./test-metadata:/app/test-metadata:ro` — Docker-only test user/tags
- **Shared memory:** `shm_size: 4gb` (Chromium tab stability)
- **Bootstrap:** `NODE_OPTIONS="--import /app/bootstrap.mjs"` ensures `@RegisterClass` fires before the CLI runs

### form-generator (opt-in)

- **Profile:** `gen-forms` only
- **Reuses:** `Dockerfile.db-setup` (same monorepo build, same `mj` CLI)
- **Purpose:** runs codegen against a temporary stack to produce Angular entity-form components into `.docker-generated/MJExplorer-forms/` on the host
- **Why separate?** Angular forms are generated from DB schema, and the DB must exist for codegen to run. That setup can't run inside a Docker `build` step. So the form-generator spawns a temp stack, runs codegen, captures output to a host bind mount, and tears itself down. The output is then COPYed into `mjexplorer` at image-build time.
- **Lifecycle:** one-shot. Runs only when explicitly invoked via `mj test regression gen-forms` (or the build-time guard in `mj test regression build`).

### byo-app (Mode D overlay-supplied)

- **Defined by:** the user's overlay (scaffold one via `mj test regression init bring-your-own-app` for a Mode D template)
- **Profile:** `full` (joins the `--profile full` startup)
- **Added by overlay** to the existing compose project; not present in the base
- **Conventional service name:** anything — the runner reaches it via the name set in the target profile's `baseUrl`

---

## 6. Entrypoint Dispatch

The `test-runner` service uses an **env-var-driven entrypoint selector** in
the compose YAML:

```yaml
test-runner:
  entrypoint: ["/bin/bash", "/app/${TEST_RUNNER_ENTRYPOINT:-test-runner-entrypoint.sh}"]
```

This lets the same container image run two completely different orchestration
flows without rebuilding:

| `TEST_RUNNER_ENTRYPOINT` value | Script | When used |
|---|---|---|
| *(unset — default)* | `test-runner-entrypoint.sh` | Mode A (self-contained MJ stack) |
| `test-runner-remote-entrypoint.sh` | `test-runner-remote-entrypoint.sh` | Modes B/C; sometimes D |

The CLI sets `TEST_RUNNER_ENTRYPOINT` from the target profile's loader output
(see §8). The Mode D path inherits the env var setting if the target profile
loaded set it; otherwise the overlay's hardcoded env vars may override.

### Full-stack entrypoint — `test-runner-entrypoint.sh`

Used for Mode A. Big steps:

1. **socat shim:** `socat TCP-LISTEN:4200,fork,reuseaddr TCP:mjexplorer:4200 &` — Auth0's `auth0-spa-js` requires a secure origin, and browsers treat `localhost` as secure but **not** arbitrary Docker hostnames like `mjexplorer`. socat forwards traffic so the browser thinks it's hitting `localhost:4200`.
2. **Application metadata sync:** `mj sync push --dir=metadata --include="applications"`
3. **Test-user metadata sync:** `mj sync push --dir=/app/test-metadata --include="tags,users"`
4. **SQL safety-net:** `scripts/setup-test-user.cjs` direct SQL upserts for the test user, roles, applications, and example data — guards against `mj sync` failing on first run (chicken-and-egg)
5. **Test/suite metadata sync:** `mj sync push --dir=metadata --include="tests"` then `…include="test-suites"`
6. **Extra metadata dirs (optional):** for Mode D / BYO — push anything in `EXTRA_METADATA_DIRS` (comma-separated)
7. **Preflight diagnostics:** `scripts/preflight-checks.cjs` probes MJAPI/nginx/socat/Auth0 + memory snapshot → `preflight.json`
8. **Per-run output dir:** `test-results/run-{TIMESTAMP}/` with `screenshots/`
9. **Background health monitor:** `scripts/health-monitor.cjs` writes `diagnostics.json` every 10s while the suite runs
10. **Run the suite:** `npx mj test suite --name "${TEST_SUITE_NAME:-MJ Explorer Regression Suite}" --parallel --max-parallel ${MAX_PARALLEL_WORKERS:-4}` (with `--oracles-module=…` if `ORACLES_MODULE` is set)
11. **Stop health monitor**
12. **Extract screenshots** from `__mj.vwTestRunOutputs` → `screenshots/<test>/step_NN.png`
13. **Generate markdown report:** `scripts/generate-md-report.cjs`
14. **Generate HTML report:** `scripts/generate-html-report.cjs` (lightbox gallery)
15. **Optional archive flow:** if `ARCHIVE_DB_DATABASE` is set, pull the suite run + children to JSON, tag, push to destination MJ
16. **Update `test-results/latest` symlink**

### Remote entrypoint — `test-runner-remote-entrypoint.sh`

Used for Modes B/C. Mirror of the above with these differences:

| Skipped | Why |
|---|---|
| socat shim | Target is reachable directly at `MJ_TEST_VAR_baseUrl` |
| Application metadata sync | No MJ Explorer to seed |
| Test-user metadata sync | No local DB to provision |
| `setup-test-user.cjs` | No local DB |
| Preflight + health monitor | Less critical for remote runs; can be added if needed |

The remote entrypoint also enforces required env-var guards: it exits 1 with
a clear FATAL message if `MJ_TEST_VAR_baseUrl` or `TEST_SUITE_NAME` is
missing.

---

## 7. Control Flow — A Single Run

Here's a complete trace of what happens when a developer runs
`mj test regression up` (Mode A). Sub-second timings approximate.

```
┌─ HOST ─────────────────────────────────────────────────────────────────┐
│                                                                        │
│  $ mj test regression up                                               │
│   │                                                                    │
│   ▼ requireMonorepoRoot() — verifies docker/regression/                │
│     docker-compose.test.yml exists in cwd                              │
│   │                                                                    │
│   ▼ spawnInherit('docker', ['compose', '-f',                           │
│     'docker/regression/docker-compose.test.yml',                       │
│     '--env-file', 'docker/regression/.env.test',                       │
│     '--profile', 'full', 'up'])                                        │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─ DOCKER COMPOSE ───────────────────────────────────────────────────────┐
│                                                                        │
│  T+0s    sqlserver starts. Healthcheck: TCP/1433 + SELECT 1.           │
│  T+15s   sqlserver healthy → db-setup starts.                          │
│  T+15s   db-setup: bootstrap-db.cjs → AssociationDB install (~30s)     │
│  T+45s   db-setup: mj migrate (Flyway migrations, ~30s)                │
│  T+75s   db-setup: mj codegen (entity classes, views, sprocs, ~60s)    │
│  T+135s  db-setup: mj sync push applications/                          │
│  T+140s  db-setup: mj sync push prompts/                               │
│  T+150s  db-setup exits 0 → mjapi can start.                           │
│  T+150s  mjapi starts. Healthcheck: GET /healthcheck (curl).           │
│  T+165s  mjapi healthy → mjexplorer can start.                         │
│  T+165s  mjexplorer starts. nginx serves /usr/share/nginx/html.        │
│  T+170s  mjexplorer started → test-runner can start.                   │
│                                                                        │
│  T+170s  test-runner starts → entrypoint dispatched.                   │
│          (TEST_RUNNER_ENTRYPOINT unset → test-runner-entrypoint.sh)    │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─ TEST RUNNER CONTAINER ────────────────────────────────────────────────┐
│                                                                        │
│  socat localhost:4200 → mjexplorer:4200  (bg)                          │
│  npx mj sync push --dir=metadata --include="applications"              │
│  npx mj sync push --dir=/app/test-metadata --include="tags,users"      │
│  node scripts/setup-test-user.cjs    # SQL safety-net                  │
│  npx mj sync push --dir=metadata --include="tests"                     │
│  npx mj sync push --dir=metadata --include="test-suites"               │
│  node scripts/preflight-checks.cjs   # MJAPI/nginx/socat/Auth0         │
│  mkdir test-results/run-${TIMESTAMP}                                   │
│  node scripts/health-monitor.cjs &   # bg loop                         │
│                                                                        │
│  ─── SUITE EXECUTION ────────────────────────────────────────────────  │
│                                                                        │
│  npx mj test suite \                                                   │
│      --name "MJ Explorer Regression Suite" \                           │
│      --format json \                                                   │
│      --output test-results/run-${TS}/results.json \                    │
│      --parallel --max-parallel 4                                       │
│                                                                        │
│  └─ TestEngine.RunSuite(suiteId, options)                              │
│       │                                                                │
│       ├─ load suite metadata + tests from DB                           │
│       ├─ extract Configuration.applicationContext from suite           │
│       ├─ parse --var flags into resolvedVariables                      │
│       ├─ for each test, in parallel (workers=4):                       │
│       │    ├─ build DriverExecutionContext                             │
│       │    ├─ ClassFactory.CreateInstance(BaseTestDriver,              │
│       │    │     test.TypeID.DriverClass) → ComputerUseTestDriver      │
│       │    ├─ driver.Execute(test, context)                            │
│       │    │   │                                                       │
│       │    │   ├─ substituteVariables(InputDefinition, values)         │
│       │    │   │   replaces {{baseUrl}}, {{authUsername}}, etc.        │
│       │    │   ├─ HeadlessBrowserEngine.GetIsolated(workerKey)         │
│       │    │   │   fresh BrowserContext, storageState replayed         │
│       │    │   │   if cached for this worker                           │
│       │    │   ├─ ComputerUseEngine.Run(params)                        │
│       │    │   │   ├─ navigate to startUrl                             │
│       │    │   │   ├─ loop:                                            │
│       │    │   │   │   ├─ screenshot                                   │
│       │    │   │   │   ├─ AIPromptRunner.run(controller prompt)        │
│       │    │   │   │   │   → action: click/type/scroll/done            │
│       │    │   │   │   ├─ Playwright executes action                   │
│       │    │   │   │   ├─ if step % judgeFrequency == 0:               │
│       │    │   │   │   │   AIPromptRunner.run(judge prompt)            │
│       │    │   │   │   │   → verdict: Done? Confidence? Reason         │
│       │    │   │   │   └─ persist screenshot + step to DB              │
│       │    │   │   └─ exit when Done=true OR maxSteps reached          │
│       │    │   └─ evaluate oracles (built-in + custom):                │
│       │    │       goal-completion (judge verdict)                     │
│       │    │       url-match (regex)                                   │
│       │    │       step-count (within budget)                          │
│       │    │       <plugin oracles>                                    │
│       │    └─ HeadlessBrowserEngine.ReleaseIsolated(adapter)           │
│       │       captures storageState before closing the context         │
│       └─ aggregate TestRun + TestRunOutput rows → SuiteRunResult       │
│                                                                        │
│  kill $health_monitor_pid                                              │
│  node scripts/extract-screenshots.cjs    # __mj.vwTestRunOutputs → PNG │
│  node scripts/generate-md-report.cjs                                   │
│  node scripts/generate-html-report.cjs                                 │
│  if ARCHIVE_DB_DATABASE set: mj sync pull + tag + push to archive MJ   │
│  ln -sfn run-${TIMESTAMP} test-results/latest                          │
│  exit ${suite_exit_code}                                               │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
                          $ open test-results/latest/report.html
```

---

## 8. Target Profile System

A **target profile** is a JSON file describing *where to run* the suite. It
keeps environment-specific config (URLs, credentials, archive destinations)
out of test definitions, so the same test JSON works against multiple
deployments without modification.

### Schema

```jsonc
{
  "schemaVersion": "1.0",                            // required, currently always "1.0"
  "name": "staging-mj",                              // required, short identifier (used for bare-name lookup)
  "kind": "mj-explorer",                             // required: mj-explorer | generic-web | mj-api-only
  "description": "...",                              // optional, free-form

  // ─── What URL to test ────────────────────────────────
  "baseUrl": "https://staging-mj.example.com",       // required
  "allowedDomains": ["staging-mj.example.com",       // optional, array of host patterns
                     "*.auth0.com"],

  // ─── Auth (optional — empty bindings for unauthed apps) ─────────
  "auth": {
    "username": "env:STAGING_TEST_USER",             // resolved from host env
    "password": "env:STAGING_TEST_PASSWORD",         // resolved from host env
    "domains": ["staging-mj.example.com",
                "*.auth0.com"]
  },

  // ─── Which suite to run ───────────────────────────────
  "suite": "MJ Explorer Regression Suite",           // optional; defaults to MJ suite

  // ─── Extra metadata directories to push before the suite runs ──
  "extraMetadataDirs": [                             // optional
    "/app/my-app-metadata"
  ],

  // ─── Custom oracles ────────────────────────────────
  "oraclesModule": "/app/.../my-oracles.cjs",        // optional, path inside the container

  // ─── Where to forward results ────────────────────────
  // The destination DB comes from the ARCHIVE_DB_* env vars (set in .env.test).
  // The profile only carries the tag/source labels for `mj test compare`.
  "archive": {                                       // optional
    "tag": "staging-nightly",
    "source": "staging-mj"
  }
}
```

### Loader — `scripts/load-target-profile.cjs`

Runs **host-side** (invoked by the CLI). Steps:

1. Read the JSON file at the given path.
2. Validate required fields (`name`, `kind`, `baseUrl`).
3. Validate `kind` is one of: `mj-explorer`, `generic-web`, `mj-api-only`.
4. For each `env:VARNAME` reference, look it up in `process.env`. Exit 1 if missing.
5. Auto-JSON-stringify array-typed values (`allowedDomains`, `auth.domains`) before exporting them.
6. Emit env-var assignments in one of two formats:
   - `--format=env` (default): shell `export FOO='bar'` lines, POSIX-safe (single-quote escape for embedded quotes).
   - `--format=json`: a single JSON object with all key/value pairs.

The CLI uses `--format=json` (cleaner parsing in TypeScript); a developer
debugging can use `eval "$(node load-target-profile.cjs my.json)"` for
inspection.

### Env-var translation

| Target field | Emitted env var |
|---|---|
| `baseUrl` | `MJ_TEST_VAR_baseUrl` |
| `allowedDomains` | `MJ_TEST_VAR_allowedDomains` (JSON-stringified array) |
| `auth.username` | `MJ_TEST_VAR_authUsername` (env-ref resolved) |
| `auth.password` | `MJ_TEST_VAR_authPassword` (env-ref resolved) |
| `auth.domains` | `MJ_TEST_VAR_authDomains` (JSON-stringified array) |
| `suite` | `TEST_SUITE_NAME` |
| `extraMetadataDirs` | `EXTRA_METADATA_DIRS` (comma-joined) |
| `oraclesModule` | `ORACLES_MODULE` |
| `archive.tag` | `ARCHIVE_TAG` |
| `archive.source` | `ARCHIVE_SOURCE` |
| *(always)* | `TEST_RUNNER_MODE=remote`, `TEST_RUNNER_ENTRYPOINT=test-runner-remote-entrypoint.sh`, `TARGET_PROFILE_NAME`, `TARGET_PROFILE_KIND` |

### Resolution by short name vs. path

`mj test regression remote --target=<value>`:

- If `<value>` contains a `/` or ends with `.target.json` → treated as a path
- Otherwise → resolved to `docker/regression/targets/<value>.target.json`

This is what lets `mj test regression remote --target=byo-app` work as a
shorthand for the full file path.

### Committed target files

| File | Use |
|---|---|
| [`targets/staging-mj.example.target.json`](targets/staging-mj.example.target.json) | Mode B example — points at a fictitious staging MJ deployment with auth via `env:` refs |
| [`targets/generic-web.example.target.json`](targets/generic-web.example.target.json) | Mode C example — no auth, custom variables block |
| [`targets/byo-app.target.json`](targets/byo-app.target.json) | Mode D — paired with the overlay scaffolded by `mj test regression init bring-your-own-app` |

`.example.target.json` files are committed scaffolds — copy them to
`<name>.target.json` (gitignored) and edit. The `.gitignore` in `targets/`
allows the checked-in `*.example.target.json` files + `byo-app.target.json` through.

---

## 9. Variable Substitution

Test JSONs reference `{{key}}` placeholders that get resolved at runtime
against a values map. Lives in
[`packages/AI/MJComputerUse/src/utils/variable-substitution.ts`](../../packages/AI/MJComputerUse/src/utils/variable-substitution.ts).

### Two sources, two precedences

```
buildVariableValuesFromContext(context, env=process.env) → values: Record<string, unknown>

  Layer 1 (low precedence):  env vars matching MJ_TEST_VAR_*
                             • key = env name minus "MJ_TEST_VAR_" prefix
                             • JSON-parse the value if it looks structured

  Layer 2 (overrides):       context.resolvedVariables.values
                             • set by `--var key=value` CLI flags
                             • or by the TestingFramework's VariableResolver
                               (with schema validation)
```

Layer 2 wins on conflict. Most callers (the docker entrypoint, BYO overlay,
target-profile loader) populate Layer 1 via env vars; the resolver is for
developers passing `--var` flags directly.

### Substitution semantics

`substituteVariables(obj, values)` is recursive and pure (returns a new object).

- **Whole-string placeholder** (`"{{baseUrl}}"`) — replaced with the **raw value** from the map, preserving non-string types. E.g. `{{allowedDomains}}` resolves to an array, not a string.
- **Embedded placeholder** (`"see {{baseUrl}}/path"`) — always produces a string; non-string values are coerced via `String()`.
- **Unknown key** — left in place verbatim. No error. This lets authors ship partially-parameterized tests.

### Auto-JSON-parsing for env-var Layer 1

A real-world quirk: env vars are always strings. If `MJ_TEST_VAR_allowedDomains` is `'["a","b"]'`, the substitution would inject the **literal string** `'["a","b"]'` into an array-typed field. The loader detects "looks like JSON" (starts with `[`, `{`, `"`, or a digit / boolean / null) and `JSON.parse()`s it before storing. Falls back to the raw string on parse failure or plain text.

### Where substitution fires

The driver applies substitution at three points before calling the engine:

```typescript
// packages/AI/MJComputerUse/src/test-driver/ComputerUseTestDriver.ts
const values = buildVariableValuesFromContext(context);
const config = substituteVariables(test.Configuration, values);
const input = substituteVariables(test.InputDefinition, values);
const expected = substituteVariables(test.ExpectedOutcomes, values);
```

Plus the suite-level applicationContext (§11) and any oracle `pattern` configs.

---

## 10. Custom Oracles

An **oracle** is a deterministic check that evaluates the test's output and
produces a `{ passed, score, message, details? }` result. Each test
configures one or more oracles (with weights); the weighted average becomes
the test's final score.

### Built-in oracles (Computer Use)

| Type | Source | What it checks |
|---|---|---|
| `goal-completion` | [`GoalCompletionOracle.ts`](../../packages/AI/MJComputerUse/src/test-driver/oracles/GoalCompletionOracle.ts) | The judge LLM verdict `Done=true` + confidence ≥ minConfidence |
| `url-match` | [`UrlMatchOracle.ts`](../../packages/AI/MJComputerUse/src/test-driver/oracles/UrlMatchOracle.ts) | The final URL matches a regex pattern |
| `step-count` | [`StepCountOracle.ts`](../../packages/AI/MJComputerUse/src/test-driver/oracles/StepCountOracle.ts) | The agent finished within maxSteps |

Built-in oracles for non-Computer-Use test types live in
[`packages/TestingFramework/Engine/src/oracles/`](../../packages/TestingFramework/Engine/src/oracles/)
and cover schema validation, LLM-judge, exact-match, SQL validation, and trace validation.

### The IOracle interface

```typescript
export interface IOracle {
    readonly type: string;                         // unique id (e.g. 'final-url-host')
    evaluate(input: OracleInput,                   // { actualOutput, expectedOutput, targetEntity, contextUser }
             config: OracleConfig): Promise<OracleResult>;
}
```

`OracleResult` shape:

```typescript
{
    oracleType: string;     // mirrors this.type
    passed: boolean;
    score: number;          // 0.0–1.0
    message: string;        // human-readable
    details?: unknown;      // optional structured context for reports
}
```

### Plugging in custom oracles — `--oracles-module`

External adopters write their own `IOracle` implementations and load them at
suite startup:

```bash
mj test suite --oracles-module=path/to/my-oracles.cjs --name "My Suite"
```

Or via a target profile's `oraclesModule` field (works through
`mj test regression remote`).

The loader at
[`packages/TestingFramework/CLI/src/utils/oracle-module-loader.ts`](../../packages/TestingFramework/CLI/src/utils/oracle-module-loader.ts)
**duck-types** each export — anything with `type: string` and an async
`evaluate()` gets registered. Helpers, constants, unrelated exports are
silently skipped.

Two export styles are both supported:

```javascript
// Class style — instantiated with no args
class FinalUrlHostOracle {
    constructor() { this.type = 'final-url-host'; }
    async evaluate(input, config) { /* … */ }
}

// Instance style — used as-is
const myOracle = {
    type: 'my-oracle',
    async evaluate(input, config) { /* … */ },
};

module.exports = { FinalUrlHostOracle, myOracle };
```

Reference implementation at
`generic-web/oracles/mdn-oracles.cjs` (scaffold it via `mj test regression init generic-web`).

### Registration order + override

Custom oracles registered via the loader land in the **engine-level**
registry. Computer Use's three built-ins are scoped to the driver's
`builtInOracles` map (which **takes precedence** over engine-level oracles
of the same type). So a custom `goal-completion` plugin would NOT override
the driver's; a custom `final-url-host` would be unique and would register.
This boundary is by design — built-ins are part of the test contract.

---

## 11. Suite-Level Application Context

A suite can declare a markdown blob of "what the controller LLM should know
before the first step" via `MJTestSuiteEntity.Configuration.applicationContext`.
This appears in the controller prompt on every step (Anthropic prompt-caching
amortizes the cost across the run), helping the agent skip rediscovery of
the app's navigation landmarks, common gotchas, etc.

### Wiring

1. **Suite metadata:** `metadata/test-suites/.regression-suite.json` has a
   `Configuration` field (JSON string column) with shape:
   ```jsonc
   {
     "applicationContext": "## MJ Explorer\n\nThe top nav has...\n### Common gotchas\n- Wait up to 30s for transitions...\n"
   }
   ```
2. **TestEngine.RunSuite** parses `suite.Configuration` JSON, extracts
   `applicationContext`, attaches it to `DriverExecutionContext.suiteContext`.
3. **ComputerUseTestDriver.Execute** reads `context.suiteContext.applicationContext`,
   applies `{{var}}` substitution (so it can reference `{{baseUrl}}`),
   concatenates with the per-test override (`test.InputDefinition.applicationContext`)
   under a `## Test-specific Notes` heading.
4. **ComputerUseEngine** forwards `params.ApplicationContext` into the
   controller prompt request on every step.
5. **Controller prompt template**
   ([`metadata/prompts/templates/computer-use/controller.template.md`](../../metadata/prompts/templates/computer-use/controller.template.md))
   has a `{% if applicationContext %}` block that renders the context under
   a `## Application Context` heading.

### Composition rules

`composeApplicationContext(suiteLevel, perTest, values)` in
[`variable-substitution.ts`](../../packages/AI/MJComputerUse/src/utils/variable-substitution.ts):

- Returns `undefined` when both layers are empty (engine then skips the
  heading entirely — no empty section rendered).
- Returns just the suite layer when only that exists.
- Returns just the per-test layer (under `## Test-specific Notes`) when only
  that exists.
- Returns both joined when both exist, suite first.
- Applies `{{var}}` substitution to each layer before joining.

> **Current state:** the plumbing is fully wired and exercised by the BYO
> suite (`Configuration.applicationContext` is 853 chars). The canonical
> MJ Explorer Regression Suite **does not yet have an applicationContext
> authored** — see [LIMITATIONS.md §2](LIMITATIONS.md#2-suite-level-application-context-phase-1b).

---

## 12. Browser Context Isolation

Each test runs in a **fresh Playwright `BrowserContext`** seeded with the
worker's cached `storageState` (cookies + localStorage). This avoids:

- **Test pollution** — cookies set in test 5 don't leak into test 6.
- **Cross-test side effects** — IndexedDB / sessionStorage entries from one
  test don't trip oracles in another.
- **Heuristic state-cleanup bugs** — the old shared-context cleanup pass
  used regex matching on localStorage keys (e.g. `/auth0|msal|okta/`) and
  was fragile; non-matching custom auth schemes got nuked.

### How it stays fast (auth replay)

A naive "fresh context per test" approach would re-trigger Auth0's login
flow (~10-20s) on every test. To avoid this:

1. After the **first** test in a worker successfully authenticates,
   `HeadlessBrowserEngine` calls `context.storageState()` and caches the
   result keyed by worker index.
2. On **subsequent** tests in the same worker, the new context is created
   with `browser.newContext({ storageState: cached })` so cookies + localStorage
   are pre-populated.
3. The `AuthHandler` sees the user is already logged in and short-circuits —
   no Auth0 round-trip needed.

### Token expiry handling

If a cached `storageState` becomes stale mid-suite (Auth0 token expired,
which happens on hour-long suites with default token TTLs), the page lands
on the login screen despite injection. The driver detects this and calls
`engine.InvalidateStorageState(workerKey)`, letting `AuthHandler` re-run
once; the new state is then captured and used for the rest of the worker's
tests.

### Opt-in shared sessions

The default `browserSession` strategy is `"new"` (isolated per test). Two
opt-ins remain for rare tests that genuinely need cross-test state:

- `browserSession: "shared:suite"` — single context across the worker's
  whole suite. Use when test N+1 verifies an artifact created by test N.
- `browserSession: "shared:global"` — single context across all workers.
  Almost never the right choice; degrades parallelism.

Both opt-ins emit a one-time warning when used.

---

## 13. Archive Flow

Optional pipeline that forwards a completed suite run + all child records to
a destination MJ instance (an "archive MJ"). Triggered when the
`ARCHIVE_DB_DATABASE` env var is set. Uses `mj-sync` for the wire protocol —
no custom sink code.

### Configuration contract

The archive flow is **env-var driven** — no files to copy, no `.cjs` to edit.
A transient `mj.config.cjs` is generated at runtime from the vars below.

| Env var | Required? | Default | Notes |
|---|---|---|---|
| `ARCHIVE_DB_DATABASE` | yes | — | destination database name |
| `ARCHIVE_DB_USERNAME` | yes | — | destination SQL user |
| `ARCHIVE_DB_PASSWORD` | yes | — | destination SQL password |
| `ARCHIVE_DB_HOST` | no | `host.docker.internal` | works for most setups; override for in-network or remote destinations |
| `ARCHIVE_DB_PORT` | no | `1433` | |
| `ARCHIVE_DB_TRUST_CERT` | no | `true` | |
| `ARCHIVE_USER_EMAIL` | no | `not.set@nowhere.com` | user that owns the archived records; must exist in destination |
| `ARCHIVE_MJ_CORE_SCHEMA` | no | `__mj` | schema name on destination |
| `ARCHIVE_TAG` | no | `<TEST_SUITE_NAME>` | written into `TestSuiteRun.Tags` |
| `ARCHIVE_SOURCE` | no | empty | written into `TestSuiteRun.MachineName` |

### Network topology cheat-sheet

| Where is the destination? | What to set |
|---|---|
| Another docker container on its own network | `docker network connect mj-regression_default <ctr>` then `ARCHIVE_DB_HOST=<ctr>` |
| Your host machine (Mac/Windows/Linux) | keep `host.docker.internal` default + ensure destination publishes its port to the host |
| A remote server | `ARCHIVE_DB_HOST=<fqdn-or-ip>` + open the firewall |

The test-runner compose service declares `extra_hosts: host.docker.internal:host-gateway`,
so the default works portably on Linux too (where Docker Desktop's auto-mapping doesn't apply).

### Pipeline

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  PRE-FLIGHT (§ 4b of entrypoint, runs BEFORE the 10-minute suite):     │
│    scripts/archive-preflight.cjs validates the destination:            │
│      a. Required env vars present                                       │
│      b. TCP connect to ARCHIVE_DB_HOST:ARCHIVE_DB_PORT                  │
│      c. SQL auth as ARCHIVE_DB_USERNAME                                 │
│      d. ARCHIVE_DB_DATABASE exists                                      │
│      e. MJ core schema present (TestSuiteRun + TestRun + TestRunOutput  │
│         + User tables in [ARCHIVE_MJ_CORE_SCHEMA])                      │
│      f. ARCHIVE_USER_EMAIL exists in [<schema>].[User]                  │
│    Fail-fast on any miss; sets ARCHIVE_PREFLIGHT_OK=1 on success.       │
│                                                                         │
│  POST-SUITE (scripts/archive-run.sh, only if ARCHIVE_PREFLIGHT_OK=1):   │
│                                                                         │
│  1. suite finishes, results.json written to test-results/run-${TS}/    │
│                                                                         │
│  2. read suiteRunId from results.json                                  │
│                                                                         │
│  3. generate destination config:                                        │
│       scripts/archive-config-gen.cjs writes a transient                 │
│       /tmp/mj-archive-config.cjs from ARCHIVE_DB_* env vars            │
│                                                                         │
│  4. copy archive entity-cascade config templates:                       │
│        cp -R docker/regression/archive/. ${RUN_DIR}/archive/           │
│                                                                         │
│  5. mj sync pull from local docker DB (default mj.config.cjs):         │
│        cd ${RUN_DIR}/archive                                            │
│        npx mj sync pull \                                               │
│            --entity="MJ: Test Suite Runs" \                             │
│            --filter="ID='${SUITE_RUN_ID}'"                              │
│      Output: nested JSON tree                                           │
│        archive/test-suite-runs/.<suite>.json                            │
│          relatedEntities → "MJ: Test Runs" [...]                        │
│              relatedEntities → "MJ: Test Run Outputs" [...]             │
│                externalizeFields: InlineData → screenshots/<id>.png     │
│                                                                         │
│      CRITICAL: do NOT set MJ_CONFIG_FILE for this step — it would       │
│      redirect the pull to the destination DB, find 0 rows, and write    │
│      an empty archive folder.                                           │
│                                                                         │
│  6. tag-archive.cjs writes Tags + MachineName into the pulled JSON:    │
│        suite.Tags = JSON.stringify(["${ARCHIVE_TAG}"])                  │
│        suite.MachineName = ${ARCHIVE_SOURCE}                            │
│                                                                         │
│  7. mj sync push to destination MJ:                                     │
│        MJ_CONFIG_FILE=${ARCHIVE_CONFIG_FILE} \                          │
│            npx mj sync push --dir=${RUN_DIR}/archive --ci               │
│      Upserts by primary key — idempotent across re-pushes.              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Archive entity-cascade config

[`docker/regression/archive/test-suite-runs/.mj-sync.json`](archive/test-suite-runs/.mj-sync.json)
defines what to pull and how to externalize:

- **3-level cascade:** `MJ: Test Suite Runs` → `MJ: Test Runs` → `MJ: Test Run Outputs`
- **`excludeFields`** at each level strips joined virtual columns (`Suite`, `Test`, `RunByUser`, `OutputType`) that aren't writable on push and would cause validation errors.
- **`externalizeFields`** on `TestRunOutput.InlineData` writes base64 PNGs as files (`screenshots/{TestRunID}/{ID}.png`) instead of inlining them in the JSON. Keeps JSON files small and human-readable; survives a JSON round-trip.

### Archive scripts

| File | Use |
|---|---|
| [`scripts/archive-run.sh`](scripts/archive-run.sh) | The post-suite archive orchestrator (config-gen → pre-seed → pull → tag → push). **Sourced** by both `test-runner-entrypoint.sh` (§8) and `test-runner-remote-entrypoint.sh` (§5) so the logic lives in one place |
| [`scripts/archive-preflight.cjs`](scripts/archive-preflight.cjs) | Pre-flight validator. Runs ~5s before the suite; fails fast on misconfig |
| [`scripts/archive-config-gen.cjs`](scripts/archive-config-gen.cjs) | Generates `/tmp/mj-archive-config.cjs` from `ARCHIVE_DB_*` env vars |
| [`scripts/tag-archive.cjs`](scripts/tag-archive.cjs) | Writes `Tags` + `MachineName` into the pulled archive JSON before push |

The `MJ_CONFIG_FILE` env override (added in
[`packages/MetadataSync/src/lib/config-manager.ts`](../../packages/MetadataSync/src/lib/config-manager.ts))
lets `mj sync push` use a non-default config without changing cwd — the
pull uses the local config (cosmiconfig defaults), the push uses the
explicit one.

**⚠️ Common pitfall:** never set `MJ_CONFIG_FILE` as a container-wide env var
(e.g., via `docker run -e MJ_CONFIG_FILE=...`). It must be scoped to the push
command only, because the pull MUST use the local docker DB config. The
entrypoint applies it inline for that reason; a container-wide setting would
redirect the pull and silently produce an empty archive.

### Tag-based filtering on the destination

The destination MJ accumulates runs from multiple sources (staging-nightly,
customer-x-monthly, etc.). `mj test compare --tag=staging-nightly` filters
the DB-mode comparison query to only runs whose `Tags` array contains the
tag. See §14.

---

## 14. CLI Surface

The user-facing commands live in
[`packages/MJCLI/src/commands/test/regression/`](../../packages/MJCLI/src/commands/test/regression/),
with shared helpers in
[`packages/MJCLI/src/lib/regression/docker-helpers.ts`](../../packages/MJCLI/src/lib/regression/docker-helpers.ts).

### Subcommand inventory

| Command | Purpose | Backing call |
|---|---|---|
| `mj test regression build [services]` | Build images (incl. gen-forms on first run) | `docker compose --profile full build` |
| `mj test regression up` | Run the full self-contained MJ stack (Mode A) | `docker compose --profile full up` |
| `mj test regression up --bacpac=<f> [--no-upgrade] [--suite] [--metadata]` | Mode A, but import the DB from a `.bacpac` instead of building it | adds the `docker-compose.bacpac.yml` overlay (amd64 db-setup + SqlPackage import) |
| `mj test regression down` | Stop the stack and wipe DB volumes | `docker compose --profile * down -v` |
| `mj test regression gen-forms` | Regenerate Angular entity forms | `bash gen-forms.sh` |
| `mj test regression compare [--tag] [--diff-only] …` | Diff the two most recent runs | `mj test compare --from-json <dir>` (or DB mode with `--tag`) |
| `mj test regression export [--run=<X>]` | Export a run as a portable standalone HTML (screenshots inlined) | `node scripts/inline-report.cjs <run-dir>` |
| `mj test regression remote --target=<X> [--overlay=Y] …` | Run against a remote URL (Mode B/C/D) | Loads target via loader, sets env, runs compose |
| `mj test regression init <name> [--list]` | Scaffold a mode template into cwd | Copies from `init-templates/` bundled inside the CLI |

### Guards + ergonomics

- **`requireMonorepoRoot()`** — every subcommand except `init` calls this at startup. Exits 1 with a helpful message if `docker/regression/docker-compose.test.yml` doesn't exist in cwd. (Until Phase 8's image is published, this is unavoidable; afterward we can lift it.)
- **`isInsideMonorepo()`** — soft check used by `compare`, `up`, `export`, `remote` to choose between monorepo-relative paths and external/published-image paths. Walks up from cwd looking for `docker/regression/docker-compose.test.yml`. `init` does NOT use it — templates ship inside the CLI package so init works the same in monorepo and external installs.
- **`envFileExists()`** — gracefully omits `--env-file` from compose args when `.env.test` is missing.
- **`resolveTargetPath()`** — handles bare names (`byo-app`), paths (`./my.target.json`), or absolute paths uniformly.

### Argument flow — `mj test regression remote`

```
mj test regression remote --target=byo-app --overlay=foo.yml
                          ─────────────  ──────────────────
                                │                │
                                │                └─ overlays array
                                │
                                ▼
                    resolveTargetPath('byo-app')
                                │
                                ▼
              docker/regression/targets/byo-app.target.json
                                │
                                ▼
              spawnCapture('node load-target-profile.cjs ... --format=json')
                                │
                                ▼
                    JSON.parse(loader stdout)
                                │
                                ▼
              { MJ_TEST_VAR_baseUrl, TEST_SUITE_NAME, ..., TEST_RUNNER_ENTRYPOINT }
                                │
                                ▼
              childEnv = { ...process.env, ...resolved }
              composeArgs = [
                'compose',
                '-f', 'docker/regression/docker-compose.test.yml',
                '-f', 'foo.yml',                       # from --overlay
                '--env-file', 'docker/regression/.env.test',
                '--profile', 'full',                   # or 'remote-target' with --no-local-stack
                'up',
              ]
                                │
                                ▼
              spawnInherit('docker', composeArgs, { env: childEnv })
```

---

## 15. Init Templates

Bundled scaffold templates live inside the published CLI at
[`packages/MJCLI/src/init-templates/`](../../packages/MJCLI/src/init-templates/) — five
directories that each illustrate one adoption pattern. The CLI's `init`
command resolves them via `__dirname` relative to the compiled
`init.js`, so they ship with `@memberjunction/cli` for both monorepo
and `npm i -g` users.

| Template | Mode | Components | When to use |
|---|---|---|---|
| `remote-mj` | B | target.json + README only | Drive the canonical MJ Explorer regression suite against a deployed MJ instance (staging / customer / production) |
| `generic-web` | C | target.json + 3 tests (MDN) + custom oracle (`mdn-oracles.cjs`) + README | Test any non-MJ web app; demonstrates `{{baseUrl}}` substitution + custom IOracle plugin |
| `static-file-server` | D (minimal) | Dockerfile + 2 HTML pages + overlay + 2 tests + README | Smallest possible Mode D — nginx serving static files, no language/framework deps |
| `bring-your-own-app` | D (scaffold) | target.json + compose overlay template + 2 example tests + suite + README | Mode D scaffold — adapt the overlay's `byo-app` service to YOUR app. The template does **not** ship a demo app. |
| `github-actions` | CI integration | Copy-paste workflow YAML + README | Drop into your repo's `.github/workflows/` to run the runner in CI |

### How they get shipped

The templates live in the CLI source tree at
`packages/MJCLI/src/init-templates/`.
[`scripts/copy-regression-assets.mjs`](../../packages/MJCLI/scripts/copy-regression-assets.mjs)
runs at build time (`npm run build`) and `cpSync`s the templates into
`dist/commands/test/regression/init-templates/` alongside the compiled
`init.js`. The published npm tarball's `files` array includes `/dist`,
so the templates ship with every install. There is no separate
`docker/regression/examples/` directory anymore.

```bash
mj test regression init --list                # show available templates
mj test regression init generic-web           # scaffold ./generic-web/
```

---

## 16. File Inventory

### `docker/regression/`

| Path | Purpose |
|---|---|
| `docker-compose.test.yml` | The 5-service compose project + form-generator overlay; profile-gated |
| `Dockerfile.db-setup` | Init container — Flyway + codegen + AssociationDB |
| `Dockerfile.api` | MJAPI runtime — `Dockerfile.db-setup` would re-do init work |
| `Dockerfile.explorer` | MJExplorer — Angular AOT build + nginx serve |
| `Dockerfile.test-runner` | Test runner — Playwright + monorepo build (will become a thin overlay on the published image post-Phase-8 publish) |
| `db-setup-entrypoint.sh` | DB-setup thin bash → `scripts/*.cjs` |
| `form-gen-entrypoint.sh` | Form-generator thin bash → `scripts/*.cjs` |
| `test-runner-entrypoint.sh` | Full-stack (Mode A) entrypoint — socat + metadata sync + suite + reports |
| `test-runner-remote-entrypoint.sh` | Remote (Mode B/C) entrypoint — skips socat + setup |
| `form-gen.config.cjs` | `mj.config.cjs` override for the form-generator (includes Angular output paths) |
| `gen-forms.sh` | Wrapper invoked by `mj test regression gen-forms` |
| `nginx.conf` | nginx config for the mjexplorer container (proxy /api → mjapi, `Connection: upgrade` map block) |
| `REGRESSION_TESTING.md` | User-facing guide |
| `ARCHITECTURE.md` | This file |
| `LIMITATIONS.md` | Known gaps + workarounds |

### `docker/regression/scripts/` (all are `.cjs`)

| File | Purpose | Where it runs |
|---|---|---|
| `bootstrap-db.cjs` | CREATE DATABASE + install AssociationDB (one-shot) | db-setup container |
| `setup-test-user.cjs` | SQL safety-net: test user + roles + apps + favorites | test-runner container |
| `preflight-checks.cjs` | Probes MJAPI/nginx/socat/Auth0 + memory snapshot | test-runner container |
| `health-monitor.cjs` | Background diagnostic probes every 10s | test-runner container (bg) |
| `extract-screenshots.cjs` | Pulls per-step PNGs from `vwTestRunOutputs` to disk | test-runner container |
| `generate-md-report.cjs` | Renders `report.md` (pass/fail table + oracles) | test-runner container |
| `generate-html-report.cjs` | Renders `report.html` (lightbox gallery with action overlays) | test-runner container |
| `inline-report.cjs` | Inlines screenshots base64 into a portable standalone HTML; exposed as `mj test regression export` | host (via CLI) |
| `patch-test-api-config.cjs` | Bake-time MJAPI config flip (`autoCreateNewUsers`, etc.) | Dockerfile.api RUN step |
| `gen-environment-ts.cjs` | Bake-time MJExplorer `environment.ts` generation | Dockerfile.explorer RUN step |
| `tag-archive.cjs` | Sets `Tags` + `MachineName` on the pulled archive JSON before push | test-runner container (post-suite) |
| `load-target-profile.cjs` | Reads + validates target profile, emits env-var mapping | **HOST** (invoked by CLI before compose) |
| `lib/db.cjs` | Shared mssql connection helper | all `.cjs` scripts that need DB |
| `lib/probes.cjs` | Shared HTTP/TCP probes (preflight + health-monitor) | preflight + health-monitor scripts |

### `docker/regression/targets/`

| File | Use |
|---|---|
| `byo-app.target.json` | Working target for the BYO Mode D example |
| `*.example.target.json` | Committed scaffolds; copy to `<name>.target.json` and edit |
| `.gitignore` | Allows `*.example.target.json` + `byo-app.target.json`; ignores other `*.target.json` |

### `docker/regression/archive/`

| File | Use |
|---|---|
| `.mj-sync.json` | Root sync config — `directoryOrder: [test-suite-runs]` |
| `test-suite-runs/.mj-sync.json` | 3-level entity cascade + screenshot externalization |

### `docker/regression/test-metadata/`

| File | Use |
|---|---|
| `users/.users.json` | Docker-only test user (`computeruse@bluecypress.io`) with UI + Integration roles |

Kept separate from `metadata/` so it never gets pushed to a developer's local DB.

### Init templates (no longer in `docker/regression/`)

Mode scaffold templates have moved into the published CLI at
[`packages/MJCLI/src/init-templates/`](../../packages/MJCLI/src/init-templates/).
See §15 above for the list of bundled templates and how they ship.

### `docker/agentic-test-runner/`

| File | Purpose |
|---|---|
| `Dockerfile` | Generic published image — Playwright + monorepo + dispatcher (no scaffold templates; those ship with `@memberjunction/cli`) |
| `dispatcher.sh` | Entrypoint script — routes `run|import-bacpac|export|exec|help` subcommands. `init` was retired here; use the MJ CLI instead. |

---

## 17. Diagnostic Outputs

Every run creates `docker/regression/test-results/run-{TIMESTAMP}/` with:

| File | Contents |
|---|---|
| `results.json` | Machine-readable suite results — emitted by `mj test suite --format=json`. Top-level keys: `suiteRunId`, `suiteName`, `status`, `passedTests`, `failedTests`, `totalTests`, `averageScore`, `testResults[]`, `durationMs`, `totalCost`, `startedAt`, `completedAt` |
| `report.md` | Human-readable markdown summary — generated by `scripts/generate-md-report.cjs` |
| `report.html` | Self-contained HTML gallery with lightbox screenshots + action overlays + oracle results table — generated by `scripts/generate-html-report.cjs` |
| `preflight.json` | Pre-run diagnostics: MJAPI health, GraphQL reachability, nginx proxy, socat TCP, Auth0 OIDC discovery, memory snapshot. Captures the environment state at run start. |
| `diagnostics.json` | Background health monitor log — probes every 10s during the suite |
| `screenshots/<test-name>/` | Per-step PNGs + `steps.json` (reasoning + actions + URL per step) |
| `archive/` | Only when `ARCHIVE_DB_DATABASE` is set — the pulled entity-cascade JSON tree + externalized PNGs ready for `mj sync push` |

Plus a `latest` symlink at `docker/regression/test-results/latest →
run-{TIMESTAMP}/` for convenience.

### Comparing runs

`mj test regression compare` auto-discovers the two newest `run-*/results.json`
files by mtime and produces a diff. Default output is a console table; with
`--format=json` or `--format=markdown` you get machine-parsable or
report-friendly variants.

Exit codes are CI-friendly:

| Code | Meaning |
|---|---|
| 0 | No regressions detected |
| 1 | One or more regressions detected (use `--diff-only` to see them) |
| 2 | Data error (fewer than 2 runs found, missing files, invalid JSON) |

Definition of "regression": previous status was `Passed` and current is not,
OR score dropped by more than 0.10. "Improvement" is the inverse.

---

## 18. Extension Points

### Add a new regression test

1. Create `metadata/tests/regression/.T26-<short-name>.json` mirroring the shape of an existing test (e.g. T01).
2. Add a `relatedEntities[].MJ: Test Suite Tests` entry to `metadata/test-suites/.regression-suite.json` linking the new test to the suite.
3. `mj test regression up` will pick up both via the bind-mounted `metadata/` volume — no rebuild needed.

### Add a new oracle type

1. Implement `IOracle` (TS or JS file). See `mdn-oracles.cjs` for a template.
2. Ship it in your repo (or copy into a project-specific scaffold).
3. Reference it in the test JSON's `Configuration.oracles[]` array.
4. Load it via `--oracles-module=path/to/file.cjs` (or `oraclesModule` in a target profile).

### Add a new target kind

1. Add the kind name to `SUPPORTED_KINDS` in `scripts/load-target-profile.cjs`.
2. (Optional) Add kind-specific validation logic in the loader.
3. (Optional) Branch entrypoint behavior on `TARGET_PROFILE_KIND` if needed (e.g. an `mj-api-only` kind that skips browser launch entirely).

### Add a new compose profile

1. Add `profiles: ["<name>"]` to relevant services in `docker-compose.test.yml`.
2. Add a new CLI subcommand in `packages/MJCLI/src/commands/test/regression/` if the profile needs a user-facing entry point.
3. Document in `REGRESSION_TESTING.md` and (if new mode) in §2 of this file.

### Add a new init template

1. Create `packages/MJCLI/src/init-templates/<name>/` with at minimum a `target.json` and a `README.md`.
2. (If Mode D) include `docker-compose.app.yml` (template form — point `byo-app` at a user-supplied image/build) and any starter metadata.
3. (If using custom oracles) include `oracles/<name>-oracles.cjs`.
4. (If shipping starter tests) include `<name>-metadata/{tests,test-suites}/` mirroring the canonical layout.
5. Add a row to §15 of this file.
6. Rebuild MJCLI (`cd packages/MJCLI && npm run build`) — `scripts/copy-regression-assets.mjs` will copy the new template into `dist/commands/test/regression/init-templates/` so it ships in the published CLI.
7. Verify with `mj test regression init --list` (your new template should appear).

### Make a package lazy-loadable in the runner image

The published image bundles `dist/` of every `@memberjunction/*` package. Trim the image size by:

1. Marking your package's expensive dependencies as `optionalDependencies` or `peerDependenciesMeta`.
2. Using dynamic `import()` for runtime-discovered modules (the existing `mj test regression` CLI does this for testing-cli).

See the [Dynamic Import policy in CLAUDE.md](../../CLAUDE.md#8-no-dynamic-import-unless-narrowly-justified) for the rules — runtime plugin discovery is one of the allowed exemptions.

---

## 19. Glossary

| Term | Definition |
|---|---|
| **Agentic test** | A test whose execution is decided at runtime by an LLM controller, not a hardcoded script. The Computer Use engine is the agentic part. |
| **Computer Use** | Anthropic's tool-use mode where the LLM produces screenshot-anchored actions (click coordinates, key presses, text inputs). MJ wraps this in [`packages/AI/ComputerUse/`](../../packages/AI/ComputerUse/). |
| **Controller** | The LLM that decides the next step (one prompt per step). Anchored by the goal + screenshot history. |
| **Judge** | The LLM that periodically evaluates whether the goal is achieved. Configurable cadence via `Configuration.judgeFrequency`. |
| **Oracle** | A deterministic post-run evaluator. Goal-completion, url-match, step-count are built-in; plugins via `--oracles-module`. |
| **Driver** | A `BaseTestDriver` subclass that executes one test type. `ComputerUseTestDriver` is the one used by the regression suite. |
| **Target profile** | JSON describing where to run (URL, auth, suite, archive). See §8. |
| **Mode A/B/C/D** | The four run modes. See §2. |
| **AssociationDB** | The demo dataset baked into the docker DB. 58 tables, 10K+ rows of members/events/courses/certifications/etc. Provides realistic UI to test against. |
| **Suite run** | One execution of `mj test suite`. Produces a `MJTestSuiteRun` row + N `MJTestRun` rows + M `MJTestRunOutput` rows. |
| **Test output** | A persisted artifact from one step — typically a screenshot stored as base64 in `TestRunOutput.InlineData`. |
| **Variable substitution** | The `{{key}}` placeholder system. See §9. |
| **Archive flow** | The optional `mj sync pull → tag → push` pipeline that forwards a suite run to a destination MJ. See §13. |
| **Bare name** | A target profile reference without a path (e.g. `byo-app`), resolved to `targets/<bare>.target.json`. |
| **Overlay** | A compose YAML file that extends the base via `-f base -f overlay`. Used for Mode D to add the byo-app service + test-runner deps. |
| **storageState** | Playwright's snapshot of cookies + localStorage for a `BrowserContext`. Captured + replayed per worker to avoid re-authenticating between tests. |
| **Externalization** (mj-sync) | Writing a large field (`InlineData`) to disk as a separate file (`screenshots/<id>.png`) and replacing the in-JSON value with `@file:` reference. Keeps JSONs small + diffable. |
