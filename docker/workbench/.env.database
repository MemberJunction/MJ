# SQL Server connection details for the claude-dev container
# This file is mounted into /workspace/.env.database so Claude Code can read it
#
# The SQL Server instance is the "sql-claude" container in this docker-compose.
# It runs SQL Server 2022 Developer Edition with SA (System Administrator) privileges.

# ─── SQL Connection (used by shell aliases and sqlcmd) ────────────────────────
DB_HOST=sql-claude
DB_PORT=1433
DB_USER=sa
DB_USERNAME=sa
DB_PASSWORD=Claude2Sql99
DB_TRUST_SERVER_CERTIFICATE=true

# ─── MJ Database (created by db-bootstrap) ───────────────────────────────────
DB_DATABASE=MJ_Workbench

# ─── MJAPI / CodeGen configuration ───────────────────────────────────────────
CODEGEN_DB_USERNAME=sa
CODEGEN_DB_PASSWORD=Claude2Sql99
MJ_CORE_SCHEMA=__mj
GRAPHQL_PORT=4000

# ─── PostgreSQL Connection (postgres-claude container) ──────────────────────
PG_HOST=postgres-claude
PG_PORT=5432
PG_USER=mj_admin
PG_PASSWORD=Claude2Pg99
PG_DATABASE=MJ_Workbench_PG

# sqlcmd one-liner (already on PATH inside the container):
#   sqlcmd -S sql-claude -U sa -P 'Claude2Sql99' -C
#
# psql one-liner:
#   PGPASSWORD=Claude2Pg99 psql -h postgres-claude -U mj_admin -d MJ_Workbench_PG
#
# Node.js / mssql connection (tedious driver):
#   {
#     "server": "sql-claude",
#     "port": 1433,
#     "user": "sa",
#     "password": "Claude2Sql99",
#     "options": { "encrypt": true, "trustServerCertificate": true }
#   }
#
# Node.js / pg connection:
#   {
#     "host": "postgres-claude",
#     "port": 5432,
#     "user": "mj_admin",
#     "password": "Claude2Pg99",
#     "database": "MJ_Workbench_PG"
#   }
