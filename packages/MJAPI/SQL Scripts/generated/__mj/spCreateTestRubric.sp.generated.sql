-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Test Rubrics
-- Item: spCreateTestRubric
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR TestRubric
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateTestRubric]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateTestRubric];
GO

CREATE PROCEDURE [__mj].[spCreateTestRubric]
    @ID uniqueidentifier = NULL,
    @TypeID uniqueidentifier,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @PromptTemplate_Clear bit = 0,
    @PromptTemplate nvarchar(MAX) = NULL,
    @Criteria_Clear bit = 0,
    @Criteria nvarchar(MAX) = NULL,
    @Version_Clear bit = 0,
    @Version nvarchar(50) = NULL,
    @Status nvarchar(20) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[TestRubric]
            (
                [ID],
                [TypeID],
                [Name],
                [Description],
                [PromptTemplate],
                [Criteria],
                [Version],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @TypeID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @PromptTemplate_Clear = 1 THEN NULL ELSE ISNULL(@PromptTemplate, NULL) END,
                CASE WHEN @Criteria_Clear = 1 THEN NULL ELSE ISNULL(@Criteria, NULL) END,
                CASE WHEN @Version_Clear = 1 THEN NULL ELSE ISNULL(@Version, NULL) END,
                ISNULL(@Status, 'Active')
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[TestRubric]
            (
                [TypeID],
                [Name],
                [Description],
                [PromptTemplate],
                [Criteria],
                [Version],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @TypeID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @PromptTemplate_Clear = 1 THEN NULL ELSE ISNULL(@PromptTemplate, NULL) END,
                CASE WHEN @Criteria_Clear = 1 THEN NULL ELSE ISNULL(@Criteria, NULL) END,
                CASE WHEN @Version_Clear = 1 THEN NULL ELSE ISNULL(@Version, NULL) END,
                ISNULL(@Status, 'Active')
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwTestRubrics] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateTestRubric] TO [cdp_Developer], [cdp_Integration]
    