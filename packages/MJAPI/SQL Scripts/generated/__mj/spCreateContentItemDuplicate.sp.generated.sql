-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Content Item Duplicates
-- Item: spCreateContentItemDuplicate
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR ContentItemDuplicate
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateContentItemDuplicate]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateContentItemDuplicate];
GO

CREATE PROCEDURE [__mj].[spCreateContentItemDuplicate]
    @ID uniqueidentifier = NULL,
    @ContentItemAID uniqueidentifier,
    @ContentItemBID uniqueidentifier,
    @SimilarityScore decimal(5, 4),
    @DetectionMethod nvarchar(30) = NULL,
    @Status nvarchar(20) = NULL,
    @ResolvedByUserID_Clear bit = 0,
    @ResolvedByUserID uniqueidentifier = NULL,
    @ResolvedAt_Clear bit = 0,
    @ResolvedAt datetimeoffset = NULL,
    @Resolution_Clear bit = 0,
    @Resolution nvarchar(20) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[ContentItemDuplicate]
            (
                [ID],
                [ContentItemAID],
                [ContentItemBID],
                [SimilarityScore],
                [DetectionMethod],
                [Status],
                [ResolvedByUserID],
                [ResolvedAt],
                [Resolution]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @ContentItemAID,
                @ContentItemBID,
                @SimilarityScore,
                ISNULL(@DetectionMethod, 'Checksum'),
                ISNULL(@Status, 'Pending'),
                CASE WHEN @ResolvedByUserID_Clear = 1 THEN NULL ELSE ISNULL(@ResolvedByUserID, NULL) END,
                CASE WHEN @ResolvedAt_Clear = 1 THEN NULL ELSE ISNULL(@ResolvedAt, NULL) END,
                CASE WHEN @Resolution_Clear = 1 THEN NULL ELSE ISNULL(@Resolution, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[ContentItemDuplicate]
            (
                [ContentItemAID],
                [ContentItemBID],
                [SimilarityScore],
                [DetectionMethod],
                [Status],
                [ResolvedByUserID],
                [ResolvedAt],
                [Resolution]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ContentItemAID,
                @ContentItemBID,
                @SimilarityScore,
                ISNULL(@DetectionMethod, 'Checksum'),
                ISNULL(@Status, 'Pending'),
                CASE WHEN @ResolvedByUserID_Clear = 1 THEN NULL ELSE ISNULL(@ResolvedByUserID, NULL) END,
                CASE WHEN @ResolvedAt_Clear = 1 THEN NULL ELSE ISNULL(@ResolvedAt, NULL) END,
                CASE WHEN @Resolution_Clear = 1 THEN NULL ELSE ISNULL(@Resolution, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwContentItemDuplicates] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateContentItemDuplicate] TO [cdp_Developer], [cdp_Integration]
    