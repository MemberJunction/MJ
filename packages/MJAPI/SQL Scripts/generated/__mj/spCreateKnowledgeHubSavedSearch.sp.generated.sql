-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Knowledge Hub Saved Searches
-- Item: spCreateKnowledgeHubSavedSearch
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR KnowledgeHubSavedSearch
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateKnowledgeHubSavedSearch]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateKnowledgeHubSavedSearch];
GO

CREATE PROCEDURE [__mj].[spCreateKnowledgeHubSavedSearch]
    @ID uniqueidentifier = NULL,
    @UserID uniqueidentifier,
    @Name nvarchar(255),
    @Query nvarchar(1000),
    @Filters_Clear bit = 0,
    @Filters nvarchar(MAX) = NULL,
    @MinScore_Clear bit = 0,
    @MinScore decimal(3, 2) = NULL,
    @MaxResults_Clear bit = 0,
    @MaxResults int = NULL,
    @NotifyOnNewResults bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[KnowledgeHubSavedSearch]
            (
                [ID],
                [UserID],
                [Name],
                [Query],
                [Filters],
                [MinScore],
                [MaxResults],
                [NotifyOnNewResults]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @UserID,
                @Name,
                @Query,
                CASE WHEN @Filters_Clear = 1 THEN NULL ELSE ISNULL(@Filters, NULL) END,
                CASE WHEN @MinScore_Clear = 1 THEN NULL ELSE ISNULL(@MinScore, NULL) END,
                CASE WHEN @MaxResults_Clear = 1 THEN NULL ELSE ISNULL(@MaxResults, 50) END,
                ISNULL(@NotifyOnNewResults, 0)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[KnowledgeHubSavedSearch]
            (
                [UserID],
                [Name],
                [Query],
                [Filters],
                [MinScore],
                [MaxResults],
                [NotifyOnNewResults]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @UserID,
                @Name,
                @Query,
                CASE WHEN @Filters_Clear = 1 THEN NULL ELSE ISNULL(@Filters, NULL) END,
                CASE WHEN @MinScore_Clear = 1 THEN NULL ELSE ISNULL(@MinScore, NULL) END,
                CASE WHEN @MaxResults_Clear = 1 THEN NULL ELSE ISNULL(@MaxResults, 50) END,
                ISNULL(@NotifyOnNewResults, 0)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwKnowledgeHubSavedSearches] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateKnowledgeHubSavedSearch] TO [cdp_Developer], [cdp_Integration]
    