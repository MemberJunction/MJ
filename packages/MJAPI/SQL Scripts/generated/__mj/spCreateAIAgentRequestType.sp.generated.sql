-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Agent Request Types
-- Item: spCreateAIAgentRequestType
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIAgentRequestType
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIAgentRequestType]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIAgentRequestType];
GO

CREATE PROCEDURE [__mj].[spCreateAIAgentRequestType]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @Icon_Clear bit = 0,
    @Icon nvarchar(100) = NULL,
    @DefaultAssignmentStrategy_Clear bit = 0,
    @DefaultAssignmentStrategy nvarchar(MAX) = NULL,
    @DefaultPriority_Clear bit = 0,
    @DefaultPriority int = NULL,
    @DefaultExpirationMinutes_Clear bit = 0,
    @DefaultExpirationMinutes int = NULL,
    @RequiresResponse bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIAgentRequestType]
            (
                [ID],
                [Name],
                [Description],
                [Icon],
                [DefaultAssignmentStrategy],
                [DefaultPriority],
                [DefaultExpirationMinutes],
                [RequiresResponse]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                CASE WHEN @DefaultAssignmentStrategy_Clear = 1 THEN NULL ELSE ISNULL(@DefaultAssignmentStrategy, NULL) END,
                CASE WHEN @DefaultPriority_Clear = 1 THEN NULL ELSE ISNULL(@DefaultPriority, NULL) END,
                CASE WHEN @DefaultExpirationMinutes_Clear = 1 THEN NULL ELSE ISNULL(@DefaultExpirationMinutes, NULL) END,
                ISNULL(@RequiresResponse, 1)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIAgentRequestType]
            (
                [Name],
                [Description],
                [Icon],
                [DefaultAssignmentStrategy],
                [DefaultPriority],
                [DefaultExpirationMinutes],
                [RequiresResponse]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                CASE WHEN @DefaultAssignmentStrategy_Clear = 1 THEN NULL ELSE ISNULL(@DefaultAssignmentStrategy, NULL) END,
                CASE WHEN @DefaultPriority_Clear = 1 THEN NULL ELSE ISNULL(@DefaultPriority, NULL) END,
                CASE WHEN @DefaultExpirationMinutes_Clear = 1 THEN NULL ELSE ISNULL(@DefaultExpirationMinutes, NULL) END,
                ISNULL(@RequiresResponse, 1)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIAgentRequestTypes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIAgentRequestType] TO [cdp_Developer], [cdp_Integration]
    