-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Permission Domains
-- Item: spCreatePermissionDomain
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR PermissionDomain
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreatePermissionDomain]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreatePermissionDomain];
GO

CREATE PROCEDURE [__mj].[spCreatePermissionDomain]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(200),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @ProviderClassName nvarchar(500),
    @SupportedGranteeTypes nvarchar(200),
    @SupportedActions nvarchar(500),
    @SupportsDeny bit = NULL,
    @SupportsExpiration bit = NULL,
    @SupportsHierarchyInheritance bit = NULL,
    @IsActive bit = NULL,
    @DisplayOrder int = NULL,
    @Icon_Clear bit = 0,
    @Icon nvarchar(100) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[PermissionDomain]
            (
                [ID],
                [Name],
                [Description],
                [ProviderClassName],
                [SupportedGranteeTypes],
                [SupportedActions],
                [SupportsDeny],
                [SupportsExpiration],
                [SupportsHierarchyInheritance],
                [IsActive],
                [DisplayOrder],
                [Icon]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @ProviderClassName,
                @SupportedGranteeTypes,
                @SupportedActions,
                ISNULL(@SupportsDeny, 0),
                ISNULL(@SupportsExpiration, 0),
                ISNULL(@SupportsHierarchyInheritance, 0),
                ISNULL(@IsActive, 1),
                ISNULL(@DisplayOrder, 100),
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[PermissionDomain]
            (
                [Name],
                [Description],
                [ProviderClassName],
                [SupportedGranteeTypes],
                [SupportedActions],
                [SupportsDeny],
                [SupportsExpiration],
                [SupportsHierarchyInheritance],
                [IsActive],
                [DisplayOrder],
                [Icon]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @ProviderClassName,
                @SupportedGranteeTypes,
                @SupportedActions,
                ISNULL(@SupportsDeny, 0),
                ISNULL(@SupportsExpiration, 0),
                ISNULL(@SupportsHierarchyInheritance, 0),
                ISNULL(@IsActive, 1),
                ISNULL(@DisplayOrder, 100),
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwPermissionDomains] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreatePermissionDomain] TO [cdp_Developer], [cdp_Integration]
    