-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Tag Scopes
-- Item: spCreateTagScope
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR TagScope
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateTagScope]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateTagScope];
GO

CREATE PROCEDURE [__mj].[spCreateTagScope]
    @ID uniqueidentifier = NULL,
    @TagID uniqueidentifier,
    @ScopeEntityID uniqueidentifier,
    @ScopeRecordID nvarchar(450)
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[TagScope]
            (
                [ID],
                [TagID],
                [ScopeEntityID],
                [ScopeRecordID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @TagID,
                @ScopeEntityID,
                @ScopeRecordID
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[TagScope]
            (
                [TagID],
                [ScopeEntityID],
                [ScopeRecordID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @TagID,
                @ScopeEntityID,
                @ScopeRecordID
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwTagScopes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateTagScope] TO [cdp_Developer], [cdp_Integration]
    