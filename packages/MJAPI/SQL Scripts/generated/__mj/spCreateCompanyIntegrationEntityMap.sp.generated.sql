-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Company Integration Entity Maps
-- Item: spCreateCompanyIntegrationEntityMap
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR CompanyIntegrationEntityMap
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateCompanyIntegrationEntityMap]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateCompanyIntegrationEntityMap];
GO

CREATE PROCEDURE [__mj].[spCreateCompanyIntegrationEntityMap]
    @ID uniqueidentifier = NULL,
    @CompanyIntegrationID uniqueidentifier,
    @ExternalObjectName nvarchar(500),
    @ExternalObjectLabel_Clear bit = 0,
    @ExternalObjectLabel nvarchar(500) = NULL,
    @EntityID uniqueidentifier,
    @SyncDirection nvarchar(50) = NULL,
    @SyncEnabled bit = NULL,
    @MatchStrategy_Clear bit = 0,
    @MatchStrategy nvarchar(MAX) = NULL,
    @ConflictResolution nvarchar(50) = NULL,
    @Priority int = NULL,
    @DeleteBehavior nvarchar(50) = NULL,
    @Status nvarchar(50) = NULL,
    @Configuration_Clear bit = 0,
    @Configuration nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[CompanyIntegrationEntityMap]
            (
                [ID],
                [CompanyIntegrationID],
                [ExternalObjectName],
                [ExternalObjectLabel],
                [EntityID],
                [SyncDirection],
                [SyncEnabled],
                [MatchStrategy],
                [ConflictResolution],
                [Priority],
                [DeleteBehavior],
                [Status],
                [Configuration]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @CompanyIntegrationID,
                @ExternalObjectName,
                CASE WHEN @ExternalObjectLabel_Clear = 1 THEN NULL ELSE ISNULL(@ExternalObjectLabel, NULL) END,
                @EntityID,
                ISNULL(@SyncDirection, 'Pull'),
                ISNULL(@SyncEnabled, 1),
                CASE WHEN @MatchStrategy_Clear = 1 THEN NULL ELSE ISNULL(@MatchStrategy, NULL) END,
                ISNULL(@ConflictResolution, 'SourceWins'),
                ISNULL(@Priority, 0),
                ISNULL(@DeleteBehavior, 'SoftDelete'),
                ISNULL(@Status, 'Active'),
                CASE WHEN @Configuration_Clear = 1 THEN NULL ELSE ISNULL(@Configuration, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[CompanyIntegrationEntityMap]
            (
                [CompanyIntegrationID],
                [ExternalObjectName],
                [ExternalObjectLabel],
                [EntityID],
                [SyncDirection],
                [SyncEnabled],
                [MatchStrategy],
                [ConflictResolution],
                [Priority],
                [DeleteBehavior],
                [Status],
                [Configuration]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @CompanyIntegrationID,
                @ExternalObjectName,
                CASE WHEN @ExternalObjectLabel_Clear = 1 THEN NULL ELSE ISNULL(@ExternalObjectLabel, NULL) END,
                @EntityID,
                ISNULL(@SyncDirection, 'Pull'),
                ISNULL(@SyncEnabled, 1),
                CASE WHEN @MatchStrategy_Clear = 1 THEN NULL ELSE ISNULL(@MatchStrategy, NULL) END,
                ISNULL(@ConflictResolution, 'SourceWins'),
                ISNULL(@Priority, 0),
                ISNULL(@DeleteBehavior, 'SoftDelete'),
                ISNULL(@Status, 'Active'),
                CASE WHEN @Configuration_Clear = 1 THEN NULL ELSE ISNULL(@Configuration, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwCompanyIntegrationEntityMaps] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateCompanyIntegrationEntityMap] TO [cdp_Developer], [cdp_Integration]
    