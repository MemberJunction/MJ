-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Search Providers
-- Item: spCreateSearchProvider
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR SearchProvider
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateSearchProvider]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateSearchProvider];
GO

CREATE PROCEDURE [__mj].[spCreateSearchProvider]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(200),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @DriverClass nvarchar(500),
    @Status nvarchar(20) = NULL,
    @Priority int = NULL,
    @SupportsPreview bit = NULL,
    @MaxResultsOverride_Clear bit = 0,
    @MaxResultsOverride int = NULL,
    @ProviderConfig_Clear bit = 0,
    @ProviderConfig nvarchar(MAX) = NULL,
    @CredentialID_Clear bit = 0,
    @CredentialID uniqueidentifier = NULL,
    @DisplayName_Clear bit = 0,
    @DisplayName nvarchar(200) = NULL,
    @Icon_Clear bit = 0,
    @Icon nvarchar(200) = NULL,
    @Comments_Clear bit = 0,
    @Comments nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[SearchProvider]
            (
                [ID],
                [Name],
                [Description],
                [DriverClass],
                [Status],
                [Priority],
                [SupportsPreview],
                [MaxResultsOverride],
                [ProviderConfig],
                [CredentialID],
                [DisplayName],
                [Icon],
                [Comments]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DriverClass,
                ISNULL(@Status, 'Active'),
                ISNULL(@Priority, 0),
                ISNULL(@SupportsPreview, 1),
                CASE WHEN @MaxResultsOverride_Clear = 1 THEN NULL ELSE ISNULL(@MaxResultsOverride, NULL) END,
                CASE WHEN @ProviderConfig_Clear = 1 THEN NULL ELSE ISNULL(@ProviderConfig, NULL) END,
                CASE WHEN @CredentialID_Clear = 1 THEN NULL ELSE ISNULL(@CredentialID, NULL) END,
                CASE WHEN @DisplayName_Clear = 1 THEN NULL ELSE ISNULL(@DisplayName, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                CASE WHEN @Comments_Clear = 1 THEN NULL ELSE ISNULL(@Comments, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[SearchProvider]
            (
                [Name],
                [Description],
                [DriverClass],
                [Status],
                [Priority],
                [SupportsPreview],
                [MaxResultsOverride],
                [ProviderConfig],
                [CredentialID],
                [DisplayName],
                [Icon],
                [Comments]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DriverClass,
                ISNULL(@Status, 'Active'),
                ISNULL(@Priority, 0),
                ISNULL(@SupportsPreview, 1),
                CASE WHEN @MaxResultsOverride_Clear = 1 THEN NULL ELSE ISNULL(@MaxResultsOverride, NULL) END,
                CASE WHEN @ProviderConfig_Clear = 1 THEN NULL ELSE ISNULL(@ProviderConfig, NULL) END,
                CASE WHEN @CredentialID_Clear = 1 THEN NULL ELSE ISNULL(@CredentialID, NULL) END,
                CASE WHEN @DisplayName_Clear = 1 THEN NULL ELSE ISNULL(@DisplayName, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                CASE WHEN @Comments_Clear = 1 THEN NULL ELSE ISNULL(@Comments, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwSearchProviders] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateSearchProvider] TO [cdp_Developer], [cdp_Integration]
    