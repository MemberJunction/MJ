-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Model Vendors
-- Item: spCreateAIModelVendor
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIModelVendor
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIModelVendor]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIModelVendor];
GO

CREATE PROCEDURE [__mj].[spCreateAIModelVendor]
    @ID uniqueidentifier = NULL,
    @ModelID uniqueidentifier,
    @VendorID uniqueidentifier,
    @Priority int = NULL,
    @Status nvarchar(20) = NULL,
    @DriverClass_Clear bit = 0,
    @DriverClass nvarchar(100) = NULL,
    @DriverImportPath_Clear bit = 0,
    @DriverImportPath nvarchar(255) = NULL,
    @APIName_Clear bit = 0,
    @APIName nvarchar(100) = NULL,
    @MaxInputTokens_Clear bit = 0,
    @MaxInputTokens int = NULL,
    @MaxOutputTokens_Clear bit = 0,
    @MaxOutputTokens int = NULL,
    @SupportedResponseFormats nvarchar(100) = NULL,
    @SupportsEffortLevel bit = NULL,
    @SupportsStreaming bit = NULL,
    @TypeID uniqueidentifier,
    @SupportsPrefill_Clear bit = 0,
    @SupportsPrefill bit = NULL,
    @PrefillFallbackText_Clear bit = 0,
    @PrefillFallbackText nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIModelVendor]
            (
                [ID],
                [ModelID],
                [VendorID],
                [Priority],
                [Status],
                [DriverClass],
                [DriverImportPath],
                [APIName],
                [MaxInputTokens],
                [MaxOutputTokens],
                [SupportedResponseFormats],
                [SupportsEffortLevel],
                [SupportsStreaming],
                [TypeID],
                [SupportsPrefill],
                [PrefillFallbackText]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @ModelID,
                @VendorID,
                ISNULL(@Priority, 0),
                ISNULL(@Status, 'Active'),
                CASE WHEN @DriverClass_Clear = 1 THEN NULL ELSE ISNULL(@DriverClass, NULL) END,
                CASE WHEN @DriverImportPath_Clear = 1 THEN NULL ELSE ISNULL(@DriverImportPath, NULL) END,
                CASE WHEN @APIName_Clear = 1 THEN NULL ELSE ISNULL(@APIName, NULL) END,
                CASE WHEN @MaxInputTokens_Clear = 1 THEN NULL ELSE ISNULL(@MaxInputTokens, NULL) END,
                CASE WHEN @MaxOutputTokens_Clear = 1 THEN NULL ELSE ISNULL(@MaxOutputTokens, NULL) END,
                ISNULL(@SupportedResponseFormats, 'Any'),
                ISNULL(@SupportsEffortLevel, 0),
                ISNULL(@SupportsStreaming, 0),
                @TypeID,
                CASE WHEN @SupportsPrefill_Clear = 1 THEN NULL ELSE ISNULL(@SupportsPrefill, NULL) END,
                CASE WHEN @PrefillFallbackText_Clear = 1 THEN NULL ELSE ISNULL(@PrefillFallbackText, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIModelVendor]
            (
                [ModelID],
                [VendorID],
                [Priority],
                [Status],
                [DriverClass],
                [DriverImportPath],
                [APIName],
                [MaxInputTokens],
                [MaxOutputTokens],
                [SupportedResponseFormats],
                [SupportsEffortLevel],
                [SupportsStreaming],
                [TypeID],
                [SupportsPrefill],
                [PrefillFallbackText]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ModelID,
                @VendorID,
                ISNULL(@Priority, 0),
                ISNULL(@Status, 'Active'),
                CASE WHEN @DriverClass_Clear = 1 THEN NULL ELSE ISNULL(@DriverClass, NULL) END,
                CASE WHEN @DriverImportPath_Clear = 1 THEN NULL ELSE ISNULL(@DriverImportPath, NULL) END,
                CASE WHEN @APIName_Clear = 1 THEN NULL ELSE ISNULL(@APIName, NULL) END,
                CASE WHEN @MaxInputTokens_Clear = 1 THEN NULL ELSE ISNULL(@MaxInputTokens, NULL) END,
                CASE WHEN @MaxOutputTokens_Clear = 1 THEN NULL ELSE ISNULL(@MaxOutputTokens, NULL) END,
                ISNULL(@SupportedResponseFormats, 'Any'),
                ISNULL(@SupportsEffortLevel, 0),
                ISNULL(@SupportsStreaming, 0),
                @TypeID,
                CASE WHEN @SupportsPrefill_Clear = 1 THEN NULL ELSE ISNULL(@SupportsPrefill, NULL) END,
                CASE WHEN @PrefillFallbackText_Clear = 1 THEN NULL ELSE ISNULL(@PrefillFallbackText, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIModelVendors] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIModelVendor] TO [cdp_Developer], [cdp_Integration]
    