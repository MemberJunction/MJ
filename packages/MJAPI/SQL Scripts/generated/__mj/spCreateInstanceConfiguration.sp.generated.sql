-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Instance Configurations
-- Item: spCreateInstanceConfiguration
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR InstanceConfiguration
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateInstanceConfiguration]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateInstanceConfiguration];
GO

CREATE PROCEDURE [__mj].[spCreateInstanceConfiguration]
    @ID uniqueidentifier = NULL,
    @FeatureKey nvarchar(200),
    @Value nvarchar(MAX),
    @ValueType nvarchar(20) = NULL,
    @Category nvarchar(100) = NULL,
    @DisplayName nvarchar(200),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @DefaultValue nvarchar(MAX)
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[InstanceConfiguration]
            (
                [ID],
                [FeatureKey],
                [Value],
                [ValueType],
                [Category],
                [DisplayName],
                [Description],
                [DefaultValue]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @FeatureKey,
                @Value,
                ISNULL(@ValueType, 'boolean'),
                ISNULL(@Category, 'General'),
                @DisplayName,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DefaultValue
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[InstanceConfiguration]
            (
                [FeatureKey],
                [Value],
                [ValueType],
                [Category],
                [DisplayName],
                [Description],
                [DefaultValue]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @FeatureKey,
                @Value,
                ISNULL(@ValueType, 'boolean'),
                ISNULL(@Category, 'General'),
                @DisplayName,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DefaultValue
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwInstanceConfigurations] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateInstanceConfiguration] TO [cdp_Developer], [cdp_Integration]
    