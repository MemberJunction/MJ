-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Search Scope Entities
-- Item: spCreateSearchScopeEntity
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR SearchScopeEntity
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateSearchScopeEntity]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateSearchScopeEntity];
GO

CREATE PROCEDURE [__mj].[spCreateSearchScopeEntity]
    @ID uniqueidentifier = NULL,
    @SearchScopeID uniqueidentifier,
    @EntityID uniqueidentifier,
    @ExtraFilter_Clear bit = 0,
    @ExtraFilter nvarchar(MAX) = NULL,
    @UserSearchString_Clear bit = 0,
    @UserSearchString nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[SearchScopeEntity]
            (
                [ID],
                [SearchScopeID],
                [EntityID],
                [ExtraFilter],
                [UserSearchString]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @SearchScopeID,
                @EntityID,
                CASE WHEN @ExtraFilter_Clear = 1 THEN NULL ELSE ISNULL(@ExtraFilter, NULL) END,
                CASE WHEN @UserSearchString_Clear = 1 THEN NULL ELSE ISNULL(@UserSearchString, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[SearchScopeEntity]
            (
                [SearchScopeID],
                [EntityID],
                [ExtraFilter],
                [UserSearchString]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @SearchScopeID,
                @EntityID,
                CASE WHEN @ExtraFilter_Clear = 1 THEN NULL ELSE ISNULL(@ExtraFilter, NULL) END,
                CASE WHEN @UserSearchString_Clear = 1 THEN NULL ELSE ISNULL(@UserSearchString, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwSearchScopeEntities] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateSearchScopeEntity] TO [cdp_Developer], [cdp_Integration]
    