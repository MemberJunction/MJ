-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Integration URL Formats
-- Item: spCreateIntegrationURLFormat
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR IntegrationURLFormat
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateIntegrationURLFormat]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateIntegrationURLFormat];
GO

CREATE PROCEDURE [__mj].[spCreateIntegrationURLFormat]
    @ID uniqueidentifier = NULL,
    @IntegrationID uniqueidentifier,
    @EntityID uniqueidentifier,
    @URLFormat nvarchar(500),
    @Comments_Clear bit = 0,
    @Comments nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[IntegrationURLFormat]
            (
                [ID],
                [IntegrationID],
                [EntityID],
                [URLFormat],
                [Comments]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @IntegrationID,
                @EntityID,
                @URLFormat,
                CASE WHEN @Comments_Clear = 1 THEN NULL ELSE ISNULL(@Comments, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[IntegrationURLFormat]
            (
                [IntegrationID],
                [EntityID],
                [URLFormat],
                [Comments]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @IntegrationID,
                @EntityID,
                @URLFormat,
                CASE WHEN @Comments_Clear = 1 THEN NULL ELSE ISNULL(@Comments, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwIntegrationURLFormats] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateIntegrationURLFormat] TO [cdp_Developer], [cdp_Integration]
    