-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: User Notification Preferences
-- Item: spCreateUserNotificationPreference
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR UserNotificationPreference
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateUserNotificationPreference]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateUserNotificationPreference];
GO

CREATE PROCEDURE [__mj].[spCreateUserNotificationPreference]
    @ID uniqueidentifier = NULL,
    @UserID uniqueidentifier,
    @NotificationTypeID uniqueidentifier,
    @InAppEnabled_Clear bit = 0,
    @InAppEnabled bit = NULL,
    @EmailEnabled_Clear bit = 0,
    @EmailEnabled bit = NULL,
    @SMSEnabled_Clear bit = 0,
    @SMSEnabled bit = NULL,
    @Enabled_Clear bit = 0,
    @Enabled bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[UserNotificationPreference]
            (
                [ID],
                [UserID],
                [NotificationTypeID],
                [InAppEnabled],
                [EmailEnabled],
                [SMSEnabled],
                [Enabled]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @UserID,
                @NotificationTypeID,
                CASE WHEN @InAppEnabled_Clear = 1 THEN NULL ELSE ISNULL(@InAppEnabled, NULL) END,
                CASE WHEN @EmailEnabled_Clear = 1 THEN NULL ELSE ISNULL(@EmailEnabled, NULL) END,
                CASE WHEN @SMSEnabled_Clear = 1 THEN NULL ELSE ISNULL(@SMSEnabled, NULL) END,
                CASE WHEN @Enabled_Clear = 1 THEN NULL ELSE ISNULL(@Enabled, 1) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[UserNotificationPreference]
            (
                [UserID],
                [NotificationTypeID],
                [InAppEnabled],
                [EmailEnabled],
                [SMSEnabled],
                [Enabled]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @UserID,
                @NotificationTypeID,
                CASE WHEN @InAppEnabled_Clear = 1 THEN NULL ELSE ISNULL(@InAppEnabled, NULL) END,
                CASE WHEN @EmailEnabled_Clear = 1 THEN NULL ELSE ISNULL(@EmailEnabled, NULL) END,
                CASE WHEN @SMSEnabled_Clear = 1 THEN NULL ELSE ISNULL(@SMSEnabled, NULL) END,
                CASE WHEN @Enabled_Clear = 1 THEN NULL ELSE ISNULL(@Enabled, 1) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwUserNotificationPreferences] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateUserNotificationPreference] TO [cdp_Developer], [cdp_Integration]
    