-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: O Auth Authorization States
-- Item: spCreateOAuthAuthorizationState
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR OAuthAuthorizationState
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateOAuthAuthorizationState]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateOAuthAuthorizationState];
GO

CREATE PROCEDURE [__mj].[spCreateOAuthAuthorizationState]
    @ID uniqueidentifier = NULL,
    @MCPServerConnectionID uniqueidentifier,
    @UserID uniqueidentifier,
    @StateParameter nvarchar(128),
    @CodeVerifier nvarchar(128),
    @CodeChallenge nvarchar(128),
    @RedirectURI nvarchar(1000),
    @RequestedScopes_Clear bit = 0,
    @RequestedScopes nvarchar(500) = NULL,
    @Status nvarchar(50) = NULL,
    @AuthorizationURL nvarchar(MAX),
    @ErrorCode_Clear bit = 0,
    @ErrorCode nvarchar(100) = NULL,
    @ErrorDescription_Clear bit = 0,
    @ErrorDescription nvarchar(MAX) = NULL,
    @InitiatedAt datetimeoffset = NULL,
    @ExpiresAt datetimeoffset,
    @CompletedAt_Clear bit = 0,
    @CompletedAt datetimeoffset = NULL,
    @FrontendReturnURL_Clear bit = 0,
    @FrontendReturnURL nvarchar(1000) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[OAuthAuthorizationState]
            (
                [ID],
                [MCPServerConnectionID],
                [UserID],
                [StateParameter],
                [CodeVerifier],
                [CodeChallenge],
                [RedirectURI],
                [RequestedScopes],
                [Status],
                [AuthorizationURL],
                [ErrorCode],
                [ErrorDescription],
                [InitiatedAt],
                [ExpiresAt],
                [CompletedAt],
                [FrontendReturnURL]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @MCPServerConnectionID,
                @UserID,
                @StateParameter,
                @CodeVerifier,
                @CodeChallenge,
                @RedirectURI,
                CASE WHEN @RequestedScopes_Clear = 1 THEN NULL ELSE ISNULL(@RequestedScopes, NULL) END,
                ISNULL(@Status, 'Pending'),
                @AuthorizationURL,
                CASE WHEN @ErrorCode_Clear = 1 THEN NULL ELSE ISNULL(@ErrorCode, NULL) END,
                CASE WHEN @ErrorDescription_Clear = 1 THEN NULL ELSE ISNULL(@ErrorDescription, NULL) END,
                ISNULL(@InitiatedAt, getutcdate()),
                @ExpiresAt,
                CASE WHEN @CompletedAt_Clear = 1 THEN NULL ELSE ISNULL(@CompletedAt, NULL) END,
                CASE WHEN @FrontendReturnURL_Clear = 1 THEN NULL ELSE ISNULL(@FrontendReturnURL, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[OAuthAuthorizationState]
            (
                [MCPServerConnectionID],
                [UserID],
                [StateParameter],
                [CodeVerifier],
                [CodeChallenge],
                [RedirectURI],
                [RequestedScopes],
                [Status],
                [AuthorizationURL],
                [ErrorCode],
                [ErrorDescription],
                [InitiatedAt],
                [ExpiresAt],
                [CompletedAt],
                [FrontendReturnURL]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @MCPServerConnectionID,
                @UserID,
                @StateParameter,
                @CodeVerifier,
                @CodeChallenge,
                @RedirectURI,
                CASE WHEN @RequestedScopes_Clear = 1 THEN NULL ELSE ISNULL(@RequestedScopes, NULL) END,
                ISNULL(@Status, 'Pending'),
                @AuthorizationURL,
                CASE WHEN @ErrorCode_Clear = 1 THEN NULL ELSE ISNULL(@ErrorCode, NULL) END,
                CASE WHEN @ErrorDescription_Clear = 1 THEN NULL ELSE ISNULL(@ErrorDescription, NULL) END,
                ISNULL(@InitiatedAt, getutcdate()),
                @ExpiresAt,
                CASE WHEN @CompletedAt_Clear = 1 THEN NULL ELSE ISNULL(@CompletedAt, NULL) END,
                CASE WHEN @FrontendReturnURL_Clear = 1 THEN NULL ELSE ISNULL(@FrontendReturnURL, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwOAuthAuthorizationStates] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateOAuthAuthorizationState] TO [cdp_Developer], [cdp_Integration]
    