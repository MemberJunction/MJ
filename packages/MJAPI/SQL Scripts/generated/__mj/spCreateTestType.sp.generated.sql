-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Test Types
-- Item: spCreateTestType
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR TestType
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateTestType]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateTestType];
GO

CREATE PROCEDURE [__mj].[spCreateTestType]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @DriverClass nvarchar(255),
    @Status nvarchar(20) = NULL,
    @VariablesSchema_Clear bit = 0,
    @VariablesSchema nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[TestType]
            (
                [ID],
                [Name],
                [Description],
                [DriverClass],
                [Status],
                [VariablesSchema]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DriverClass,
                ISNULL(@Status, 'Active'),
                CASE WHEN @VariablesSchema_Clear = 1 THEN NULL ELSE ISNULL(@VariablesSchema, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[TestType]
            (
                [Name],
                [Description],
                [DriverClass],
                [Status],
                [VariablesSchema]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DriverClass,
                ISNULL(@Status, 'Active'),
                CASE WHEN @VariablesSchema_Clear = 1 THEN NULL ELSE ISNULL(@VariablesSchema, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwTestTypes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateTestType] TO [cdp_Developer], [cdp_Integration]
    