-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Test Run Feedbacks
-- Item: spCreateTestRunFeedback
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR TestRunFeedback
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateTestRunFeedback]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateTestRunFeedback];
GO

CREATE PROCEDURE [__mj].[spCreateTestRunFeedback]
    @ID uniqueidentifier = NULL,
    @TestRunID uniqueidentifier,
    @ReviewerUserID uniqueidentifier,
    @Rating_Clear bit = 0,
    @Rating int = NULL,
    @IsCorrect_Clear bit = 0,
    @IsCorrect bit = NULL,
    @CorrectionSummary_Clear bit = 0,
    @CorrectionSummary nvarchar(MAX) = NULL,
    @Comments_Clear bit = 0,
    @Comments nvarchar(MAX) = NULL,
    @ReviewedAt datetimeoffset = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[TestRunFeedback]
            (
                [ID],
                [TestRunID],
                [ReviewerUserID],
                [Rating],
                [IsCorrect],
                [CorrectionSummary],
                [Comments],
                [ReviewedAt]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @TestRunID,
                @ReviewerUserID,
                CASE WHEN @Rating_Clear = 1 THEN NULL ELSE ISNULL(@Rating, NULL) END,
                CASE WHEN @IsCorrect_Clear = 1 THEN NULL ELSE ISNULL(@IsCorrect, NULL) END,
                CASE WHEN @CorrectionSummary_Clear = 1 THEN NULL ELSE ISNULL(@CorrectionSummary, NULL) END,
                CASE WHEN @Comments_Clear = 1 THEN NULL ELSE ISNULL(@Comments, NULL) END,
                ISNULL(@ReviewedAt, sysdatetimeoffset())
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[TestRunFeedback]
            (
                [TestRunID],
                [ReviewerUserID],
                [Rating],
                [IsCorrect],
                [CorrectionSummary],
                [Comments],
                [ReviewedAt]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @TestRunID,
                @ReviewerUserID,
                CASE WHEN @Rating_Clear = 1 THEN NULL ELSE ISNULL(@Rating, NULL) END,
                CASE WHEN @IsCorrect_Clear = 1 THEN NULL ELSE ISNULL(@IsCorrect, NULL) END,
                CASE WHEN @CorrectionSummary_Clear = 1 THEN NULL ELSE ISNULL(@CorrectionSummary, NULL) END,
                CASE WHEN @Comments_Clear = 1 THEN NULL ELSE ISNULL(@Comments, NULL) END,
                ISNULL(@ReviewedAt, sysdatetimeoffset())
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwTestRunFeedbacks] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateTestRunFeedback] TO [cdp_Developer], [cdp_Integration]
    