-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Communication Logs
-- Item: spCreateCommunicationLog
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR CommunicationLog
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateCommunicationLog]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateCommunicationLog];
GO

CREATE PROCEDURE [__mj].[spCreateCommunicationLog]
    @ID uniqueidentifier = NULL,
    @CommunicationProviderID uniqueidentifier,
    @CommunicationProviderMessageTypeID uniqueidentifier,
    @CommunicationRunID_Clear bit = 0,
    @CommunicationRunID uniqueidentifier = NULL,
    @Direction nvarchar(20),
    @MessageDate datetimeoffset,
    @Status nvarchar(20) = NULL,
    @MessageContent_Clear bit = 0,
    @MessageContent nvarchar(MAX) = NULL,
    @ErrorMessage_Clear bit = 0,
    @ErrorMessage nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[CommunicationLog]
            (
                [ID],
                [CommunicationProviderID],
                [CommunicationProviderMessageTypeID],
                [CommunicationRunID],
                [Direction],
                [MessageDate],
                [Status],
                [MessageContent],
                [ErrorMessage]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @CommunicationProviderID,
                @CommunicationProviderMessageTypeID,
                CASE WHEN @CommunicationRunID_Clear = 1 THEN NULL ELSE ISNULL(@CommunicationRunID, NULL) END,
                @Direction,
                @MessageDate,
                ISNULL(@Status, 'Pending'),
                CASE WHEN @MessageContent_Clear = 1 THEN NULL ELSE ISNULL(@MessageContent, NULL) END,
                CASE WHEN @ErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@ErrorMessage, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[CommunicationLog]
            (
                [CommunicationProviderID],
                [CommunicationProviderMessageTypeID],
                [CommunicationRunID],
                [Direction],
                [MessageDate],
                [Status],
                [MessageContent],
                [ErrorMessage]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @CommunicationProviderID,
                @CommunicationProviderMessageTypeID,
                CASE WHEN @CommunicationRunID_Clear = 1 THEN NULL ELSE ISNULL(@CommunicationRunID, NULL) END,
                @Direction,
                @MessageDate,
                ISNULL(@Status, 'Pending'),
                CASE WHEN @MessageContent_Clear = 1 THEN NULL ELSE ISNULL(@MessageContent, NULL) END,
                CASE WHEN @ErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@ErrorMessage, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwCommunicationLogs] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateCommunicationLog] TO [cdp_Developer], [cdp_Integration]
    