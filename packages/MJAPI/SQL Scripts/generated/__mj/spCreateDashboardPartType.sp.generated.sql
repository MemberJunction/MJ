-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Dashboard Part Types
-- Item: spCreateDashboardPartType
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR DashboardPartType
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateDashboardPartType]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateDashboardPartType];
GO

CREATE PROCEDURE [__mj].[spCreateDashboardPartType]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @Icon_Clear bit = 0,
    @Icon nvarchar(100) = NULL,
    @DriverClass nvarchar(255),
    @ConfigDialogClass_Clear bit = 0,
    @ConfigDialogClass nvarchar(255) = NULL,
    @DefaultConfig_Clear bit = 0,
    @DefaultConfig nvarchar(MAX) = NULL,
    @SortOrder int = NULL,
    @IsActive bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[DashboardPartType]
            (
                [ID],
                [Name],
                [Description],
                [Icon],
                [DriverClass],
                [ConfigDialogClass],
                [DefaultConfig],
                [SortOrder],
                [IsActive]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                @DriverClass,
                CASE WHEN @ConfigDialogClass_Clear = 1 THEN NULL ELSE ISNULL(@ConfigDialogClass, NULL) END,
                CASE WHEN @DefaultConfig_Clear = 1 THEN NULL ELSE ISNULL(@DefaultConfig, NULL) END,
                ISNULL(@SortOrder, 0),
                ISNULL(@IsActive, 1)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[DashboardPartType]
            (
                [Name],
                [Description],
                [Icon],
                [DriverClass],
                [ConfigDialogClass],
                [DefaultConfig],
                [SortOrder],
                [IsActive]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                @DriverClass,
                CASE WHEN @ConfigDialogClass_Clear = 1 THEN NULL ELSE ISNULL(@ConfigDialogClass, NULL) END,
                CASE WHEN @DefaultConfig_Clear = 1 THEN NULL ELSE ISNULL(@DefaultConfig, NULL) END,
                ISNULL(@SortOrder, 0),
                ISNULL(@IsActive, 1)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwDashboardPartTypes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateDashboardPartType] TO [cdp_Developer], [cdp_Integration]
    