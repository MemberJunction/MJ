-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: State Provinces
-- Item: spCreateStateProvince
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR StateProvince
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateStateProvince]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateStateProvince];
GO

CREATE PROCEDURE [__mj].[spCreateStateProvince]
    @ID uniqueidentifier = NULL,
    @CountryID uniqueidentifier,
    @Name nvarchar(200),
    @Code nvarchar(10),
    @ISO3166_2 nvarchar(10),
    @Latitude_Clear bit = 0,
    @Latitude decimal(10, 6) = NULL,
    @Longitude_Clear bit = 0,
    @Longitude decimal(10, 6) = NULL,
    @BoundaryGeoJSON_Clear bit = 0,
    @BoundaryGeoJSON nvarchar(MAX) = NULL,
    @CommonAliases_Clear bit = 0,
    @CommonAliases nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[StateProvince]
            (
                [ID],
                [CountryID],
                [Name],
                [Code],
                [ISO3166_2],
                [Latitude],
                [Longitude],
                [BoundaryGeoJSON],
                [CommonAliases]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @CountryID,
                @Name,
                @Code,
                @ISO3166_2,
                CASE WHEN @Latitude_Clear = 1 THEN NULL ELSE ISNULL(@Latitude, NULL) END,
                CASE WHEN @Longitude_Clear = 1 THEN NULL ELSE ISNULL(@Longitude, NULL) END,
                CASE WHEN @BoundaryGeoJSON_Clear = 1 THEN NULL ELSE ISNULL(@BoundaryGeoJSON, NULL) END,
                CASE WHEN @CommonAliases_Clear = 1 THEN NULL ELSE ISNULL(@CommonAliases, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[StateProvince]
            (
                [CountryID],
                [Name],
                [Code],
                [ISO3166_2],
                [Latitude],
                [Longitude],
                [BoundaryGeoJSON],
                [CommonAliases]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @CountryID,
                @Name,
                @Code,
                @ISO3166_2,
                CASE WHEN @Latitude_Clear = 1 THEN NULL ELSE ISNULL(@Latitude, NULL) END,
                CASE WHEN @Longitude_Clear = 1 THEN NULL ELSE ISNULL(@Longitude, NULL) END,
                CASE WHEN @BoundaryGeoJSON_Clear = 1 THEN NULL ELSE ISNULL(@BoundaryGeoJSON, NULL) END,
                CASE WHEN @CommonAliases_Clear = 1 THEN NULL ELSE ISNULL(@CommonAliases, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwStateProvinces] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateStateProvince] TO [cdp_Developer], [cdp_Integration]
    