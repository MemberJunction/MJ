-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: O Auth Tokens
-- Item: spCreateOAuthToken
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR OAuthToken
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateOAuthToken]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateOAuthToken];
GO

CREATE PROCEDURE [__mj].[spCreateOAuthToken]
    @ID uniqueidentifier = NULL,
    @MCPServerConnectionID uniqueidentifier,
    @CredentialID_Clear bit = 0,
    @CredentialID uniqueidentifier = NULL,
    @TokenType nvarchar(50) = NULL,
    @ExpiresAt datetimeoffset,
    @Scope_Clear bit = 0,
    @Scope nvarchar(500) = NULL,
    @IssuerURL nvarchar(1000),
    @LastRefreshAt_Clear bit = 0,
    @LastRefreshAt datetimeoffset = NULL,
    @RefreshCount int = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[OAuthToken]
            (
                [ID],
                [MCPServerConnectionID],
                [CredentialID],
                [TokenType],
                [ExpiresAt],
                [Scope],
                [IssuerURL],
                [LastRefreshAt],
                [RefreshCount]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @MCPServerConnectionID,
                CASE WHEN @CredentialID_Clear = 1 THEN NULL ELSE ISNULL(@CredentialID, NULL) END,
                ISNULL(@TokenType, 'Bearer'),
                @ExpiresAt,
                CASE WHEN @Scope_Clear = 1 THEN NULL ELSE ISNULL(@Scope, NULL) END,
                @IssuerURL,
                CASE WHEN @LastRefreshAt_Clear = 1 THEN NULL ELSE ISNULL(@LastRefreshAt, NULL) END,
                ISNULL(@RefreshCount, 0)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[OAuthToken]
            (
                [MCPServerConnectionID],
                [CredentialID],
                [TokenType],
                [ExpiresAt],
                [Scope],
                [IssuerURL],
                [LastRefreshAt],
                [RefreshCount]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @MCPServerConnectionID,
                CASE WHEN @CredentialID_Clear = 1 THEN NULL ELSE ISNULL(@CredentialID, NULL) END,
                ISNULL(@TokenType, 'Bearer'),
                @ExpiresAt,
                CASE WHEN @Scope_Clear = 1 THEN NULL ELSE ISNULL(@Scope, NULL) END,
                @IssuerURL,
                CASE WHEN @LastRefreshAt_Clear = 1 THEN NULL ELSE ISNULL(@LastRefreshAt, NULL) END,
                ISNULL(@RefreshCount, 0)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwOAuthTokens] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateOAuthToken] TO [cdp_Developer], [cdp_Integration]
    