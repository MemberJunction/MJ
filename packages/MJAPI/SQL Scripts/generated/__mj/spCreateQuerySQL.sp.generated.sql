-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Query SQLs
-- Item: spCreateQuerySQL
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR QuerySQL
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateQuerySQL]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateQuerySQL];
GO

CREATE PROCEDURE [__mj].[spCreateQuerySQL]
    @ID uniqueidentifier = NULL,
    @QueryID uniqueidentifier,
    @SQLDialectID uniqueidentifier,
    @SQL nvarchar(MAX)
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[QuerySQL]
            (
                [ID],
                [QueryID],
                [SQLDialectID],
                [SQL]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @QueryID,
                @SQLDialectID,
                @SQL
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[QuerySQL]
            (
                [QueryID],
                [SQLDialectID],
                [SQL]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @QueryID,
                @SQLDialectID,
                @SQL
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwQuerySQLs] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateQuerySQL] TO [cdp_Developer], [cdp_Integration]
    