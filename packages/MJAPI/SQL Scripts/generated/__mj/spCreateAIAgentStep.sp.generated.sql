-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Agent Steps
-- Item: spCreateAIAgentStep
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIAgentStep
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIAgentStep]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIAgentStep];
GO

CREATE PROCEDURE [__mj].[spCreateAIAgentStep]
    @ID uniqueidentifier = NULL,
    @AgentID uniqueidentifier,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @StepType nvarchar(20),
    @StartingStep bit = NULL,
    @TimeoutSeconds_Clear bit = 0,
    @TimeoutSeconds int = NULL,
    @RetryCount int = NULL,
    @OnErrorBehavior nvarchar(20) = NULL,
    @ActionID_Clear bit = 0,
    @ActionID uniqueidentifier = NULL,
    @SubAgentID_Clear bit = 0,
    @SubAgentID uniqueidentifier = NULL,
    @PromptID_Clear bit = 0,
    @PromptID uniqueidentifier = NULL,
    @ActionOutputMapping_Clear bit = 0,
    @ActionOutputMapping nvarchar(MAX) = NULL,
    @PositionX int = NULL,
    @PositionY int = NULL,
    @Width int = NULL,
    @Height int = NULL,
    @Status nvarchar(20) = NULL,
    @ActionInputMapping_Clear bit = 0,
    @ActionInputMapping nvarchar(MAX) = NULL,
    @LoopBodyType_Clear bit = 0,
    @LoopBodyType nvarchar(50) = NULL,
    @Configuration_Clear bit = 0,
    @Configuration nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIAgentStep]
            (
                [ID],
                [AgentID],
                [Name],
                [Description],
                [StepType],
                [StartingStep],
                [TimeoutSeconds],
                [RetryCount],
                [OnErrorBehavior],
                [ActionID],
                [SubAgentID],
                [PromptID],
                [ActionOutputMapping],
                [PositionX],
                [PositionY],
                [Width],
                [Height],
                [Status],
                [ActionInputMapping],
                [LoopBodyType],
                [Configuration]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @AgentID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @StepType,
                ISNULL(@StartingStep, 0),
                CASE WHEN @TimeoutSeconds_Clear = 1 THEN NULL ELSE ISNULL(@TimeoutSeconds, 600) END,
                ISNULL(@RetryCount, 0),
                ISNULL(@OnErrorBehavior, 'fail'),
                CASE WHEN @ActionID_Clear = 1 THEN NULL ELSE ISNULL(@ActionID, NULL) END,
                CASE WHEN @SubAgentID_Clear = 1 THEN NULL ELSE ISNULL(@SubAgentID, NULL) END,
                CASE WHEN @PromptID_Clear = 1 THEN NULL ELSE ISNULL(@PromptID, NULL) END,
                CASE WHEN @ActionOutputMapping_Clear = 1 THEN NULL ELSE ISNULL(@ActionOutputMapping, NULL) END,
                ISNULL(@PositionX, 0),
                ISNULL(@PositionY, 0),
                ISNULL(@Width, 200),
                ISNULL(@Height, 80),
                ISNULL(@Status, 'Active'),
                CASE WHEN @ActionInputMapping_Clear = 1 THEN NULL ELSE ISNULL(@ActionInputMapping, NULL) END,
                CASE WHEN @LoopBodyType_Clear = 1 THEN NULL ELSE ISNULL(@LoopBodyType, NULL) END,
                CASE WHEN @Configuration_Clear = 1 THEN NULL ELSE ISNULL(@Configuration, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIAgentStep]
            (
                [AgentID],
                [Name],
                [Description],
                [StepType],
                [StartingStep],
                [TimeoutSeconds],
                [RetryCount],
                [OnErrorBehavior],
                [ActionID],
                [SubAgentID],
                [PromptID],
                [ActionOutputMapping],
                [PositionX],
                [PositionY],
                [Width],
                [Height],
                [Status],
                [ActionInputMapping],
                [LoopBodyType],
                [Configuration]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @AgentID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @StepType,
                ISNULL(@StartingStep, 0),
                CASE WHEN @TimeoutSeconds_Clear = 1 THEN NULL ELSE ISNULL(@TimeoutSeconds, 600) END,
                ISNULL(@RetryCount, 0),
                ISNULL(@OnErrorBehavior, 'fail'),
                CASE WHEN @ActionID_Clear = 1 THEN NULL ELSE ISNULL(@ActionID, NULL) END,
                CASE WHEN @SubAgentID_Clear = 1 THEN NULL ELSE ISNULL(@SubAgentID, NULL) END,
                CASE WHEN @PromptID_Clear = 1 THEN NULL ELSE ISNULL(@PromptID, NULL) END,
                CASE WHEN @ActionOutputMapping_Clear = 1 THEN NULL ELSE ISNULL(@ActionOutputMapping, NULL) END,
                ISNULL(@PositionX, 0),
                ISNULL(@PositionY, 0),
                ISNULL(@Width, 200),
                ISNULL(@Height, 80),
                ISNULL(@Status, 'Active'),
                CASE WHEN @ActionInputMapping_Clear = 1 THEN NULL ELSE ISNULL(@ActionInputMapping, NULL) END,
                CASE WHEN @LoopBodyType_Clear = 1 THEN NULL ELSE ISNULL(@LoopBodyType, NULL) END,
                CASE WHEN @Configuration_Clear = 1 THEN NULL ELSE ISNULL(@Configuration, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIAgentSteps] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIAgentStep] TO [cdp_Developer], [cdp_Integration]
    