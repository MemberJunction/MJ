-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Search Scope Storage Accounts
-- Item: spCreateSearchScopeStorageAccount
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR SearchScopeStorageAccount
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateSearchScopeStorageAccount]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateSearchScopeStorageAccount];
GO

CREATE PROCEDURE [__mj].[spCreateSearchScopeStorageAccount]
    @ID uniqueidentifier = NULL,
    @SearchScopeID uniqueidentifier,
    @FileStorageAccountID uniqueidentifier,
    @FolderPath_Clear bit = 0,
    @FolderPath nvarchar(1000) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[SearchScopeStorageAccount]
            (
                [ID],
                [SearchScopeID],
                [FileStorageAccountID],
                [FolderPath]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @SearchScopeID,
                @FileStorageAccountID,
                CASE WHEN @FolderPath_Clear = 1 THEN NULL ELSE ISNULL(@FolderPath, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[SearchScopeStorageAccount]
            (
                [SearchScopeID],
                [FileStorageAccountID],
                [FolderPath]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @SearchScopeID,
                @FileStorageAccountID,
                CASE WHEN @FolderPath_Clear = 1 THEN NULL ELSE ISNULL(@FolderPath, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwSearchScopeStorageAccounts] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateSearchScopeStorageAccount] TO [cdp_Developer], [cdp_Integration]
    