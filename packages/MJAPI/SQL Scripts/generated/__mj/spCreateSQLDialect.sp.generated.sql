-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: SQL Dialects
-- Item: spCreateSQLDialect
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR SQLDialect
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateSQLDialect]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateSQLDialect];
GO

CREATE PROCEDURE [__mj].[spCreateSQLDialect]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @PlatformKey nvarchar(50),
    @DatabaseName nvarchar(100),
    @LanguageName nvarchar(100),
    @VendorName_Clear bit = 0,
    @VendorName nvarchar(200) = NULL,
    @WebURL_Clear bit = 0,
    @WebURL nvarchar(500) = NULL,
    @Icon_Clear bit = 0,
    @Icon nvarchar(500) = NULL,
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[SQLDialect]
            (
                [ID],
                [Name],
                [PlatformKey],
                [DatabaseName],
                [LanguageName],
                [VendorName],
                [WebURL],
                [Icon],
                [Description]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                @PlatformKey,
                @DatabaseName,
                @LanguageName,
                CASE WHEN @VendorName_Clear = 1 THEN NULL ELSE ISNULL(@VendorName, NULL) END,
                CASE WHEN @WebURL_Clear = 1 THEN NULL ELSE ISNULL(@WebURL, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[SQLDialect]
            (
                [Name],
                [PlatformKey],
                [DatabaseName],
                [LanguageName],
                [VendorName],
                [WebURL],
                [Icon],
                [Description]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                @PlatformKey,
                @DatabaseName,
                @LanguageName,
                CASE WHEN @VendorName_Clear = 1 THEN NULL ELSE ISNULL(@VendorName, NULL) END,
                CASE WHEN @WebURL_Clear = 1 THEN NULL ELSE ISNULL(@WebURL, NULL) END,
                CASE WHEN @Icon_Clear = 1 THEN NULL ELSE ISNULL(@Icon, NULL) END,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwSQLDialects] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateSQLDialect] TO [cdp_Developer], [cdp_Integration]
    