-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Duplicate Run Details
-- Item: spCreateDuplicateRunDetail
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR DuplicateRunDetail
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateDuplicateRunDetail]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateDuplicateRunDetail];
GO

CREATE PROCEDURE [__mj].[spCreateDuplicateRunDetail]
    @ID uniqueidentifier = NULL,
    @DuplicateRunID uniqueidentifier,
    @RecordID nvarchar(500),
    @MatchStatus nvarchar(20) = NULL,
    @SkippedReason_Clear bit = 0,
    @SkippedReason nvarchar(MAX) = NULL,
    @MatchErrorMessage_Clear bit = 0,
    @MatchErrorMessage nvarchar(MAX) = NULL,
    @MergeStatus nvarchar(20) = NULL,
    @MergeErrorMessage_Clear bit = 0,
    @MergeErrorMessage nvarchar(MAX) = NULL,
    @RecordMetadata_Clear bit = 0,
    @RecordMetadata nvarchar(MAX) = NULL,
    @StartedAt_Clear bit = 0,
    @StartedAt datetimeoffset = NULL,
    @EndedAt_Clear bit = 0,
    @EndedAt datetimeoffset = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[DuplicateRunDetail]
            (
                [ID],
                [DuplicateRunID],
                [RecordID],
                [MatchStatus],
                [SkippedReason],
                [MatchErrorMessage],
                [MergeStatus],
                [MergeErrorMessage],
                [RecordMetadata],
                [StartedAt],
                [EndedAt]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @DuplicateRunID,
                @RecordID,
                ISNULL(@MatchStatus, 'Pending'),
                CASE WHEN @SkippedReason_Clear = 1 THEN NULL ELSE ISNULL(@SkippedReason, NULL) END,
                CASE WHEN @MatchErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@MatchErrorMessage, NULL) END,
                ISNULL(@MergeStatus, 'Not Applicable'),
                CASE WHEN @MergeErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@MergeErrorMessage, NULL) END,
                CASE WHEN @RecordMetadata_Clear = 1 THEN NULL ELSE ISNULL(@RecordMetadata, NULL) END,
                CASE WHEN @StartedAt_Clear = 1 THEN NULL ELSE ISNULL(@StartedAt, NULL) END,
                CASE WHEN @EndedAt_Clear = 1 THEN NULL ELSE ISNULL(@EndedAt, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[DuplicateRunDetail]
            (
                [DuplicateRunID],
                [RecordID],
                [MatchStatus],
                [SkippedReason],
                [MatchErrorMessage],
                [MergeStatus],
                [MergeErrorMessage],
                [RecordMetadata],
                [StartedAt],
                [EndedAt]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @DuplicateRunID,
                @RecordID,
                ISNULL(@MatchStatus, 'Pending'),
                CASE WHEN @SkippedReason_Clear = 1 THEN NULL ELSE ISNULL(@SkippedReason, NULL) END,
                CASE WHEN @MatchErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@MatchErrorMessage, NULL) END,
                ISNULL(@MergeStatus, 'Not Applicable'),
                CASE WHEN @MergeErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@MergeErrorMessage, NULL) END,
                CASE WHEN @RecordMetadata_Clear = 1 THEN NULL ELSE ISNULL(@RecordMetadata, NULL) END,
                CASE WHEN @StartedAt_Clear = 1 THEN NULL ELSE ISNULL(@StartedAt, NULL) END,
                CASE WHEN @EndedAt_Clear = 1 THEN NULL ELSE ISNULL(@EndedAt, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwDuplicateRunDetails] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateDuplicateRunDetail] TO [cdp_Developer], [cdp_Integration]
    