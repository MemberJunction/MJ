-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Company Integration Sync Watermarks
-- Item: spCreateCompanyIntegrationSyncWatermark
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR CompanyIntegrationSyncWatermark
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateCompanyIntegrationSyncWatermark]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateCompanyIntegrationSyncWatermark];
GO

CREATE PROCEDURE [__mj].[spCreateCompanyIntegrationSyncWatermark]
    @ID uniqueidentifier = NULL,
    @EntityMapID uniqueidentifier,
    @Direction nvarchar(50) = NULL,
    @WatermarkType nvarchar(50) = NULL,
    @WatermarkValue_Clear bit = 0,
    @WatermarkValue nvarchar(MAX) = NULL,
    @LastSyncAt_Clear bit = 0,
    @LastSyncAt datetimeoffset = NULL,
    @RecordsSynced int = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[CompanyIntegrationSyncWatermark]
            (
                [ID],
                [EntityMapID],
                [Direction],
                [WatermarkType],
                [WatermarkValue],
                [LastSyncAt],
                [RecordsSynced]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @EntityMapID,
                ISNULL(@Direction, 'Pull'),
                ISNULL(@WatermarkType, 'Timestamp'),
                CASE WHEN @WatermarkValue_Clear = 1 THEN NULL ELSE ISNULL(@WatermarkValue, NULL) END,
                CASE WHEN @LastSyncAt_Clear = 1 THEN NULL ELSE ISNULL(@LastSyncAt, NULL) END,
                ISNULL(@RecordsSynced, 0)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[CompanyIntegrationSyncWatermark]
            (
                [EntityMapID],
                [Direction],
                [WatermarkType],
                [WatermarkValue],
                [LastSyncAt],
                [RecordsSynced]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @EntityMapID,
                ISNULL(@Direction, 'Pull'),
                ISNULL(@WatermarkType, 'Timestamp'),
                CASE WHEN @WatermarkValue_Clear = 1 THEN NULL ELSE ISNULL(@WatermarkValue, NULL) END,
                CASE WHEN @LastSyncAt_Clear = 1 THEN NULL ELSE ISNULL(@LastSyncAt, NULL) END,
                ISNULL(@RecordsSynced, 0)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwCompanyIntegrationSyncWatermarks] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateCompanyIntegrationSyncWatermark] TO [cdp_Developer], [cdp_Integration]
    