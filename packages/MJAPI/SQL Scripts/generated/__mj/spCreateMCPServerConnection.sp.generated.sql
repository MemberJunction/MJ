-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: MCP Server Connections
-- Item: spCreateMCPServerConnection
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR MCPServerConnection
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateMCPServerConnection]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateMCPServerConnection];
GO

CREATE PROCEDURE [__mj].[spCreateMCPServerConnection]
    @ID uniqueidentifier = NULL,
    @MCPServerID uniqueidentifier,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @CredentialID_Clear bit = 0,
    @CredentialID uniqueidentifier = NULL,
    @CustomHeaderName_Clear bit = 0,
    @CustomHeaderName nvarchar(100) = NULL,
    @CompanyID_Clear bit = 0,
    @CompanyID uniqueidentifier = NULL,
    @Status nvarchar(50) = NULL,
    @AutoSyncTools bit = NULL,
    @AutoGenerateActions bit = NULL,
    @LogToolCalls bit = NULL,
    @LogInputParameters bit = NULL,
    @LogOutputContent bit = NULL,
    @MaxOutputLogSize_Clear bit = 0,
    @MaxOutputLogSize int = NULL,
    @LastConnectedAt_Clear bit = 0,
    @LastConnectedAt datetimeoffset = NULL,
    @LastErrorMessage_Clear bit = 0,
    @LastErrorMessage nvarchar(MAX) = NULL,
    @EnvironmentVars_Clear bit = 0,
    @EnvironmentVars nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[MCPServerConnection]
            (
                [ID],
                [MCPServerID],
                [Name],
                [Description],
                [CredentialID],
                [CustomHeaderName],
                [CompanyID],
                [Status],
                [AutoSyncTools],
                [AutoGenerateActions],
                [LogToolCalls],
                [LogInputParameters],
                [LogOutputContent],
                [MaxOutputLogSize],
                [LastConnectedAt],
                [LastErrorMessage],
                [EnvironmentVars]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @MCPServerID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @CredentialID_Clear = 1 THEN NULL ELSE ISNULL(@CredentialID, NULL) END,
                CASE WHEN @CustomHeaderName_Clear = 1 THEN NULL ELSE ISNULL(@CustomHeaderName, NULL) END,
                CASE WHEN @CompanyID_Clear = 1 THEN NULL ELSE ISNULL(@CompanyID, NULL) END,
                ISNULL(@Status, 'Active'),
                ISNULL(@AutoSyncTools, 1),
                ISNULL(@AutoGenerateActions, 0),
                ISNULL(@LogToolCalls, 1),
                ISNULL(@LogInputParameters, 1),
                ISNULL(@LogOutputContent, 1),
                CASE WHEN @MaxOutputLogSize_Clear = 1 THEN NULL ELSE ISNULL(@MaxOutputLogSize, 102400) END,
                CASE WHEN @LastConnectedAt_Clear = 1 THEN NULL ELSE ISNULL(@LastConnectedAt, NULL) END,
                CASE WHEN @LastErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@LastErrorMessage, NULL) END,
                CASE WHEN @EnvironmentVars_Clear = 1 THEN NULL ELSE ISNULL(@EnvironmentVars, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[MCPServerConnection]
            (
                [MCPServerID],
                [Name],
                [Description],
                [CredentialID],
                [CustomHeaderName],
                [CompanyID],
                [Status],
                [AutoSyncTools],
                [AutoGenerateActions],
                [LogToolCalls],
                [LogInputParameters],
                [LogOutputContent],
                [MaxOutputLogSize],
                [LastConnectedAt],
                [LastErrorMessage],
                [EnvironmentVars]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @MCPServerID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @CredentialID_Clear = 1 THEN NULL ELSE ISNULL(@CredentialID, NULL) END,
                CASE WHEN @CustomHeaderName_Clear = 1 THEN NULL ELSE ISNULL(@CustomHeaderName, NULL) END,
                CASE WHEN @CompanyID_Clear = 1 THEN NULL ELSE ISNULL(@CompanyID, NULL) END,
                ISNULL(@Status, 'Active'),
                ISNULL(@AutoSyncTools, 1),
                ISNULL(@AutoGenerateActions, 0),
                ISNULL(@LogToolCalls, 1),
                ISNULL(@LogInputParameters, 1),
                ISNULL(@LogOutputContent, 1),
                CASE WHEN @MaxOutputLogSize_Clear = 1 THEN NULL ELSE ISNULL(@MaxOutputLogSize, 102400) END,
                CASE WHEN @LastConnectedAt_Clear = 1 THEN NULL ELSE ISNULL(@LastConnectedAt, NULL) END,
                CASE WHEN @LastErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@LastErrorMessage, NULL) END,
                CASE WHEN @EnvironmentVars_Clear = 1 THEN NULL ELSE ISNULL(@EnvironmentVars, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwMCPServerConnections] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateMCPServerConnection] TO [cdp_Developer], [cdp_Integration]
    