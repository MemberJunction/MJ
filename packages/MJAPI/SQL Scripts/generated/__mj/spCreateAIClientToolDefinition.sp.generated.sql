-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Client Tool Definitions
-- Item: spCreateAIClientToolDefinition
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIClientToolDefinition
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIClientToolDefinition]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIClientToolDefinition];
GO

CREATE PROCEDURE [__mj].[spCreateAIClientToolDefinition]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(200),
    @Description nvarchar(MAX),
    @Category_Clear bit = 0,
    @Category nvarchar(50) = NULL,
    @InputSchemaJSON_Clear bit = 0,
    @InputSchemaJSON nvarchar(MAX) = NULL,
    @OutputSchemaJSON_Clear bit = 0,
    @OutputSchemaJSON nvarchar(MAX) = NULL,
    @DefaultTimeoutMs_Clear bit = 0,
    @DefaultTimeoutMs int = NULL,
    @RequiresContextType_Clear bit = 0,
    @RequiresContextType nvarchar(100) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIClientToolDefinition]
            (
                [ID],
                [Name],
                [Description],
                [Category],
                [InputSchemaJSON],
                [OutputSchemaJSON],
                [DefaultTimeoutMs],
                [RequiresContextType]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                @Description,
                CASE WHEN @Category_Clear = 1 THEN NULL ELSE ISNULL(@Category, NULL) END,
                CASE WHEN @InputSchemaJSON_Clear = 1 THEN NULL ELSE ISNULL(@InputSchemaJSON, NULL) END,
                CASE WHEN @OutputSchemaJSON_Clear = 1 THEN NULL ELSE ISNULL(@OutputSchemaJSON, NULL) END,
                CASE WHEN @DefaultTimeoutMs_Clear = 1 THEN NULL ELSE ISNULL(@DefaultTimeoutMs, 30000) END,
                CASE WHEN @RequiresContextType_Clear = 1 THEN NULL ELSE ISNULL(@RequiresContextType, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIClientToolDefinition]
            (
                [Name],
                [Description],
                [Category],
                [InputSchemaJSON],
                [OutputSchemaJSON],
                [DefaultTimeoutMs],
                [RequiresContextType]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                @Description,
                CASE WHEN @Category_Clear = 1 THEN NULL ELSE ISNULL(@Category, NULL) END,
                CASE WHEN @InputSchemaJSON_Clear = 1 THEN NULL ELSE ISNULL(@InputSchemaJSON, NULL) END,
                CASE WHEN @OutputSchemaJSON_Clear = 1 THEN NULL ELSE ISNULL(@OutputSchemaJSON, NULL) END,
                CASE WHEN @DefaultTimeoutMs_Clear = 1 THEN NULL ELSE ISNULL(@DefaultTimeoutMs, 30000) END,
                CASE WHEN @RequiresContextType_Clear = 1 THEN NULL ELSE ISNULL(@RequiresContextType, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIClientToolDefinitions] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIClientToolDefinition] TO [cdp_Developer], [cdp_Integration]
    