-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Dashboard Permissions
-- Item: spCreateDashboardPermission
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR DashboardPermission
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateDashboardPermission]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateDashboardPermission];
GO

CREATE PROCEDURE [__mj].[spCreateDashboardPermission]
    @ID uniqueidentifier = NULL,
    @DashboardID uniqueidentifier,
    @UserID uniqueidentifier,
    @CanRead bit = NULL,
    @CanEdit bit = NULL,
    @CanDelete bit = NULL,
    @CanShare bit = NULL,
    @SharedByUserID_Clear bit = 0,
    @SharedByUserID uniqueidentifier = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[DashboardPermission]
            (
                [ID],
                [DashboardID],
                [UserID],
                [CanRead],
                [CanEdit],
                [CanDelete],
                [CanShare],
                [SharedByUserID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @DashboardID,
                @UserID,
                ISNULL(@CanRead, 1),
                ISNULL(@CanEdit, 0),
                ISNULL(@CanDelete, 0),
                ISNULL(@CanShare, 0),
                CASE WHEN @SharedByUserID_Clear = 1 THEN NULL ELSE ISNULL(@SharedByUserID, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[DashboardPermission]
            (
                [DashboardID],
                [UserID],
                [CanRead],
                [CanEdit],
                [CanDelete],
                [CanShare],
                [SharedByUserID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @DashboardID,
                @UserID,
                ISNULL(@CanRead, 1),
                ISNULL(@CanEdit, 0),
                ISNULL(@CanDelete, 0),
                ISNULL(@CanShare, 0),
                CASE WHEN @SharedByUserID_Clear = 1 THEN NULL ELSE ISNULL(@SharedByUserID, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwDashboardPermissions] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateDashboardPermission] TO [cdp_UI], [cdp_Developer], [cdp_Integration]
    