-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Architectures
-- Item: spCreateAIArchitecture
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIArchitecture
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIArchitecture]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIArchitecture];
GO

CREATE PROCEDURE [__mj].[spCreateAIArchitecture]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @Category nvarchar(50),
    @ParentArchitectureID_Clear bit = 0,
    @ParentArchitectureID uniqueidentifier = NULL,
    @WikipediaURL_Clear bit = 0,
    @WikipediaURL nvarchar(500) = NULL,
    @YearIntroduced_Clear bit = 0,
    @YearIntroduced int = NULL,
    @KeyPaper_Clear bit = 0,
    @KeyPaper nvarchar(500) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIArchitecture]
            (
                [ID],
                [Name],
                [Description],
                [Category],
                [ParentArchitectureID],
                [WikipediaURL],
                [YearIntroduced],
                [KeyPaper]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @Category,
                CASE WHEN @ParentArchitectureID_Clear = 1 THEN NULL ELSE ISNULL(@ParentArchitectureID, NULL) END,
                CASE WHEN @WikipediaURL_Clear = 1 THEN NULL ELSE ISNULL(@WikipediaURL, NULL) END,
                CASE WHEN @YearIntroduced_Clear = 1 THEN NULL ELSE ISNULL(@YearIntroduced, NULL) END,
                CASE WHEN @KeyPaper_Clear = 1 THEN NULL ELSE ISNULL(@KeyPaper, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIArchitecture]
            (
                [Name],
                [Description],
                [Category],
                [ParentArchitectureID],
                [WikipediaURL],
                [YearIntroduced],
                [KeyPaper]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @Category,
                CASE WHEN @ParentArchitectureID_Clear = 1 THEN NULL ELSE ISNULL(@ParentArchitectureID, NULL) END,
                CASE WHEN @WikipediaURL_Clear = 1 THEN NULL ELSE ISNULL(@WikipediaURL, NULL) END,
                CASE WHEN @YearIntroduced_Clear = 1 THEN NULL ELSE ISNULL(@YearIntroduced, NULL) END,
                CASE WHEN @KeyPaper_Clear = 1 THEN NULL ELSE ISNULL(@KeyPaper, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIArchitectures] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIArchitecture] TO [cdp_Developer], [cdp_Integration]
    