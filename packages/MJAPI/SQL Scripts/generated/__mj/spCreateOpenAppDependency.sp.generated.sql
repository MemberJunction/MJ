-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Open App Dependencies
-- Item: spCreateOpenAppDependency
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR OpenAppDependency
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateOpenAppDependency]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateOpenAppDependency];
GO

CREATE PROCEDURE [__mj].[spCreateOpenAppDependency]
    @ID uniqueidentifier = NULL,
    @OpenAppID uniqueidentifier,
    @DependsOnAppName nvarchar(64),
    @DependsOnAppID_Clear bit = 0,
    @DependsOnAppID uniqueidentifier = NULL,
    @VersionRange nvarchar(100),
    @InstalledVersion_Clear bit = 0,
    @InstalledVersion nvarchar(50) = NULL,
    @Status nvarchar(20) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[OpenAppDependency]
            (
                [ID],
                [OpenAppID],
                [DependsOnAppName],
                [DependsOnAppID],
                [VersionRange],
                [InstalledVersion],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @OpenAppID,
                @DependsOnAppName,
                CASE WHEN @DependsOnAppID_Clear = 1 THEN NULL ELSE ISNULL(@DependsOnAppID, NULL) END,
                @VersionRange,
                CASE WHEN @InstalledVersion_Clear = 1 THEN NULL ELSE ISNULL(@InstalledVersion, NULL) END,
                ISNULL(@Status, 'Satisfied')
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[OpenAppDependency]
            (
                [OpenAppID],
                [DependsOnAppName],
                [DependsOnAppID],
                [VersionRange],
                [InstalledVersion],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @OpenAppID,
                @DependsOnAppName,
                CASE WHEN @DependsOnAppID_Clear = 1 THEN NULL ELSE ISNULL(@DependsOnAppID, NULL) END,
                @VersionRange,
                CASE WHEN @InstalledVersion_Clear = 1 THEN NULL ELSE ISNULL(@InstalledVersion, NULL) END,
                ISNULL(@Status, 'Satisfied')
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwOpenAppDependencies] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateOpenAppDependency] TO [cdp_Developer], [cdp_Integration]
    