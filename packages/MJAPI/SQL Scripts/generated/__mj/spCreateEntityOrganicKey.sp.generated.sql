-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Entity Organic Keys
-- Item: spCreateEntityOrganicKey
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR EntityOrganicKey
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateEntityOrganicKey]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateEntityOrganicKey];
GO

CREATE PROCEDURE [__mj].[spCreateEntityOrganicKey]
    @ID uniqueidentifier = NULL,
    @EntityID uniqueidentifier,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @MatchFieldNames nvarchar(500),
    @NormalizationStrategy nvarchar(50) = NULL,
    @CustomNormalizationExpression_Clear bit = 0,
    @CustomNormalizationExpression nvarchar(MAX) = NULL,
    @AutoCreateRelatedViewOnForm bit = NULL,
    @Sequence int = NULL,
    @Status nvarchar(20) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[EntityOrganicKey]
            (
                [ID],
                [EntityID],
                [Name],
                [Description],
                [MatchFieldNames],
                [NormalizationStrategy],
                [CustomNormalizationExpression],
                [AutoCreateRelatedViewOnForm],
                [Sequence],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @EntityID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @MatchFieldNames,
                ISNULL(@NormalizationStrategy, 'LowerCaseTrim'),
                CASE WHEN @CustomNormalizationExpression_Clear = 1 THEN NULL ELSE ISNULL(@CustomNormalizationExpression, NULL) END,
                ISNULL(@AutoCreateRelatedViewOnForm, 0),
                ISNULL(@Sequence, 0),
                ISNULL(@Status, 'Active')
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[EntityOrganicKey]
            (
                [EntityID],
                [Name],
                [Description],
                [MatchFieldNames],
                [NormalizationStrategy],
                [CustomNormalizationExpression],
                [AutoCreateRelatedViewOnForm],
                [Sequence],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @EntityID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @MatchFieldNames,
                ISNULL(@NormalizationStrategy, 'LowerCaseTrim'),
                CASE WHEN @CustomNormalizationExpression_Clear = 1 THEN NULL ELSE ISNULL(@CustomNormalizationExpression, NULL) END,
                ISNULL(@AutoCreateRelatedViewOnForm, 0),
                ISNULL(@Sequence, 0),
                ISNULL(@Status, 'Active')
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwEntityOrganicKeys] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateEntityOrganicKey] TO [cdp_Developer], [cdp_Integration]
    