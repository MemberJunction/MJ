-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Agent Modalities
-- Item: spCreateAIAgentModality
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIAgentModality
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIAgentModality]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIAgentModality];
GO

CREATE PROCEDURE [__mj].[spCreateAIAgentModality]
    @ID uniqueidentifier = NULL,
    @AgentID uniqueidentifier,
    @ModalityID uniqueidentifier,
    @Direction nvarchar(10),
    @IsAllowed bit = NULL,
    @MaxSizeBytes_Clear bit = 0,
    @MaxSizeBytes int = NULL,
    @MaxCountPerMessage_Clear bit = 0,
    @MaxCountPerMessage int = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIAgentModality]
            (
                [ID],
                [AgentID],
                [ModalityID],
                [Direction],
                [IsAllowed],
                [MaxSizeBytes],
                [MaxCountPerMessage]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @AgentID,
                @ModalityID,
                @Direction,
                ISNULL(@IsAllowed, 1),
                CASE WHEN @MaxSizeBytes_Clear = 1 THEN NULL ELSE ISNULL(@MaxSizeBytes, NULL) END,
                CASE WHEN @MaxCountPerMessage_Clear = 1 THEN NULL ELSE ISNULL(@MaxCountPerMessage, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIAgentModality]
            (
                [AgentID],
                [ModalityID],
                [Direction],
                [IsAllowed],
                [MaxSizeBytes],
                [MaxCountPerMessage]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @AgentID,
                @ModalityID,
                @Direction,
                ISNULL(@IsAllowed, 1),
                CASE WHEN @MaxSizeBytes_Clear = 1 THEN NULL ELSE ISNULL(@MaxSizeBytes, NULL) END,
                CASE WHEN @MaxCountPerMessage_Clear = 1 THEN NULL ELSE ISNULL(@MaxCountPerMessage, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIAgentModalities] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIAgentModality] TO [cdp_Developer], [cdp_Integration]
    