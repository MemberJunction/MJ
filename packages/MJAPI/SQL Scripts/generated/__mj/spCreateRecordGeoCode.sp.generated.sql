-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Record Geo Codes
-- Item: spCreateRecordGeoCode
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR RecordGeoCode
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateRecordGeoCode]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateRecordGeoCode];
GO

CREATE PROCEDURE [__mj].[spCreateRecordGeoCode]
    @ID uniqueidentifier = NULL,
    @EntityID uniqueidentifier,
    @RecordID nvarchar(450),
    @LocationType nvarchar(50) = NULL,
    @Latitude_Clear bit = 0,
    @Latitude decimal(10, 6) = NULL,
    @Longitude_Clear bit = 0,
    @Longitude decimal(10, 6) = NULL,
    @Precision_Clear bit = 0,
    @Precision nvarchar(20) = NULL,
    @CountryID_Clear bit = 0,
    @CountryID uniqueidentifier = NULL,
    @StateProvinceID_Clear bit = 0,
    @StateProvinceID uniqueidentifier = NULL,
    @Status nvarchar(20) = NULL,
    @ErrorMessage_Clear bit = 0,
    @ErrorMessage nvarchar(MAX) = NULL,
    @RetryCount int = NULL,
    @SourceFieldHash_Clear bit = 0,
    @SourceFieldHash nvarchar(64) = NULL,
    @GeocodedAt_Clear bit = 0,
    @GeocodedAt datetimeoffset = NULL,
    @GeocodingSource_Clear bit = 0,
    @GeocodingSource nvarchar(30) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[RecordGeoCode]
            (
                [ID],
                [EntityID],
                [RecordID],
                [LocationType],
                [Latitude],
                [Longitude],
                [Precision],
                [CountryID],
                [StateProvinceID],
                [Status],
                [ErrorMessage],
                [RetryCount],
                [SourceFieldHash],
                [GeocodedAt],
                [GeocodingSource]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @EntityID,
                @RecordID,
                ISNULL(@LocationType, 'Primary'),
                CASE WHEN @Latitude_Clear = 1 THEN NULL ELSE ISNULL(@Latitude, NULL) END,
                CASE WHEN @Longitude_Clear = 1 THEN NULL ELSE ISNULL(@Longitude, NULL) END,
                CASE WHEN @Precision_Clear = 1 THEN NULL ELSE ISNULL(@Precision, NULL) END,
                CASE WHEN @CountryID_Clear = 1 THEN NULL ELSE ISNULL(@CountryID, NULL) END,
                CASE WHEN @StateProvinceID_Clear = 1 THEN NULL ELSE ISNULL(@StateProvinceID, NULL) END,
                ISNULL(@Status, 'pending'),
                CASE WHEN @ErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@ErrorMessage, NULL) END,
                ISNULL(@RetryCount, 0),
                CASE WHEN @SourceFieldHash_Clear = 1 THEN NULL ELSE ISNULL(@SourceFieldHash, NULL) END,
                CASE WHEN @GeocodedAt_Clear = 1 THEN NULL ELSE ISNULL(@GeocodedAt, NULL) END,
                CASE WHEN @GeocodingSource_Clear = 1 THEN NULL ELSE ISNULL(@GeocodingSource, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[RecordGeoCode]
            (
                [EntityID],
                [RecordID],
                [LocationType],
                [Latitude],
                [Longitude],
                [Precision],
                [CountryID],
                [StateProvinceID],
                [Status],
                [ErrorMessage],
                [RetryCount],
                [SourceFieldHash],
                [GeocodedAt],
                [GeocodingSource]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @EntityID,
                @RecordID,
                ISNULL(@LocationType, 'Primary'),
                CASE WHEN @Latitude_Clear = 1 THEN NULL ELSE ISNULL(@Latitude, NULL) END,
                CASE WHEN @Longitude_Clear = 1 THEN NULL ELSE ISNULL(@Longitude, NULL) END,
                CASE WHEN @Precision_Clear = 1 THEN NULL ELSE ISNULL(@Precision, NULL) END,
                CASE WHEN @CountryID_Clear = 1 THEN NULL ELSE ISNULL(@CountryID, NULL) END,
                CASE WHEN @StateProvinceID_Clear = 1 THEN NULL ELSE ISNULL(@StateProvinceID, NULL) END,
                ISNULL(@Status, 'pending'),
                CASE WHEN @ErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@ErrorMessage, NULL) END,
                ISNULL(@RetryCount, 0),
                CASE WHEN @SourceFieldHash_Clear = 1 THEN NULL ELSE ISNULL(@SourceFieldHash, NULL) END,
                CASE WHEN @GeocodedAt_Clear = 1 THEN NULL ELSE ISNULL(@GeocodedAt, NULL) END,
                CASE WHEN @GeocodingSource_Clear = 1 THEN NULL ELSE ISNULL(@GeocodingSource, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwRecordGeoCodes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateRecordGeoCode] TO [cdp_Developer], [cdp_Integration]
    