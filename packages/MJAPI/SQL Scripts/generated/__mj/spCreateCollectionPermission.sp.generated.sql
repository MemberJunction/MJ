-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Collection Permissions
-- Item: spCreateCollectionPermission
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR CollectionPermission
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateCollectionPermission]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateCollectionPermission];
GO

CREATE PROCEDURE [__mj].[spCreateCollectionPermission]
    @ID uniqueidentifier = NULL,
    @CollectionID uniqueidentifier,
    @UserID uniqueidentifier,
    @CanRead bit = NULL,
    @CanShare bit = NULL,
    @CanEdit bit = NULL,
    @CanDelete bit = NULL,
    @SharedByUserID_Clear bit = 0,
    @SharedByUserID uniqueidentifier = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[CollectionPermission]
            (
                [ID],
                [CollectionID],
                [UserID],
                [CanRead],
                [CanShare],
                [CanEdit],
                [CanDelete],
                [SharedByUserID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @CollectionID,
                @UserID,
                ISNULL(@CanRead, 1),
                ISNULL(@CanShare, 0),
                ISNULL(@CanEdit, 0),
                ISNULL(@CanDelete, 0),
                CASE WHEN @SharedByUserID_Clear = 1 THEN NULL ELSE ISNULL(@SharedByUserID, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[CollectionPermission]
            (
                [CollectionID],
                [UserID],
                [CanRead],
                [CanShare],
                [CanEdit],
                [CanDelete],
                [SharedByUserID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @CollectionID,
                @UserID,
                ISNULL(@CanRead, 1),
                ISNULL(@CanShare, 0),
                ISNULL(@CanEdit, 0),
                ISNULL(@CanDelete, 0),
                CASE WHEN @SharedByUserID_Clear = 1 THEN NULL ELSE ISNULL(@SharedByUserID, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwCollectionPermissions] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateCollectionPermission] TO [cdp_UI], [cdp_Developer], [cdp_Integration]
    