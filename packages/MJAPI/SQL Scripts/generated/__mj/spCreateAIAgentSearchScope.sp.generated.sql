-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Agent Search Scopes
-- Item: spCreateAIAgentSearchScope
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AIAgentSearchScope
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAIAgentSearchScope]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAIAgentSearchScope];
GO

CREATE PROCEDURE [__mj].[spCreateAIAgentSearchScope]
    @ID uniqueidentifier = NULL,
    @AgentID uniqueidentifier,
    @SearchScopeID uniqueidentifier,
    @Phase nvarchar(20) = NULL,
    @Status nvarchar(20) = NULL,
    @StartAt_Clear bit = 0,
    @StartAt datetimeoffset = NULL,
    @EndAt_Clear bit = 0,
    @EndAt datetimeoffset = NULL,
    @Priority int = NULL,
    @MaxResults_Clear bit = 0,
    @MaxResults int = NULL,
    @MinScore_Clear bit = 0,
    @MinScore decimal(5, 4) = NULL,
    @QueryTemplateID_Clear bit = 0,
    @QueryTemplateID uniqueidentifier = NULL,
    @FusionWeightsOverride_Clear bit = 0,
    @FusionWeightsOverride nvarchar(MAX) = NULL,
    @IsDefault bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AIAgentSearchScope]
            (
                [ID],
                [AgentID],
                [SearchScopeID],
                [Phase],
                [Status],
                [StartAt],
                [EndAt],
                [Priority],
                [MaxResults],
                [MinScore],
                [QueryTemplateID],
                [FusionWeightsOverride],
                [IsDefault]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @AgentID,
                @SearchScopeID,
                ISNULL(@Phase, 'Both'),
                ISNULL(@Status, 'Active'),
                CASE WHEN @StartAt_Clear = 1 THEN NULL ELSE ISNULL(@StartAt, NULL) END,
                CASE WHEN @EndAt_Clear = 1 THEN NULL ELSE ISNULL(@EndAt, NULL) END,
                ISNULL(@Priority, 100),
                CASE WHEN @MaxResults_Clear = 1 THEN NULL ELSE ISNULL(@MaxResults, NULL) END,
                CASE WHEN @MinScore_Clear = 1 THEN NULL ELSE ISNULL(@MinScore, NULL) END,
                CASE WHEN @QueryTemplateID_Clear = 1 THEN NULL ELSE ISNULL(@QueryTemplateID, NULL) END,
                CASE WHEN @FusionWeightsOverride_Clear = 1 THEN NULL ELSE ISNULL(@FusionWeightsOverride, NULL) END,
                ISNULL(@IsDefault, 0)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AIAgentSearchScope]
            (
                [AgentID],
                [SearchScopeID],
                [Phase],
                [Status],
                [StartAt],
                [EndAt],
                [Priority],
                [MaxResults],
                [MinScore],
                [QueryTemplateID],
                [FusionWeightsOverride],
                [IsDefault]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @AgentID,
                @SearchScopeID,
                ISNULL(@Phase, 'Both'),
                ISNULL(@Status, 'Active'),
                CASE WHEN @StartAt_Clear = 1 THEN NULL ELSE ISNULL(@StartAt, NULL) END,
                CASE WHEN @EndAt_Clear = 1 THEN NULL ELSE ISNULL(@EndAt, NULL) END,
                ISNULL(@Priority, 100),
                CASE WHEN @MaxResults_Clear = 1 THEN NULL ELSE ISNULL(@MaxResults, NULL) END,
                CASE WHEN @MinScore_Clear = 1 THEN NULL ELSE ISNULL(@MinScore, NULL) END,
                CASE WHEN @QueryTemplateID_Clear = 1 THEN NULL ELSE ISNULL(@QueryTemplateID, NULL) END,
                CASE WHEN @FusionWeightsOverride_Clear = 1 THEN NULL ELSE ISNULL(@FusionWeightsOverride, NULL) END,
                ISNULL(@IsDefault, 0)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAIAgentSearchScopes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAIAgentSearchScope] TO [cdp_Developer], [cdp_Integration]
    