-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Open App Install Histories
-- Item: spCreateOpenAppInstallHistory
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR OpenAppInstallHistory
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateOpenAppInstallHistory]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateOpenAppInstallHistory];
GO

CREATE PROCEDURE [__mj].[spCreateOpenAppInstallHistory]
    @ID uniqueidentifier = NULL,
    @OpenAppID uniqueidentifier,
    @Version nvarchar(50),
    @PreviousVersion_Clear bit = 0,
    @PreviousVersion nvarchar(50) = NULL,
    @Action nvarchar(20),
    @ManifestJSON nvarchar(MAX),
    @Summary_Clear bit = 0,
    @Summary nvarchar(MAX) = NULL,
    @ExecutedByUserID uniqueidentifier,
    @DurationSeconds_Clear bit = 0,
    @DurationSeconds int = NULL,
    @StartedAt_Clear bit = 0,
    @StartedAt datetimeoffset = NULL,
    @EndedAt_Clear bit = 0,
    @EndedAt datetimeoffset = NULL,
    @Success bit = NULL,
    @ErrorMessage_Clear bit = 0,
    @ErrorMessage nvarchar(MAX) = NULL,
    @ErrorPhase_Clear bit = 0,
    @ErrorPhase nvarchar(50) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[OpenAppInstallHistory]
            (
                [ID],
                [OpenAppID],
                [Version],
                [PreviousVersion],
                [Action],
                [ManifestJSON],
                [Summary],
                [ExecutedByUserID],
                [DurationSeconds],
                [StartedAt],
                [EndedAt],
                [Success],
                [ErrorMessage],
                [ErrorPhase]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @OpenAppID,
                @Version,
                CASE WHEN @PreviousVersion_Clear = 1 THEN NULL ELSE ISNULL(@PreviousVersion, NULL) END,
                @Action,
                @ManifestJSON,
                CASE WHEN @Summary_Clear = 1 THEN NULL ELSE ISNULL(@Summary, NULL) END,
                @ExecutedByUserID,
                CASE WHEN @DurationSeconds_Clear = 1 THEN NULL ELSE ISNULL(@DurationSeconds, NULL) END,
                CASE WHEN @StartedAt_Clear = 1 THEN NULL ELSE ISNULL(@StartedAt, NULL) END,
                CASE WHEN @EndedAt_Clear = 1 THEN NULL ELSE ISNULL(@EndedAt, NULL) END,
                ISNULL(@Success, 1),
                CASE WHEN @ErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@ErrorMessage, NULL) END,
                CASE WHEN @ErrorPhase_Clear = 1 THEN NULL ELSE ISNULL(@ErrorPhase, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[OpenAppInstallHistory]
            (
                [OpenAppID],
                [Version],
                [PreviousVersion],
                [Action],
                [ManifestJSON],
                [Summary],
                [ExecutedByUserID],
                [DurationSeconds],
                [StartedAt],
                [EndedAt],
                [Success],
                [ErrorMessage],
                [ErrorPhase]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @OpenAppID,
                @Version,
                CASE WHEN @PreviousVersion_Clear = 1 THEN NULL ELSE ISNULL(@PreviousVersion, NULL) END,
                @Action,
                @ManifestJSON,
                CASE WHEN @Summary_Clear = 1 THEN NULL ELSE ISNULL(@Summary, NULL) END,
                @ExecutedByUserID,
                CASE WHEN @DurationSeconds_Clear = 1 THEN NULL ELSE ISNULL(@DurationSeconds, NULL) END,
                CASE WHEN @StartedAt_Clear = 1 THEN NULL ELSE ISNULL(@StartedAt, NULL) END,
                CASE WHEN @EndedAt_Clear = 1 THEN NULL ELSE ISNULL(@EndedAt, NULL) END,
                ISNULL(@Success, 1),
                CASE WHEN @ErrorMessage_Clear = 1 THEN NULL ELSE ISNULL(@ErrorMessage, NULL) END,
                CASE WHEN @ErrorPhase_Clear = 1 THEN NULL ELSE ISNULL(@ErrorPhase, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwOpenAppInstallHistories] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateOpenAppInstallHistory] TO [cdp_Developer], [cdp_Integration]
    