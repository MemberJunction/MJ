-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: MCP Server Connection Permissions
-- Item: spCreateMCPServerConnectionPermission
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR MCPServerConnectionPermission
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateMCPServerConnectionPermission]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateMCPServerConnectionPermission];
GO

CREATE PROCEDURE [__mj].[spCreateMCPServerConnectionPermission]
    @ID uniqueidentifier = NULL,
    @MCPServerConnectionID uniqueidentifier,
    @UserID_Clear bit = 0,
    @UserID uniqueidentifier = NULL,
    @RoleID_Clear bit = 0,
    @RoleID uniqueidentifier = NULL,
    @CanExecute bit = NULL,
    @CanModify bit = NULL,
    @CanViewCredentials bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[MCPServerConnectionPermission]
            (
                [ID],
                [MCPServerConnectionID],
                [UserID],
                [RoleID],
                [CanExecute],
                [CanModify],
                [CanViewCredentials]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @MCPServerConnectionID,
                CASE WHEN @UserID_Clear = 1 THEN NULL ELSE ISNULL(@UserID, NULL) END,
                CASE WHEN @RoleID_Clear = 1 THEN NULL ELSE ISNULL(@RoleID, NULL) END,
                ISNULL(@CanExecute, 1),
                ISNULL(@CanModify, 0),
                ISNULL(@CanViewCredentials, 0)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[MCPServerConnectionPermission]
            (
                [MCPServerConnectionID],
                [UserID],
                [RoleID],
                [CanExecute],
                [CanModify],
                [CanViewCredentials]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @MCPServerConnectionID,
                CASE WHEN @UserID_Clear = 1 THEN NULL ELSE ISNULL(@UserID, NULL) END,
                CASE WHEN @RoleID_Clear = 1 THEN NULL ELSE ISNULL(@RoleID, NULL) END,
                ISNULL(@CanExecute, 1),
                ISNULL(@CanModify, 0),
                ISNULL(@CanViewCredentials, 0)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwMCPServerConnectionPermissions] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateMCPServerConnectionPermission] TO [cdp_Developer], [cdp_Integration]
    