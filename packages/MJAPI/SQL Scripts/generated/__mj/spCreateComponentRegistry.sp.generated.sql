-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Component Registries
-- Item: spCreateComponentRegistry
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR ComponentRegistry
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateComponentRegistry]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateComponentRegistry];
GO

CREATE PROCEDURE [__mj].[spCreateComponentRegistry]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @URI_Clear bit = 0,
    @URI nvarchar(500) = NULL,
    @Type_Clear bit = 0,
    @Type nvarchar(50) = NULL,
    @APIVersion_Clear bit = 0,
    @APIVersion nvarchar(50) = NULL,
    @Status_Clear bit = 0,
    @Status nvarchar(50) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[ComponentRegistry]
            (
                [ID],
                [Name],
                [Description],
                [URI],
                [Type],
                [APIVersion],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @URI_Clear = 1 THEN NULL ELSE ISNULL(@URI, NULL) END,
                CASE WHEN @Type_Clear = 1 THEN NULL ELSE ISNULL(@Type, NULL) END,
                CASE WHEN @APIVersion_Clear = 1 THEN NULL ELSE ISNULL(@APIVersion, NULL) END,
                CASE WHEN @Status_Clear = 1 THEN NULL ELSE ISNULL(@Status, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[ComponentRegistry]
            (
                [Name],
                [Description],
                [URI],
                [Type],
                [APIVersion],
                [Status]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @URI_Clear = 1 THEN NULL ELSE ISNULL(@URI, NULL) END,
                CASE WHEN @Type_Clear = 1 THEN NULL ELSE ISNULL(@Type, NULL) END,
                CASE WHEN @APIVersion_Clear = 1 THEN NULL ELSE ISNULL(@APIVersion, NULL) END,
                CASE WHEN @Status_Clear = 1 THEN NULL ELSE ISNULL(@Status, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwComponentRegistries] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateComponentRegistry] TO [cdp_Developer], [cdp_Integration]
    