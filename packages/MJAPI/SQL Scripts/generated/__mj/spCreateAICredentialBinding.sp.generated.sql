-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: AI Credential Bindings
-- Item: spCreateAICredentialBinding
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AICredentialBinding
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAICredentialBinding]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAICredentialBinding];
GO

CREATE PROCEDURE [__mj].[spCreateAICredentialBinding]
    @ID uniqueidentifier = NULL,
    @CredentialID uniqueidentifier,
    @BindingType nvarchar(20),
    @AIVendorID_Clear bit = 0,
    @AIVendorID uniqueidentifier = NULL,
    @AIModelVendorID_Clear bit = 0,
    @AIModelVendorID uniqueidentifier = NULL,
    @AIPromptModelID_Clear bit = 0,
    @AIPromptModelID uniqueidentifier = NULL,
    @Priority int = NULL,
    @IsActive bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AICredentialBinding]
            (
                [ID],
                [CredentialID],
                [BindingType],
                [AIVendorID],
                [AIModelVendorID],
                [AIPromptModelID],
                [Priority],
                [IsActive]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @CredentialID,
                @BindingType,
                CASE WHEN @AIVendorID_Clear = 1 THEN NULL ELSE ISNULL(@AIVendorID, NULL) END,
                CASE WHEN @AIModelVendorID_Clear = 1 THEN NULL ELSE ISNULL(@AIModelVendorID, NULL) END,
                CASE WHEN @AIPromptModelID_Clear = 1 THEN NULL ELSE ISNULL(@AIPromptModelID, NULL) END,
                ISNULL(@Priority, 0),
                ISNULL(@IsActive, 1)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AICredentialBinding]
            (
                [CredentialID],
                [BindingType],
                [AIVendorID],
                [AIModelVendorID],
                [AIPromptModelID],
                [Priority],
                [IsActive]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @CredentialID,
                @BindingType,
                CASE WHEN @AIVendorID_Clear = 1 THEN NULL ELSE ISNULL(@AIVendorID, NULL) END,
                CASE WHEN @AIModelVendorID_Clear = 1 THEN NULL ELSE ISNULL(@AIModelVendorID, NULL) END,
                CASE WHEN @AIPromptModelID_Clear = 1 THEN NULL ELSE ISNULL(@AIPromptModelID, NULL) END,
                ISNULL(@Priority, 0),
                ISNULL(@IsActive, 1)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAICredentialBindings] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAICredentialBinding] TO [cdp_Developer], [cdp_Integration]
    