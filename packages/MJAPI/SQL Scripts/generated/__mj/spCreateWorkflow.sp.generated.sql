-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Workflows
-- Item: spCreateWorkflow
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR Workflow
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateWorkflow]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateWorkflow];
GO

CREATE PROCEDURE [__mj].[spCreateWorkflow]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @WorkflowEngineID uniqueidentifier,
    @ExternalSystemRecordID nvarchar(100),
    @AutoRunEnabled bit = NULL,
    @AutoRunIntervalUnits_Clear bit = 0,
    @AutoRunIntervalUnits nvarchar(20) = NULL,
    @AutoRunInterval_Clear bit = 0,
    @AutoRunInterval int = NULL,
    @SubclassName_Clear bit = 0,
    @SubclassName nvarchar(200) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[Workflow]
            (
                [ID],
                [Name],
                [Description],
                [WorkflowEngineID],
                [ExternalSystemRecordID],
                [AutoRunEnabled],
                [AutoRunIntervalUnits],
                [AutoRunInterval],
                [SubclassName]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @WorkflowEngineID,
                @ExternalSystemRecordID,
                ISNULL(@AutoRunEnabled, 0),
                CASE WHEN @AutoRunIntervalUnits_Clear = 1 THEN NULL ELSE ISNULL(@AutoRunIntervalUnits, NULL) END,
                CASE WHEN @AutoRunInterval_Clear = 1 THEN NULL ELSE ISNULL(@AutoRunInterval, NULL) END,
                CASE WHEN @SubclassName_Clear = 1 THEN NULL ELSE ISNULL(@SubclassName, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[Workflow]
            (
                [Name],
                [Description],
                [WorkflowEngineID],
                [ExternalSystemRecordID],
                [AutoRunEnabled],
                [AutoRunIntervalUnits],
                [AutoRunInterval],
                [SubclassName]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @WorkflowEngineID,
                @ExternalSystemRecordID,
                ISNULL(@AutoRunEnabled, 0),
                CASE WHEN @AutoRunIntervalUnits_Clear = 1 THEN NULL ELSE ISNULL(@AutoRunIntervalUnits, NULL) END,
                CASE WHEN @AutoRunInterval_Clear = 1 THEN NULL ELSE ISNULL(@AutoRunInterval, NULL) END,
                CASE WHEN @SubclassName_Clear = 1 THEN NULL ELSE ISNULL(@SubclassName, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwWorkflows] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateWorkflow] TO [cdp_Developer], [cdp_Integration]
    