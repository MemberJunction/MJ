-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Scheduled Actions
-- Item: spCreateScheduledAction
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR ScheduledAction
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateScheduledAction]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateScheduledAction];
GO

CREATE PROCEDURE [__mj].[spCreateScheduledAction]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @CreatedByUserID uniqueidentifier,
    @ActionID uniqueidentifier,
    @Type nvarchar(20),
    @CronExpression_Clear bit = 0,
    @CronExpression nvarchar(100) = NULL,
    @Timezone nvarchar(100),
    @Status nvarchar(20) = NULL,
    @IntervalDays_Clear bit = 0,
    @IntervalDays int = NULL,
    @DayOfWeek_Clear bit = 0,
    @DayOfWeek nvarchar(20) = NULL,
    @DayOfMonth_Clear bit = 0,
    @DayOfMonth int = NULL,
    @Month_Clear bit = 0,
    @Month nvarchar(20) = NULL,
    @CustomCronExpression_Clear bit = 0,
    @CustomCronExpression nvarchar(255) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[ScheduledAction]
            (
                [ID],
                [Name],
                [Description],
                [CreatedByUserID],
                [ActionID],
                [Type],
                [CronExpression],
                [Timezone],
                [Status],
                [IntervalDays],
                [DayOfWeek],
                [DayOfMonth],
                [Month],
                [CustomCronExpression]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @CreatedByUserID,
                @ActionID,
                @Type,
                CASE WHEN @CronExpression_Clear = 1 THEN NULL ELSE ISNULL(@CronExpression, NULL) END,
                @Timezone,
                ISNULL(@Status, 'Pending'),
                CASE WHEN @IntervalDays_Clear = 1 THEN NULL ELSE ISNULL(@IntervalDays, NULL) END,
                CASE WHEN @DayOfWeek_Clear = 1 THEN NULL ELSE ISNULL(@DayOfWeek, NULL) END,
                CASE WHEN @DayOfMonth_Clear = 1 THEN NULL ELSE ISNULL(@DayOfMonth, NULL) END,
                CASE WHEN @Month_Clear = 1 THEN NULL ELSE ISNULL(@Month, NULL) END,
                CASE WHEN @CustomCronExpression_Clear = 1 THEN NULL ELSE ISNULL(@CustomCronExpression, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[ScheduledAction]
            (
                [Name],
                [Description],
                [CreatedByUserID],
                [ActionID],
                [Type],
                [CronExpression],
                [Timezone],
                [Status],
                [IntervalDays],
                [DayOfWeek],
                [DayOfMonth],
                [Month],
                [CustomCronExpression]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @CreatedByUserID,
                @ActionID,
                @Type,
                CASE WHEN @CronExpression_Clear = 1 THEN NULL ELSE ISNULL(@CronExpression, NULL) END,
                @Timezone,
                ISNULL(@Status, 'Pending'),
                CASE WHEN @IntervalDays_Clear = 1 THEN NULL ELSE ISNULL(@IntervalDays, NULL) END,
                CASE WHEN @DayOfWeek_Clear = 1 THEN NULL ELSE ISNULL(@DayOfWeek, NULL) END,
                CASE WHEN @DayOfMonth_Clear = 1 THEN NULL ELSE ISNULL(@DayOfMonth, NULL) END,
                CASE WHEN @Month_Clear = 1 THEN NULL ELSE ISNULL(@Month, NULL) END,
                CASE WHEN @CustomCronExpression_Clear = 1 THEN NULL ELSE ISNULL(@CustomCronExpression, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwScheduledActions] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateScheduledAction] TO [cdp_Integration], [cdp_Developer]
    