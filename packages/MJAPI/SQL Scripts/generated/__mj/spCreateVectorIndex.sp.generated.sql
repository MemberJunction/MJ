-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Vector Indexes
-- Item: spCreateVectorIndex
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR VectorIndex
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateVectorIndex]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateVectorIndex];
GO

CREATE PROCEDURE [__mj].[spCreateVectorIndex]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(255),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @VectorDatabaseID uniqueidentifier,
    @EmbeddingModelID uniqueidentifier,
    @ExternalID_Clear bit = 0,
    @ExternalID nvarchar(500) = NULL,
    @Dimensions_Clear bit = 0,
    @Dimensions int = NULL,
    @Metric_Clear bit = 0,
    @Metric nvarchar(50) = NULL,
    @ProviderConfig_Clear bit = 0,
    @ProviderConfig nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[VectorIndex]
            (
                [ID],
                [Name],
                [Description],
                [VectorDatabaseID],
                [EmbeddingModelID],
                [ExternalID],
                [Dimensions],
                [Metric],
                [ProviderConfig]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @VectorDatabaseID,
                @EmbeddingModelID,
                CASE WHEN @ExternalID_Clear = 1 THEN NULL ELSE ISNULL(@ExternalID, NULL) END,
                CASE WHEN @Dimensions_Clear = 1 THEN NULL ELSE ISNULL(@Dimensions, NULL) END,
                CASE WHEN @Metric_Clear = 1 THEN NULL ELSE ISNULL(@Metric, NULL) END,
                CASE WHEN @ProviderConfig_Clear = 1 THEN NULL ELSE ISNULL(@ProviderConfig, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[VectorIndex]
            (
                [Name],
                [Description],
                [VectorDatabaseID],
                [EmbeddingModelID],
                [ExternalID],
                [Dimensions],
                [Metric],
                [ProviderConfig]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @VectorDatabaseID,
                @EmbeddingModelID,
                CASE WHEN @ExternalID_Clear = 1 THEN NULL ELSE ISNULL(@ExternalID, NULL) END,
                CASE WHEN @Dimensions_Clear = 1 THEN NULL ELSE ISNULL(@Dimensions, NULL) END,
                CASE WHEN @Metric_Clear = 1 THEN NULL ELSE ISNULL(@Metric, NULL) END,
                CASE WHEN @ProviderConfig_Clear = 1 THEN NULL ELSE ISNULL(@ProviderConfig, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwVectorIndexes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateVectorIndex] TO [cdp_Integration], [cdp_Developer]
    