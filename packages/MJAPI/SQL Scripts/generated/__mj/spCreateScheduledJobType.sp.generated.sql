-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Scheduled Job Types
-- Item: spCreateScheduledJobType
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR ScheduledJobType
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateScheduledJobType]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateScheduledJobType];
GO

CREATE PROCEDURE [__mj].[spCreateScheduledJobType]
    @ID uniqueidentifier = NULL,
    @Name nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @DriverClass nvarchar(255),
    @DomainRunEntity_Clear bit = 0,
    @DomainRunEntity nvarchar(255) = NULL,
    @DomainRunEntityFKey_Clear bit = 0,
    @DomainRunEntityFKey nvarchar(100) = NULL,
    @NotificationsAvailable bit = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[ScheduledJobType]
            (
                [ID],
                [Name],
                [Description],
                [DriverClass],
                [DomainRunEntity],
                [DomainRunEntityFKey],
                [NotificationsAvailable]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DriverClass,
                CASE WHEN @DomainRunEntity_Clear = 1 THEN NULL ELSE ISNULL(@DomainRunEntity, NULL) END,
                CASE WHEN @DomainRunEntityFKey_Clear = 1 THEN NULL ELSE ISNULL(@DomainRunEntityFKey, NULL) END,
                ISNULL(@NotificationsAvailable, 1)
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[ScheduledJobType]
            (
                [Name],
                [Description],
                [DriverClass],
                [DomainRunEntity],
                [DomainRunEntityFKey],
                [NotificationsAvailable]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Name,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                @DriverClass,
                CASE WHEN @DomainRunEntity_Clear = 1 THEN NULL ELSE ISNULL(@DomainRunEntity, NULL) END,
                CASE WHEN @DomainRunEntityFKey_Clear = 1 THEN NULL ELSE ISNULL(@DomainRunEntityFKey, NULL) END,
                ISNULL(@NotificationsAvailable, 1)
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwScheduledJobTypes] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateScheduledJobType] TO [cdp_Developer], [cdp_Integration]
    