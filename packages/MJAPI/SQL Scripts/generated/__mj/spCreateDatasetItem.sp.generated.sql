-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Dataset Items
-- Item: spCreateDatasetItem
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR DatasetItem
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateDatasetItem]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateDatasetItem];
GO

CREATE PROCEDURE [__mj].[spCreateDatasetItem]
    @ID uniqueidentifier = NULL,
    @Code nvarchar(50),
    @DatasetID uniqueidentifier,
    @Sequence int = NULL,
    @EntityID uniqueidentifier,
    @WhereClause_Clear bit = 0,
    @WhereClause nvarchar(MAX) = NULL,
    @DateFieldToCheck nvarchar(100),
    @Description_Clear bit = 0,
    @Description nvarchar(MAX) = NULL,
    @Columns_Clear bit = 0,
    @Columns nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[DatasetItem]
            (
                [ID],
                [Code],
                [DatasetID],
                [Sequence],
                [EntityID],
                [WhereClause],
                [DateFieldToCheck],
                [Description],
                [Columns]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @Code,
                @DatasetID,
                ISNULL(@Sequence, 0),
                @EntityID,
                CASE WHEN @WhereClause_Clear = 1 THEN NULL ELSE ISNULL(@WhereClause, NULL) END,
                @DateFieldToCheck,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @Columns_Clear = 1 THEN NULL ELSE ISNULL(@Columns, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[DatasetItem]
            (
                [Code],
                [DatasetID],
                [Sequence],
                [EntityID],
                [WhereClause],
                [DateFieldToCheck],
                [Description],
                [Columns]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @Code,
                @DatasetID,
                ISNULL(@Sequence, 0),
                @EntityID,
                CASE WHEN @WhereClause_Clear = 1 THEN NULL ELSE ISNULL(@WhereClause, NULL) END,
                @DateFieldToCheck,
                CASE WHEN @Description_Clear = 1 THEN NULL ELSE ISNULL(@Description, NULL) END,
                CASE WHEN @Columns_Clear = 1 THEN NULL ELSE ISNULL(@Columns, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwDatasetItems] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateDatasetItem] TO [cdp_Developer], [cdp_Integration]
    