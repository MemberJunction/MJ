-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Search Scope Test Queries
-- Item: spCreateSearchScopeTestQuery
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR SearchScopeTestQuery
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateSearchScopeTestQuery]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateSearchScopeTestQuery];
GO

CREATE PROCEDURE [__mj].[spCreateSearchScopeTestQuery]
    @ID uniqueidentifier = NULL,
    @SearchScopeID uniqueidentifier,
    @Label nvarchar(200),
    @Query nvarchar(MAX),
    @ExpectedTopResultEntity_Clear bit = 0,
    @ExpectedTopResultEntity nvarchar(255) = NULL,
    @ExpectedTopResultRecordID_Clear bit = 0,
    @ExpectedTopResultRecordID uniqueidentifier = NULL,
    @Notes_Clear bit = 0,
    @Notes nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[SearchScopeTestQuery]
            (
                [ID],
                [SearchScopeID],
                [Label],
                [Query],
                [ExpectedTopResultEntity],
                [ExpectedTopResultRecordID],
                [Notes]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @SearchScopeID,
                @Label,
                @Query,
                CASE WHEN @ExpectedTopResultEntity_Clear = 1 THEN NULL ELSE ISNULL(@ExpectedTopResultEntity, NULL) END,
                CASE WHEN @ExpectedTopResultRecordID_Clear = 1 THEN NULL ELSE ISNULL(@ExpectedTopResultRecordID, NULL) END,
                CASE WHEN @Notes_Clear = 1 THEN NULL ELSE ISNULL(@Notes, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[SearchScopeTestQuery]
            (
                [SearchScopeID],
                [Label],
                [Query],
                [ExpectedTopResultEntity],
                [ExpectedTopResultRecordID],
                [Notes]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @SearchScopeID,
                @Label,
                @Query,
                CASE WHEN @ExpectedTopResultEntity_Clear = 1 THEN NULL ELSE ISNULL(@ExpectedTopResultEntity, NULL) END,
                CASE WHEN @ExpectedTopResultRecordID_Clear = 1 THEN NULL ELSE ISNULL(@ExpectedTopResultRecordID, NULL) END,
                CASE WHEN @Notes_Clear = 1 THEN NULL ELSE ISNULL(@Notes, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwSearchScopeTestQueries] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateSearchScopeTestQuery] TO [cdp_Developer], [cdp_Integration]
    