-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Access Control Rules
-- Item: spCreateAccessControlRule
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR AccessControlRule
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateAccessControlRule]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateAccessControlRule];
GO

CREATE PROCEDURE [__mj].[spCreateAccessControlRule]
    @ID uniqueidentifier = NULL,
    @EntityID uniqueidentifier,
    @RecordID nvarchar(500),
    @GranteeType nvarchar(50),
    @GranteeID_Clear bit = 0,
    @GranteeID uniqueidentifier = NULL,
    @CanRead bit = NULL,
    @CanCreate bit = NULL,
    @CanUpdate bit = NULL,
    @CanDelete bit = NULL,
    @CanShare bit = NULL,
    @ExpiresAt_Clear bit = 0,
    @ExpiresAt datetimeoffset = NULL,
    @GrantedByUserID uniqueidentifier
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[AccessControlRule]
            (
                [ID],
                [EntityID],
                [RecordID],
                [GranteeType],
                [GranteeID],
                [CanRead],
                [CanCreate],
                [CanUpdate],
                [CanDelete],
                [CanShare],
                [ExpiresAt],
                [GrantedByUserID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @EntityID,
                @RecordID,
                @GranteeType,
                CASE WHEN @GranteeID_Clear = 1 THEN NULL ELSE ISNULL(@GranteeID, NULL) END,
                ISNULL(@CanRead, 0),
                ISNULL(@CanCreate, 0),
                ISNULL(@CanUpdate, 0),
                ISNULL(@CanDelete, 0),
                ISNULL(@CanShare, 0),
                CASE WHEN @ExpiresAt_Clear = 1 THEN NULL ELSE ISNULL(@ExpiresAt, NULL) END,
                @GrantedByUserID
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[AccessControlRule]
            (
                [EntityID],
                [RecordID],
                [GranteeType],
                [GranteeID],
                [CanRead],
                [CanCreate],
                [CanUpdate],
                [CanDelete],
                [CanShare],
                [ExpiresAt],
                [GrantedByUserID]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @EntityID,
                @RecordID,
                @GranteeType,
                CASE WHEN @GranteeID_Clear = 1 THEN NULL ELSE ISNULL(@GranteeID, NULL) END,
                ISNULL(@CanRead, 0),
                ISNULL(@CanCreate, 0),
                ISNULL(@CanUpdate, 0),
                ISNULL(@CanDelete, 0),
                ISNULL(@CanShare, 0),
                CASE WHEN @ExpiresAt_Clear = 1 THEN NULL ELSE ISNULL(@ExpiresAt, NULL) END,
                @GrantedByUserID
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwAccessControlRules] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateAccessControlRule] TO [cdp_Developer], [cdp_Integration]
    