-----------------------------------------------------------------
-- SQL Code Generation
-- Entity: MJ: Company Integration Run API Logs
-- Item: spCreateCompanyIntegrationRunAPILog
--
-- This was generated by the MemberJunction CodeGen tool.
-- This file should NOT be edited by hand.
-----------------------------------------------------------------

------------------------------------------------------------
----- CREATE PROCEDURE FOR CompanyIntegrationRunAPILog
------------------------------------------------------------
IF OBJECT_ID('[__mj].[spCreateCompanyIntegrationRunAPILog]', 'P') IS NOT NULL
    DROP PROCEDURE [__mj].[spCreateCompanyIntegrationRunAPILog];
GO

CREATE PROCEDURE [__mj].[spCreateCompanyIntegrationRunAPILog]
    @ID uniqueidentifier = NULL,
    @CompanyIntegrationRunID uniqueidentifier,
    @ExecutedAt datetimeoffset = NULL,
    @IsSuccess bit = NULL,
    @RequestMethod_Clear bit = 0,
    @RequestMethod nvarchar(12) = NULL,
    @URL_Clear bit = 0,
    @URL nvarchar(MAX) = NULL,
    @Parameters_Clear bit = 0,
    @Parameters nvarchar(MAX) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    DECLARE @InsertedRow TABLE ([ID] UNIQUEIDENTIFIER)
    
    IF @ID IS NOT NULL
    BEGIN
        -- User provided a value, use it
        INSERT INTO [__mj].[CompanyIntegrationRunAPILog]
            (
                [ID],
                [CompanyIntegrationRunID],
                [ExecutedAt],
                [IsSuccess],
                [RequestMethod],
                [URL],
                [Parameters]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @ID,
                @CompanyIntegrationRunID,
                ISNULL(@ExecutedAt, sysdatetimeoffset()),
                ISNULL(@IsSuccess, 0),
                CASE WHEN @RequestMethod_Clear = 1 THEN NULL ELSE ISNULL(@RequestMethod, NULL) END,
                CASE WHEN @URL_Clear = 1 THEN NULL ELSE ISNULL(@URL, NULL) END,
                CASE WHEN @Parameters_Clear = 1 THEN NULL ELSE ISNULL(@Parameters, NULL) END
            )
    END
    ELSE
    BEGIN
        -- No value provided, let database use its default (e.g., NEWSEQUENTIALID())
        INSERT INTO [__mj].[CompanyIntegrationRunAPILog]
            (
                [CompanyIntegrationRunID],
                [ExecutedAt],
                [IsSuccess],
                [RequestMethod],
                [URL],
                [Parameters]
            )
        OUTPUT INSERTED.[ID] INTO @InsertedRow
        VALUES
            (
                @CompanyIntegrationRunID,
                ISNULL(@ExecutedAt, sysdatetimeoffset()),
                ISNULL(@IsSuccess, 0),
                CASE WHEN @RequestMethod_Clear = 1 THEN NULL ELSE ISNULL(@RequestMethod, NULL) END,
                CASE WHEN @URL_Clear = 1 THEN NULL ELSE ISNULL(@URL, NULL) END,
                CASE WHEN @Parameters_Clear = 1 THEN NULL ELSE ISNULL(@Parameters, NULL) END
            )
    END
    -- return the new record from the base view, which might have some calculated fields
    SELECT * FROM [__mj].[vwCompanyIntegrationRunAPILogs] WHERE [ID] = (SELECT [ID] FROM @InsertedRow)
END
GO
GRANT EXECUTE ON [__mj].[spCreateCompanyIntegrationRunAPILog] TO [cdp_Developer], [cdp_Integration]
    