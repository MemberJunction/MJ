// Open App client bootstrap — regenerated by `mj app install` / `mj app remove`.
//
// `app.module.ts` statically imports this file so an installed Open App's client
// packages are pulled into the bundle (their `@RegisterClass` decorators run at
// module load). It is a tracked, committed file (NOT git-ignored) so the static
// import always resolves on a fresh checkout; `mj app install` / `mj app remove`
// overwrite it with the real per-app side-effect imports in a deployment.
//
// No Open Apps installed yet.
export {};
