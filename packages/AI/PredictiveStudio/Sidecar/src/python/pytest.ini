[pytest]
# Run from this directory so `app` (the FastAPI package) and `tests` resolve as
# top-level imports — matches the original docker/predictive-studio-sidecar layout.
testpaths = tests
addopts = -q
