/**
 * @module stories
 *
 * Barrel for the model-story layer — the prose identity a model and its components acquire at
 * promotion, and the embedding that makes reuse-by-meaning searchable. See `./model-story-tagger`
 * for the flow and `./story-context-loader` for what a story is written ABOUT.
 */

export * from './seams';
export * from './story-context-loader';
export * from './model-story-tagger';
