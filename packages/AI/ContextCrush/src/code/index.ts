/**
 * @memberjunction/context-crush/code — AST-aware code reduction (CrushCode).
 *
 * Published as a subpath so the base @memberjunction/context-crush entry point stays
 * focused on structural-JSON + prefix primitives. Independent TypeScript re-implementation
 * of the CodeCompressor concept from Headroom (Apache-2.0); see NOTICE and
 * plans/agent-token-optimization.md §0 for attribution.
 */
export * from './crush-code';
