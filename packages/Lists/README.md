# @memberjunction/lists

Pure TypeScript core for MemberJunction Lists. Owns list materialization, set algebra, delta computation, lineage tracking, refresh modes, and drop guards. Framework-agnostic — no GraphQL, HTTP, or Angular dependencies.

See [plans/lists/plan.md](../../plans/lists/plan.md) for the design.
