{
  "name": "TestComponent",
  "description": "Component accessing field not declared in dataRequirements (violates field-not-in-requirements rule)",
  "code": "function TestComponent({ utilities, savedUserSettings, onSavedUserSettingsChange }) {\n  const [data, setData] = useState([]);\n  \n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({\n        EntityName: 'Products',\n        MaxRows: 100\n      });\n      if (result.Success) {\n        setData(result.Results || []);\n      }\n    }\n    loadData();\n  }, []);\n  \n  return (\n    <div>\n      <h1>Products</h1>\n      {data.map(item => (\n        <div key={item.ID}>\n          <span>{item.Name}</span>\n          {/* Accessing 'Description' which is not in dataRequirements */}\n          <span>{item.Description}</span>\n          {/* Accessing 'Price' which is not in dataRequirements */}\n          <span>${item.Price}</span>\n        </div>\n      ))}\n    </div>\n  );\n}",
  "componentType": "interactive",
  "libraries": [],
  "dataRequirements": {
    "mode": "views",
    "entities": [
      {
        "name": "Products",
        "fields": ["ID", "Name", "Status"],
        "sortFields": ["Name"]
      }
    ]
  }
}
