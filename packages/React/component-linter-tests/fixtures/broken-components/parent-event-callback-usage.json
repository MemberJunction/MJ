{
  "name": "TestComponent",
  "description": "Parent component receives callback events but doesn't handle them (violates parent-event-callback-usage rule)",
  "code": "function TestComponent({ utilities, savedUserSettings, onSavedUserSettingsChange, ChildSelector, onItemSelected, onFilterChange }) {\n  const [data, setData] = useState([]);\n  \n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({\n        EntityName: 'Items',\n        MaxRows: 100\n      });\n      if (result.Success) {\n        setData(result.Results || []);\n      }\n    }\n    loadData();\n  }, []);\n  \n  // onItemSelected callback is received but never called\n  // onFilterChange callback is received but never called\n  \n  return (\n    <div>\n      <h1>Item Selector</h1>\n      <ChildSelector \n        items={data}\n        utilities={utilities}\n        savedUserSettings={savedUserSettings}\n        onSavedUserSettingsChange={onSavedUserSettingsChange}\n        // Not passing onSelect to child, even though parent has onItemSelected\n      />\n      <p>Selected items will appear here</p>\n    </div>\n  );\n}",
  "componentType": "interactive",
  "libraries": [],
  "componentDependencies": [
    {
      "name": "ChildSelector",
      "type": "child"
    }
  ]
}
