{
  "name": "TestComponent",
  "description": "Component accessing array elements without bounds checking (violates unsafe-array-access rule)",
  "code": "function TestComponent({ utilities, savedUserSettings, onSavedUserSettingsChange }) {\n  const [data, setData] = useState([]);\n  const [firstItem, setFirstItem] = useState(null);\n  const [lastItem, setLastItem] = useState(null);\n  \n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({\n        EntityName: 'Accounts',\n        MaxRows: 100\n      });\n      if (result.Success) {\n        const items = result.Results || [];\n        setData(items);\n        // Unsafe: accessing [0] without checking length\n        setFirstItem(items[0]);\n        // Unsafe: accessing last element without bounds check\n        setLastItem(items[items.length - 1]);\n      }\n    }\n    loadData();\n  }, []);\n  \n  return (\n    <div>\n      <h1>Accounts</h1>\n      {/* Unsafe access to nested property */}\n      <p>First: {firstItem.Name}</p>\n      <p>Last: {lastItem.Name}</p>\n    </div>\n  );\n}",
  "componentType": "interactive",
  "libraries": []
}
