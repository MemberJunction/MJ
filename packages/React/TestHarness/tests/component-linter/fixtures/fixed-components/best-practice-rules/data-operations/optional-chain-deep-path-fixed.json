{
  "name": "OptionalChainDeepPathFixed",
  "type": "generic",
  "title": "Optional Chain Deep Path - Fixed Pattern",
  "description": "Tests optional chaining on valid nested path",
  "code": "function OptionalChainDeepPathFixed({ utilities }) {\n  const [orderTotal, setOrderTotal] = useState(0);\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rq.RunQuery({\n        QueryName: 'Order Details',\n        CategoryPath: 'Sales/Reports'\n      });\n      if (result?.Success) {\n        // ✅ Accessing flat TotalAmount field that exists\n        const total = result.Results?.[0]?.TotalAmount || 0;\n        setOrderTotal(total);\n      }\n    }\n    loadData();\n  }, []);\n\n  return <div>Total: {orderTotal}</div>;\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "queries": [{
      "name": "Order Details",
      "categoryPath": "Sales/Reports",
      "fields": ["OrderID", "OrderDate", "TotalAmount", "CustomerName"]
    }]
  }
}
