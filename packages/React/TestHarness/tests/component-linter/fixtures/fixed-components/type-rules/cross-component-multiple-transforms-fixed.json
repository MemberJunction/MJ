{
  "name": "CrossComponentMultipleTransformsFixed",
  "type": "generic",
  "title": "Cross-Component Multiple Transforms - Fixed Pattern",
  "description": "Tests multiple transformations that preserve fields needed by final component",
  "code": "function CrossComponentMultipleTransformsFixed({ utilities, components }) {\n  const [chartData, setChartData] = useState([]);\n  const { SimpleChart } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Orders' });\n      if (result?.Success) {\n        // First transformation: add computed field\n        const withMonth = result.Results.map(o => ({\n          ...o,\n          Month: o.OrderDate.substring(0, 7)\n        }));\n        \n        // ✅ Second transformation: keeps TotalAmount\n        const grouped = withMonth.map(o => ({\n          Month: o.Month,\n          CustomerName: o.CustomerName,\n          TotalAmount: o.TotalAmount\n        }));\n        \n        setChartData(grouped);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Orders Chart</h3>\n      {/* Chart has access to TotalAmount */}\n      <SimpleChart data={chartData} groupBy=\"Month\" valueField=\"TotalAmount\" />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Orders",
      "fields": ["ID", "OrderDate", "CustomerName", "TotalAmount"]
    }]
  }
}
