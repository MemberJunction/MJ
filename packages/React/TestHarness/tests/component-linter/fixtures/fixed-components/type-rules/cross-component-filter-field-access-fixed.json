{
  "name": "CrossComponentFilterFieldAccessFixed",
  "type": "generic",
  "title": "Cross-Component Filter Field Access - Fixed Pattern",
  "description": "Tests filtering data preserves fields needed by downstream component",
  "code": "function CrossComponentFilterFieldAccessFixed({ utilities, components }) {\n  const [products, setProducts] = useState([]);\n  const { DataGrid } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Products' });\n      if (result?.Success) {\n        // Filter keeps all fields including Category\n        const active = result.Results.filter(p => p.IsActive);\n        setProducts(active);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Active Products</h3>\n      {/* ✅ Grid references Category field that exists */}\n      <DataGrid \n        data={products} \n        columns={[\n          {field: 'ProductName', title: 'Product'},\n          {field: 'Category', title: 'Category'}\n        ]} \n      />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Products",
      "fields": ["ID", "ProductName", "Category", "Price", "IsActive"]
    }]
  }
}
