{
  "name": "CrossComponentTransformFieldLostFixed",
  "type": "generic",
  "title": "Cross-Component Transform Field Lost - Fixed Pattern",
  "description": "Tests data transformation that preserves required fields for downstream component",
  "code": "function CrossComponentTransformFieldLostFixed({ utilities, components }) {\n  const [chartData, setChartData] = useState([]);\n  const { SimpleChart } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Orders' });\n      if (result?.Success) {\n        // ✅ Transformation includes TotalAmount field that chart needs\n        const transformed = result.Results.map(o => ({\n          OrderDate: o.OrderDate,\n          CustomerName: o.CustomerName,\n          TotalAmount: o.TotalAmount\n        }));\n        setChartData(transformed);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Orders by Date</h3>\n      {/* Chart has access to TotalAmount field */}\n      <SimpleChart data={chartData} groupBy=\"OrderDate\" valueField=\"TotalAmount\" />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Orders",
      "fields": ["ID", "OrderDate", "CustomerName", "TotalAmount", "Status"]
    }]
  }
}
