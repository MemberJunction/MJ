{
  "name": "ChartDataEmptyFieldsBroken",
  "type": "generic",
  "title": "Chart Data Empty Fields - Broken Pattern",
  "description": "Tests chart referencing fields after data transformation removes them (may fail until linter refactor)",
  "code": "function ChartDataEmptyFieldsBroken({ utilities, components }) {\n  const [chartData, setChartData] = useState([]);\n  const { SimpleChart } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Sales' });\n      if (result?.Success) {\n        // ❌ Filter removes data but chart still references fields\n        const filtered = result.Results.filter(s => s.Region === 'NonExistentRegion');\n        setChartData(filtered);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Regional Sales</h3>\n      {/* Chart may have empty data but fields are still referenced */}\n      <SimpleChart data={chartData} groupBy=\"Month\" valueField=\"Amount\" />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Sales",
      "fields": ["ID", "Month", "Amount", "Region"]
    }]
  }
}
