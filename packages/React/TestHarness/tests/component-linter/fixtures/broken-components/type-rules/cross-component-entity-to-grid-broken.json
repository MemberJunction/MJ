{
  "name": "CrossComponentEntityToGridBroken",
  "type": "generic",
  "title": "Cross-Component Entity to Grid - Broken Pattern",
  "description": "Tests entity data passed to grid with invalid field reference (may fail until linter refactor)",
  "code": "function CrossComponentEntityToGridBroken({ utilities, components }) {\n  const [members, setMembers] = useState([]);\n  const { DataGrid } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Members' });\n      if (result?.Success) {\n        setMembers(result.Results);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Members</h3>\n      {/* ❌ Grid references AccountStatus but entity has Status */}\n      <DataGrid \n        data={members} \n        columns={[\n          {field: 'FirstName', title: 'Name'},\n          {field: 'AccountStatus', title: 'Status'}\n        ]} \n      />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Members",
      "fields": ["ID", "FirstName", "LastName", "Email", "Status"]
    }]
  }
}
