{
  "name": "CrossComponentComputedFieldTypoBroken",
  "type": "generic",
  "title": "Cross-Component Computed Field Typo - Broken Pattern",
  "description": "Tests computed field with typo passed to downstream component (may fail until linter refactor)",
  "code": "function CrossComponentComputedFieldTypoBroken({ utilities, components }) {\n  const [members, setMembers] = useState([]);\n  const { DataGrid } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Members' });\n      if (result?.Success) {\n        // Adding computed field with typo\n        const enhanced = result.Results.map(m => ({\n          ...m,\n          FullNam: `${m.FirstName} ${m.LastName}` // ❌ Typo: FullNam\n        }));\n        setMembers(enhanced);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Members</h3>\n      {/* Grid expects FullName but data has FullNam */}\n      <DataGrid \n        data={members} \n        columns={[\n          {field: 'FullName', title: 'Name'},\n          {field: 'Email', title: 'Email'}\n        ]} \n      />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Members",
      "fields": ["ID", "FirstName", "LastName", "Email"]
    }]
  }
}
