{
  "name": "SpreadFieldNameConflictBroken",
  "type": "generic",
  "title": "Spread Field Name Conflict - Broken Pattern",
  "description": "Tests spread operator that creates field name conflict (may fail until linter refactor)",
  "code": "function SpreadFieldNameConflictBroken({ utilities, components }) {\n  const [orders, setOrders] = useState([]);\n  const { DataGrid } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Orders' });\n      if (result?.Success) {\n        // ❌ Spread overwrites Status with computed value\n        const enhanced = result.Results.map(o => ({\n          ...o,\n          Status: o.IsPaid ? 'Paid' : 'Unpaid' // Overwrites original Status\n        }));\n        setOrders(enhanced);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Orders</h3>\n      {/* Grid references Status but type changed from entity value */}\n      <DataGrid \n        data={orders} \n        columns={[\n          {field: 'OrderNumber', title: 'Order'},\n          {field: 'Status', title: 'Status'}\n        ]} \n      />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "entities": [{
      "name": "Orders",
      "fields": ["ID", "OrderNumber", "Status", "IsPaid", "TotalAmount"]
    }]
  }
}
