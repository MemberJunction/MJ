{
  "name": "ComponentPropTypeArrayToStringFixed",
  "type": "generic",
  "title": "Component Property Type Array to String - Fixed Pattern",
  "description": "Tests correct usage of string property with single value",
  "code": "function ComponentPropTypeArrayToStringFixed({ utilities, components }) {\n  const [selectedId, setSelectedId] = useState('id1');\n  const { RecordSelector } = components;\n\n  return (\n    <div>\n      <h3>Select Record</h3>\n      {/* ✅ selectedId expects string, passing single string value */}\n      <RecordSelector entityName=\"Members\" selectedId={selectedId} />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "components": [{
      "name": "RecordSelector",
      "properties": [
        {"name": "entityName", "type": "string", "isRequired": true},
        {"name": "selectedId", "type": "string", "isRequired": false}
      ]
    }]
  }
}
