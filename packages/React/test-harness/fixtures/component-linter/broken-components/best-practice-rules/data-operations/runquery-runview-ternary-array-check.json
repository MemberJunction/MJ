{
  "name": "TestComponent",
  "description": "Component using ternary without proper array check for Results (violates runview-runquery-result-validation rule)",
  "code": "function TestComponent({ utilities, savedUserSettings, onSavedUserSettingsChange }) {\n  const [data, setData] = useState([]);\n  \n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'Users' });\n      const items = result.Success ? result.Results : null;\n      setData(items);\n    }\n    loadData();\n  }, []);\n  \n  return (\n    <div>\n      <h1>Test Component</h1>\n      <p>Data count: {data?.length || 0}</p>\n    </div>\n  );\n}",
  "componentType": "interactive",
  "libraries": [],
  "dataRequirements": [
    {
      "type": "view",
      "entityName": "Users",
      "maxRows": 100
    }
  ]
}
