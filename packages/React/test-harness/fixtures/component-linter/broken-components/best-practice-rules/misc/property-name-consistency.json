{
  "name": "TestComponent",
  "description": "Component with inconsistent property name casing (violates property-name-consistency rule)",
  "code": "function TestComponent({ utilities, savedUserSettings, onSavedUserSettingsChange }) {\n  const [data, setData] = useState([]);\n  \n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({\n        EntityName: 'Contacts',\n        MaxRows: 100\n      });\n      if (result.Success) {\n        setData(result.Results || []);\n      }\n    }\n    loadData();\n  }, []);\n  \n  return (\n    <div>\n      <h1>Contacts</h1>\n      {data.map(item => (\n        <div key={item.ID}>\n          {/* Inconsistent casing: mixing camelCase and PascalCase */}\n          <span>{item.firstName}</span>  {/* Should be FirstName */}\n          <span>{item.LastName}</span>\n          <span>{item.emailAddress}</span>  {/* Should be EmailAddress */}\n          <span>{item.PhoneNumber}</span>\n        </div>\n      ))}\n    </div>\n  );\n}",
  "componentType": "interactive",
  "libraries": [],
  "dataRequirements": {
    "mode": "views",
    "entities": [
      {
        "name": "Contacts",
        "fields": ["ID", "FirstName", "LastName", "EmailAddress", "PhoneNumber"],
        "sortFields": ["LastName", "FirstName"]
      }
    ]
  }
}
