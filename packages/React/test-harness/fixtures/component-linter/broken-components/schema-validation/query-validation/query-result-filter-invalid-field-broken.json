{
  "name": "QueryResultFilterInvalidFieldBroken",
  "type": "generic",
  "title": "Query Result Filter Invalid Field - Broken Pattern",
  "description": "Tests linter detection of filtering query results by invalid field",
  "code": "function QueryResultFilterInvalidFieldBroken({ utilities }) {\n  const [activeModels, setActiveModels] = useState([]);\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rq.RunQuery({\n        QueryName: 'AI Model Catalog',\n        CategoryPath: 'AI/Reports'\n      });\n      if (result?.Success) {\n        // ❌ Filtering by Enabled field that doesn't exist in query\n        const active = result.Results.filter(p => p.Enabled);\n        setActiveModels(active);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Active Models</h3>\n      {activeModels.map((p, i) => (\n        <div key={i}>{p.Name} - {p.Vendor}</div>\n      ))}\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "queries": [{
      "name": "AI Model Catalog",
      "categoryPath": "AI/Reports",
      "fields": [
        {"name": "ID", "type": "uniqueidentifier"},
        {"name": "Name", "type": "nvarchar"},
        {"name": "Vendor", "type": "nvarchar"},
        {"name": "PowerRank", "type": "int"},
        {"name": "IsActive", "type": "bit"}
      ]
    }]
  }
}
