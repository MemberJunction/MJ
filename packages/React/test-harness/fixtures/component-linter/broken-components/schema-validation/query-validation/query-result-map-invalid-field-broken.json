{
  "name": "QueryResultMapInvalidFieldBroken",
  "type": "generic",
  "title": "Query Result Map Invalid Field - Broken Pattern",
  "description": "Tests linter detection of mapping query results with invalid field",
  "code": "function QueryResultMapInvalidFieldBroken({ utilities }) {\n  const [vendorNames, setVendorNames] = useState([]);\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rq.RunQuery({\n        QueryName: 'Employee Directory',\n        CategoryPath: 'HR/Reports'\n      });\n      if (result?.Success) {\n        // ❌ Mapping to Department field that doesn't exist in query\n        const names = result.Results.map(c => c.Department);\n        setVendorNames(names);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Departments</h3>\n      {vendorNames.map((name, i) => <div key={i}>{name}</div>)}\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "queries": [{
      "name": "Employee Directory",
      "categoryPath": "HR/Reports",
      "fields": [
        {"name": "EmployeeID", "type": "uniqueidentifier"},
        {"name": "FirstName", "type": "nvarchar"},
        {"name": "LastName", "type": "nvarchar"},
        {"name": "Email", "type": "nvarchar"},
        {"name": "Title", "type": "nvarchar"}
      ]
    }]
  }
}
