{
  "name": "QueryParamComputedWrongTypeBroken",
  "type": "generic",
  "title": "Query Parameter Computed Wrong Type - Broken Pattern",
  "description": "Tests linter detection of computed value with wrong type used as query parameter",
  "code": "function QueryParamComputedWrongTypeBroken({ utilities }) {\n  const [models, setModels] = useState([]);\n  const [primaryVendor] = useState('OpenAI');\n  const [secondaryVendor] = useState('Anthropic');\n  \n  // ❌ Concatenating with + operator instead of template literal\n  const vendors = primaryVendor + ',' + secondaryVendor;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rq.RunQuery({\n        QueryName: 'Models By Vendor',\n        CategoryPath: 'AI/Reports',\n        Parameters: { Vendors: vendors }\n      });\n      if (result?.Success) {\n        setModels(result.Results);\n      }\n    }\n    loadData();\n  }, [vendors]);\n\n  return (\n    <div>\n      <h3>AI Models</h3>\n      {models.map((p, i) => <div key={i}>{p.Name}</div>)}\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "queries": [{
      "name": "Models By Vendor",
      "categoryPath": "AI/Reports",
      "parameters": [
        {"name": "Vendors", "type": "nvarchar", "isRequired": true}
      ]
    }]
  }
}
