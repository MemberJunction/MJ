{
  "name": "TestComponent",
  "description": "Component not passing required props to child components (violates dependency-prop-validation rule)",
  "code": "function TestComponent({ utilities, savedUserSettings, onSavedUserSettingsChange, ChildChart }) {\n  const [data, setData] = useState([]);\n  \n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({\n        EntityName: 'Analytics',\n        MaxRows: 100\n      });\n      if (result.Success) {\n        setData(result.Results || []);\n      }\n    }\n    loadData();\n  }, []);\n  \n  return (\n    <div>\n      <h1>Analytics Dashboard</h1>\n      {/* Missing required props: utilities, savedUserSettings, onSavedUserSettingsChange */}\n      <ChildChart data={data} />\n      {/* Only passing some props */}\n      <ChildChart \n        data={data} \n        utilities={utilities}\n        // Missing savedUserSettings and onSavedUserSettingsChange\n      />\n    </div>\n  );\n}",
  "componentType": "interactive",
  "libraries": [],
  "componentDependencies": [
    {
      "name": "ChildChart",
      "type": "child"
    }
  ]
}
