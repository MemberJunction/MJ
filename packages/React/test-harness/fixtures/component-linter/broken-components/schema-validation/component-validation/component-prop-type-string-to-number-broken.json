{
  "name": "ComponentPropTypeStringToNumberBroken",
  "type": "generic",
  "title": "Component Property Type String to Number - Broken Pattern",
  "description": "Tests linter detection of passing string to number property",
  "code": "function ComponentPropTypeStringToNumberBroken({ utilities, components }) {\n  const [pageSize, setPageSize] = useState('50');\n  const { DataTable } = components;\n  const [employees, setEmployees] = useState([]);\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'MJ: Employees' });\n      if (result?.Success) {\n        setEmployees(result.Results);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Employees</h3>\n      {/* ❌ pageSize expects number, passing string */}\n      <DataTable data={employees} pageSize={pageSize} />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "components": [{
      "name": "DataTable",
      "properties": [
        {"name": "data", "type": "array", "isRequired": true},
        {"name": "pageSize", "type": "number", "isRequired": false}
      ]
    }]
  },
  "dependencies": [
    {
      "name": "DataTable",
      "location": "registry",
      "type": "table",
      "title": "Data Table",
      "description": "Displays data in a configurable table with pagination and sorting",
      "code": "",
      "functionalRequirements": "",
      "technicalDesign": "",
      "exampleUsage": "<DataTable data={employees} pageSize={25} />",
      "properties": [
        {"name": "data", "type": "array", "required": true, "description": "Array of records to display in the table"},
        {"name": "pageSize", "type": "number", "required": false, "description": "Number of rows to display per page"}
      ]
    }
  ]
}
