{
  "name": "CrossComponentFilterFieldAccessBroken",
  "type": "generic",
  "title": "Cross-Component Filter Field Access - Broken Pattern",
  "description": "Tests filtering data removes field needed by downstream component (may fail until linter refactor)",
  "code": "function CrossComponentFilterFieldAccessBroken({ utilities, components }) {\n  const [models, setModels] = useState([]);\n  const { DataGrid } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'MJ: AI Models' });\n      if (result?.Success) {\n        // Filter keeps all fields, but grid needs Category\n        const active = result.Results.filter(p => p.IsActive);\n        setModels(active);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Active Models</h3>\n      {/* ❌ Grid references Category but entity has AIModelType */}\n      <DataGrid \n        data={models} \n        columns={[\n          {field: 'Name', title: 'Model'},\n          {field: 'Category', title: 'Category'}\n        ]} \n      />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "mode": "views",
    "entities": [{
      "name": "MJ: AI Models",
      "type": "view",
      "fieldMetadata": [
        {"name": "ID", "type": "uniqueidentifier"},
        {"name": "Name", "type": "nvarchar"},
        {"name": "Description", "type": "nvarchar"},
        {"name": "Vendor", "type": "nvarchar"},
        {"name": "AIModelTypeID", "type": "uniqueidentifier"},
        {"name": "AIModelType", "type": "nvarchar"},
        {"name": "IsActive", "type": "bit"},
        {"name": "APIName", "type": "nvarchar"},
        {"name": "PowerRank", "type": "int"},
        {"name": "SpeedRank", "type": "int"},
        {"name": "CostRank", "type": "int"},
        {"name": "InputTokenLimit", "type": "int"},
        {"name": "DriverClass", "type": "nvarchar"},
        {"name": "DriverImportPath", "type": "nvarchar"},
        {"name": "__mj_CreatedAt", "type": "datetimeoffset"},
        {"name": "__mj_UpdatedAt", "type": "datetimeoffset"}
      ]
    }]
  }
}
