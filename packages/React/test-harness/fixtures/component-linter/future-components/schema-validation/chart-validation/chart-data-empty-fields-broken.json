{
  "name": "ChartDataEmptyFieldsBroken",
  "type": "generic",
  "title": "Chart Data Empty Fields - Broken Pattern",
  "description": "Tests chart referencing fields after data transformation removes them (may fail until linter refactor)",
  "code": "function ChartDataEmptyFieldsBroken({ utilities, components }) {\n  const [chartData, setChartData] = useState([]);\n  const { SimpleChart } = components;\n\n  useEffect(() => {\n    async function loadData() {\n      const result = await utilities.rv.RunView({ EntityName: 'MJ: Action Execution Logs' });\n      if (result?.Success) {\n        // ❌ Filter removes data but chart still references fields\n        const filtered = result.Results.filter(s => s.ResultCode === 'NonExistentCode');\n        setChartData(filtered);\n      }\n    }\n    loadData();\n  }, []);\n\n  return (\n    <div>\n      <h3>Execution Results</h3>\n      {/* Chart may have empty data but fields are still referenced */}\n      <SimpleChart data={chartData} groupBy=\"Action\" valueField=\"RetentionPeriod\" />\n    </div>\n  );\n}",
  "location": "embedded",
  "namespace": "test/fixtures/type-rules",
  "version": "1.0.0",
  "registry": "MJ",
  "status": "Active",
  "dataRequirements": {
    "mode": "views",
    "entities": [{
      "name": "MJ: Action Execution Logs",
      "type": "view",
      "fieldMetadata": [
        {"name": "ID", "type": "uniqueidentifier"},
        {"name": "ActionID", "type": "uniqueidentifier"},
        {"name": "Action", "type": "nvarchar"},
        {"name": "UserID", "type": "uniqueidentifier"},
        {"name": "User", "type": "nvarchar"},
        {"name": "StartedAt", "type": "datetimeoffset"},
        {"name": "EndedAt", "type": "datetimeoffset"},
        {"name": "Params", "type": "nvarchar"},
        {"name": "ResultCode", "type": "nvarchar"},
        {"name": "Message", "type": "nvarchar"},
        {"name": "RetentionPeriod", "type": "int"},
        {"name": "__mj_CreatedAt", "type": "datetimeoffset"},
        {"name": "__mj_UpdatedAt", "type": "datetimeoffset"}
      ]
    }]
  }
}
