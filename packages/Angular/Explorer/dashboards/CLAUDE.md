# Dashboards Package Development Guide

## 🚨 Page Chrome — Always Use the Shared Trio 🚨

Every new dashboard in this package must use **`<mj-page-layout>`** + **`<mj-page-header>`** + **`<mj-page-body>`** from `@memberjunction/ng-ui-components`. Do not roll bespoke headers, gradients, or sidebars — those were consolidated in the chrome migration and the per-section CSS has been deleted.

```html
<mj-page-layout>
  <mj-page-header Title="..." Icon="fa-solid fa-..." Subtitle="...">
    <div meta>     <!-- State the user can't get by glancing at the list below.
                        See the §2 [meta] decision rule in conventions doc -
                        four passing patterns: status-callout-with-variant,
                        X-of-Y filtered count, non-trivially-derived metric,
                        single hero metric. NEVER buttons. -->
      <mj-stat-badge [Count]="filteredItems.length" [Total]="items.length" Label="items" />
    </div>
    <div actions>  <!-- Secondary buttons (Refresh, Export) → the one primary
                        CTA (rightmost). Filter + view-toggle live in the
                        [toolbar] control bar (§3 concise model), NOT here. -->
      <mj-refresh-button [Loading]="isLoading" (Clicked)="loadData()" />
      <button mjButton variant="primary" size="sm" (click)="create()">+ New</button>
    </div>
    <div toolbar>  <!-- Control bar: search · Filter · view. ALL filters live
                        behind the ONE Filter button (§3 concise model) — no
                        inline quick-filter chips, no applied-filter chip row. -->
      <mj-page-search [Value]="searchTerm" (ValueChange)="onSearch($event)" />
      <mj-filter-popover Label="Filters" Icon="fa-solid fa-filter"
          [ActiveCount]="activeFilterCount" [ShowClearAll]="activeFilterCount > 0"
          (ClearAllRequested)="resetFilters()">
        <mj-filter-panel [Fields]="filterFields" [Values]="filterValues"
          (ValuesChange)="onFilterValuesChange($event)" (Reset)="resetFilters()" />
      </mj-filter-popover>
    </div>
  </mj-page-header>
  <mj-page-body>
    <!-- card grid / table / panels -->
  </mj-page-body>
</mj-page-layout>
```

> **Don't drop a plain `<mj-stat-badge [Count]="items.length" Label="items">` into `[meta]`** —
> that's the row-count anti-pattern (mirrors what the list below already shows).
> Either use the X-of-Y form above (`[Count] + [Total]` tells the user how the
> filter is narrowing the data, signal at any scale), drop `[meta]` entirely if
> there's nothing signal-bearing to show, or use a conditional status pill with
> a non-default `Variant` (the canonical good case — see App Roles' "Unsaved
> changes" pill).

**For pages with a section rail** (multi-section dashboards like AI Analytics, Knowledge Hub Configuration / Analytics / Tags / Classify): use **`<mj-left-nav>`** + **`<mj-left-nav-content>`** inside the body, and render a per-section **`<mj-page-header-interior>`** for section identity + section-specific controls. The outer chrome stays still as the user switches sections; only the inner card rebuilds. `<mj-left-nav>` supports both flat sections and trees (Testing Explorer is the canonical tree consumer).

**Full reference:**
- [/guides/DASHBOARD_BEST_PRACTICES.md#page-chrome](../../../../guides/DASHBOARD_BEST_PRACTICES.md#page-chrome) — overview + shared component list
- [/plans/explorer-chrome-conventions.md](../../../../plans/explorer-chrome-conventions.md) — the canonical rulebook with slot rules, the **§3 concise filter model** (one Filter button; no inline chips / applied-chip row), exception list
- [/plans/concise-chrome-rollout.md](../../../../plans/concise-chrome-rollout.md) — per-page checklist for migrating filter chrome to the concise model
- [/plans/chrome-slot-discipline-audit.md](../../../../plans/chrome-slot-discipline-audit.md) — Tasks A/B/C audit log; per-page record of what was changed and why
- [/plans/list-page-standardization.md](../../../../plans/list-page-standardization.md) — forward-looking proposal for standardizing the list-page control set (not yet implemented)

### Exception: dynamically-loaded sub-pages of a left-nav shell

If your component is loaded **into** another resource's left-nav shell (Admin's `admin-container` loading the explorer-settings sub-pages, Admin → Developer Tools loading the 7 inspectors, Admin → Monitoring loading SystemDiagnostics / SQL Logging, Admin → Data & Schema loading Database Designer, etc.), do NOT wrap it in the chrome trio — the parent owns the page identity.

Instead, use **`<mj-page-header-interior>`** at the top of the body (Section 10 of the conventions doc): a two-row card with `[Title]` / `[Subtitle]` inputs and `[meta]` / `[actions]` / `[toolbar]` slots — same slot conventions as `<mj-page-header>`, different visual shape. Primary row holds identity + meta + actions; toolbar row holds the control bar (search · Filter · view per the §3 concise model) / tab-nav and collapses entirely when there's nothing to render. Same shared primitives as the exterior chrome (`<mj-page-search>`, `<mj-filter-popover>` + `<mj-filter-panel>`, `<mj-refresh-button>`, `mjButton`) — one mental model, no doubled-header.

Reference implementations cover all four Admin shells (~15 sub-pages) — copy any of them. Canonical filter-card shape: `UserManagementComponent` (`packages/Angular/Explorer/explorer-settings/src/lib/user-management/`). Action-only chrome (no toolbar): the Dev Tools inspectors. Pattern X with internal tab nav: `SystemDiagnosticsComponent`, `APIKeysResourceComponent`.

---

## 🚨 CRITICAL: NotifyLoadComplete() is MANDATORY 🚨

Every resource component in this package extends `BaseResourceComponent`. You **MUST** call `this.NotifyLoadComplete()` when initialization is complete. Without it, the app loading screen hangs forever on direct URL navigation (e.g., `/app/knowledge-hub/Search`).

### For BaseDashboard Subclasses
`BaseDashboard.ngOnInit()` calls `NotifyLoadComplete()` automatically after `loadData()` completes. No action needed — just implement `initDashboard()` and `loadData()` as usual.

### For Direct BaseResourceComponent Subclasses
You MUST call `this.NotifyLoadComplete()` yourself:

```typescript
@RegisterClass(BaseResourceComponent, 'MyResource')
@Component({ ... })
export class MyResourceComponent extends BaseResourceComponent implements AfterViewInit {
    ngAfterViewInit(): void {
        this.loadMyData();
        this.NotifyLoadComplete(); // REQUIRED
    }
}
```

### Where to Place the Call
- If your init is **synchronous** or fires-and-forgets async work: call at the end of `ngOnInit()` / `ngAfterViewInit()`
- If your init **awaits** data loading: call after the await completes
- The call signals "component is rendered and ready" — it does NOT need to wait for all data to finish loading, just for the component to be interactive

### What Happens Without It
The shell component waits for `onFirstResourceLoadComplete()` before hiding the loading screen. That event is driven by `NotifyLoadComplete()`. If no resource component calls it, the user sees the loading animation indefinitely with only a "Reset" button (which clears all local data) as recovery.

## 🚨 Agent Context & Client Tools — REQUIRED FOR EVERY DASHBOARD 🚨

**This is the baseline for ALL MJ Explorer dashboards, not just Knowledge Hub.** Every `BaseResourceComponent` / `BaseDashboard` subclass MUST report its state to the agent and register the operations the agent can invoke. A surface that skips this leaves the agent — the conversational chat agent **and** the realtime co-agent — blind to what the user is doing and unable to act in it.

**Required for every resource component** (in `ngAfterViewInit()` and on every meaningful state change):
1. `this.navigationService.SetAgentContext(this, {...})` — report surface state (active selection, filters, mode, metrics). Keep it to ~15 salient fields; send deltas. This feeds the shared `AppContextSnapshot` (`@memberjunction/ai-core-plus`) — its `AdditionalContext`, plus where relevant `View` (what's on screen / selected) and `Capabilities` (the live tool/agent manifest). **One call lights the surface up for the async chat agent (system-prompt `appContext`) AND the realtime co-agent (mint-time injection + streaming).**
2. `this.navigationService.SetAgentClientTools(this, [...])` — register the surface's agent-actionable operations (tab switches, record selection, mutations, exports). These resolve through the **one unified client-tool resolver** (`ResolveClientTools` in `@memberjunction/ai-core-plus`; precedence override > session > app > static).

**Do NOT add a parallel mechanism.** `SetAgentContext`/`SetAgentClientTools` are the single surface API — they already flow to async agents and the realtime co-agent consumes the same snapshot + resolved tools. Wire them in from the start when scaffolding a new dashboard.

**Living registry of wired surfaces** (keep current as surfaces are wired — gaps are visible here):

**Knowledge Hub / AI**
| Dashboard | Context Fields | Tools |
|-----------|---------------|-------|
| Search | CurrentQuery, ResultCount, ShowFilters, MinScoreThreshold, TopResults | RunKnowledgeSearch, ClearKnowledgeSearch, ToggleSearchFilters |
| Classify (Autotagging + Tags) | **deep — see the dedicated block below** | SwitchClassifyTab, RunClassificationPipeline, SearchClassifyTags, RefreshClassifyData/Tags + (Tags) SelectTaxonomyTag, OpenTagRecord, RefreshTaxonomy |
| Analytics | **deep, mode-scoped — see the dedicated block below** | per-tab tool set (common nav/filter/refresh + Sources select + drill-down + export + co-occurrence recompute) |
| Visualize | **deep — see the dedicated block below** | SwitchVisualizationMode, OpenDrilldownRecord, CloseDrilldown, ListDrilldownRecords |
| Clusters | **deep — see the dedicated block below** | RegenerateClusters, ResetClusterAnalysis, SetClusterSourceEntity, SetClusterAlgorithm, SetClusterDimensions, OpenSavedVisualization, ListSavedVisualizations |
| Config | **deep — see the dedicated KH-Config/Pipelines/Scheduling block below** | SwitchConfigSection, ReloadConfiguration, SelectSearchScope, OpenSearchScopeRecord, LoadSearchAnalytics, LoadPermissionsAudit, FilterFullTextEntities |
| Scheduling (top-level dashboard) | **deep, mode-scoped — see the dedicated block below** | mode-scoped per tab (Dashboard/Jobs/Activity) |
| Scheduling (KH resource tab) | **deep — see the dedicated KH block below** | RefreshSchedules, FilterSchedulesByStatus, SearchSchedules, OpenScheduleRecord, OpenSchedulingApp |
| Duplicates | **deep — see the dedicated block below** | SelectEntityDocument, RunDuplicateDetection, FilterDuplicatesByEntity, SetDuplicateDisplayMode, RefreshDuplicateDetection |
| Vectors | **deep — see the dedicated block below** | RefreshVectors, SyncVectorsForEntity, SetVectorViewMode, OpenVectorEntityDocument, GoToVectorConfiguration |
| Feature Pipelines | **deep — see the dedicated KH block below** | RunFeaturePipeline, SearchFeaturePipelines, RefreshFeaturePipelines, OpenFeaturePipelineEditor |

#### KH Config / Feature Pipelines / Scheduling — deep ("hardcore") enrichment, Data-Explorer depth

These three Knowledge Hub **resource components** (each a separate `BaseResourceComponent`, naturally mode-scoped — they own their own context + tools, no `lastRegisteredToolMode` flip) were deepened to Data-Explorer depth. The pure, vitest-tested context builders + tolerant id→name→contains resolvers live **LOCAL** to each surface under `KnowledgeHub/components/<surface>/`; the shared validation/cap helpers (`validateStringParam`, `boundNameList`) come from `src/shared/agent-tool-validation.ts` (unmodified). All published name lists are **bounded** (cap 25) with a companion `*Truncated`/`*Count` flag; ID-taking Select/Open tools also accept a **name** (exact ID → exact name → partial-contains, case-insensitive) and return a tolerant "available names" error on a miss. Context is re-emitted on every meaningful state change (section switch, scope select, filter, search, run, reload).

- **Config** — [`knowledge-config-agent-context.ts`](src/KnowledgeHub/components/config/knowledge-config-agent-context.ts) (`buildKnowledgeConfigAgentContext` + `resolveConfigSection` + `resolveByIDOrName` + `buildConfigNotFoundError`). Context: ActiveSection (+label) and bounded section list, IsLoading/HasUnsavedChanges, vector-setup completeness (SetupStepsCompleted, VectorSetupComplete), provider/index/embedding/FTS counts + bounded names, search-scope set + ActiveScope **id+name**+tab, and **gated** sub-section detail (Analytics/Permissions metrics surface **only once loaded** — never fabricated zeros). Tools: SwitchConfigSection (id **or** label), ReloadConfiguration, SelectSearchScope (id **or** name), OpenSearchScopeRecord (view), LoadSearchAnalytics, LoadPermissionsAudit (both idempotent read-only aggregations), FilterFullTextEntities.
- **Feature Pipelines** — [`feature-pipelines-agent-context.ts`](src/KnowledgeHub/components/feature-pipelines/feature-pipelines-agent-context.ts) (`buildFeaturePipelinesAgentContext` + `resolvePipeline` + `buildPipelineNotFoundError`). Context: counts by status (Active/Disabled/Draft) AND by last-run outcome (Never/Completed/Failed/Running), in-flight RunningCount + names, SearchQuery + filtered count, and a bounded structured `Pipelines[]` (name · target entity · output attribute · status · last-run). Tools: RunFeaturePipeline (id **or** name — the on-demand, OutputMapping-driven Record-Set-Processing pass, gated on `OnDemandEnabled`), SearchFeaturePipelines, RefreshFeaturePipelines, OpenFeaturePipelineEditor (UI dialog — no auto-save).
- **Scheduling (KH resource tab)** — [`scheduling-agent-context.ts`](src/KnowledgeHub/components/scheduling/scheduling-agent-context.ts) (`buildSchedulingAgentContext` + `resolveScheduledJob` + `nextDueJobName` + `buildScheduleNotFoundError`). Context: counts by lifecycle status, StatusFilter + SearchQuery, RecentRunCount, NextDueJobName, and a bounded structured `Jobs[]` (name · status · cron · success rate). Tools: RefreshSchedules, FilterSchedulesByStatus, SearchSchedules, OpenScheduleRecord (id **or** name — view), OpenSchedulingApp. No-op (no context, no tools) when embedded in KH Config's Scheduling section (`HideToolbar`) — the host owns reporting.

> **KH Config/Pipelines/Scheduling safety:** 🔒 Each component carries a documented `SAFETY BOUNDARY` comment. **Config** NEVER exposes SaveConfiguration / threshold-persist, Create/Delete vector index, Create/Save/Delete scope, or SaveProviderCredential (credential write). **Feature Pipelines** exposes the idempotent on-demand RUN (no agent-supplied params, writes via the pipeline's own OutputMapping) but NEVER a tool that saves/deletes a pipeline definition. **Scheduling** NEVER exposes create/edit/save, run-now, pause/resume, or delete. Across all three, ID-taking Open tools open records/forms for **viewing** only; the agent navigates/filters/selects/opens, the user commits from the UI. Pure helpers covered by [`knowledge-config-agent-context.test.ts`](src/__tests__/knowledge-config-agent-context.test.ts) (13), [`feature-pipelines-agent-context.test.ts`](src/__tests__/feature-pipelines-agent-context.test.ts) (9), [`kh-scheduling-agent-context.test.ts`](src/__tests__/kh-scheduling-agent-context.test.ts) (11) — 33 tests.

#### Duplicates / Vectors / Classify (Autotagging + Tags) — deep ("hardcore") enrichment, Data-Explorer depth

These four AI surfaces (each a separate `BaseResourceComponent`, naturally mode-scoped — they own their own context + tools, no `lastRegisteredToolMode` flip) were deepened from baseline to Data-Explorer depth. The pure, vitest-tested context builders + tolerant id→name→contains resolvers live **LOCAL** to each surface under `AI/components/<surface>/*-agent-context.ts`; the shared validation helper (`validateStringParam`) comes from `src/shared/agent-tool-validation.ts` (unmodified). All published name lists are **bounded** (cap 25) with a companion `*Count` when truncated; ID-taking Select/Open/Filter tools also accept a **name** (exact ID → exact name → partial-contains, case-insensitive) and return a tolerant "available names" error on a miss. Context is re-emitted on every meaningful state change (detection status, group move, sync start/finish, filter, select, tab switch, reload). Each surface no-ops its emit when embedded (`HideToolbar` — the host owns reporting), preserving the inherited `this.navigationService` + `!Embedded` gating.

- **Duplicates** — [`duplicate-detection-agent-context.ts`](src/AI/components/duplicates/duplicate-detection-agent-context.ts) (`buildDuplicateAgentContext` + `resolveEntityDoc` + `resolveEntityFilter` + `buildDupeNotFoundError`). Context: DetectionStatus/Progress(+Stage while running), TotalGroupCount + per-column Pending/Approved/Rejected, SelectedEntityDoc **id+name**, DisplayMode, EntityFilter + score/date filters (surfaced only when set), HasActiveFilters, MergeEnabled, and bounded **AvailableEntities** + **AvailableEntityDocuments** lists. Tools: SelectEntityDocument (id **or** name — picks the detection target), RunDuplicateDetection (idempotent run), FilterDuplicatesByEntity (name **or** "all"), SetDuplicateDisplayMode (kanban/table), RefreshDuplicateDetection.
- **Vectors** — [`vector-management-agent-context.ts`](src/AI/components/vectors/vector-management-agent-context.ts) (`buildVectorAgentContext` + `resolveSyncRow` + `buildVectorNotFoundError`). Context: TotalVectors, EntityDocumentCount, SyncingCount, VectorDBName/Status, EmbeddingModel, PrerequisitesMet, ViewMode, a bounded per-entity breakdown (entity · doc · vector count · status) + **AvailableEntityNames**. Tools: RefreshVectors, SyncVectorsForEntity (idempotent embedding sync; id **or** entity/doc name; guards already-syncing), SetVectorViewMode (index/operations), OpenVectorEntityDocument (id **or** name — opens config panel for viewing), GoToVectorConfiguration (navigation).
- **Classify** — two registered surfaces sharing the Content* substrate: **Autotagging** ([`autotagging-agent-context.ts`](src/AI/components/autotagging/autotagging-agent-context.ts), `RegisterClass('AutotaggingPipelineResource')`) and **Tags** ([`tags-agent-context.ts`](src/AI/components/tags/tags-agent-context.ts), `RegisterClass('Tags')`). Autotagging context: ActiveTab, source/item/tag/type/run counts **with true DB totals** (TotalContentItemCount/TotalContentTagCount), PipelineStatus (idle/running/paused) + Progress(+Stage), Inbox/Health pending badges, bounded **AvailableSourceNames** + **AvailableContentTypeNames**. Tools: SwitchClassifyTab (tolerant tab validation), RunClassificationPipeline (the documented "run autotagging" happy path), SearchClassifyTags, RefreshClassifyData, ReloadClassifyRunHistory. Tags context adds the **tag library** (TagLibraryCount/FilteredTagCount + TagSearchQuery + bounded **TopTagNames**), **taxonomy** (TaxonomyNodeCount + selected node **id+name** + bounded **TaxonomyNodeNames**), and SuggestionCount. Tags tools add SelectTaxonomyTag (id **or** name — selects the tree node), OpenTagRecord (id **or** name — opens the `MJ: Tags` record for viewing), RefreshTags, RefreshTaxonomy.

> **Duplicates/Vectors/Classify safety:** 🔒 Each component carries a documented `SAFETY BOUNDARY` comment. **Duplicates** keeps group **Approve/Reject** (and drag-to-column) and record **Merge** (+ per-match approve/reject) GATED — there is NO current-selection model, so the agent would have to fabricate a group id, and merge is irreversible record consolidation that requires the human comparison-panel flow. **Vectors** keeps **DeleteEntityDocument / vector purge** and config writes (SaveEditedDocument / SaveAsEntityDocument / RegenerateTemplate / SaveScheduleSync) gated; re-vectorizing (SyncVectorsForEntity) is the documented happy path, not destructive. **Classify** keeps **Pause/Resume/Cancel** pipeline, **Confirm/Dismiss content duplicate**, source/type create-edit-delete, and (Tags) all **taxonomy mutations** (create/rename/move/merge/delete tag, promote-to-global, synonym/scope edits, suggestion approve/merge/reject) gated. RunClassificationPipeline is exposed as the surface's primary purpose, paralleling RunDuplicateDetection (gated server-side by the run lifecycle). All Open* tools open records for **viewing** only. Pure helpers covered by [`duplicate-detection-agent-context.test.ts`](src/__tests__/duplicate-detection-agent-context.test.ts) (18), [`vector-management-agent-context.test.ts`](src/__tests__/vector-management-agent-context.test.ts) (9), [`autotagging-agent-context.test.ts`](src/__tests__/autotagging-agent-context.test.ts) (10), [`tags-agent-context.test.ts`](src/__tests__/tags-agent-context.test.ts) (10) — 47 tests.

**Data Explorer** (entity grid + record detail + home)
| Surface | Context Fields | Tools |
|---------|---------------|-------|
| Data Explorer | SelectedEntityName, ViewMode, ActiveViewId, FilterText, Total/FilteredRecordCount, SelectedRecordName, DetailPanelOpen, Home: HomeViewMode/EntitySearchText/VisibleEntityCount | OpenEntityData, FilterRecords, ClearRecordFilter, SetViewMode, SelectView, OpenRecord, CreateNewRecord, NavigateToRelated |

**Admin** (🔒 **read-only / navigational tools only — no mutations exposed to the agent**)
| Surface | Context Fields | Tools (all read-only) |
|---------|---------------|-------|
| System Diagnostics | ActiveSection, PerfTab, EngineCount, memory/cache/slow-query metrics | SwitchDiagnosticsSection, SwitchPerformanceTab, RefreshDiagnostics, FilterTelemetryByCategory, SetSlowQueryThreshold |
| Users | **deep — see the dedicated explorer-settings block below** | SwitchUserStatusFilter, FilterUsersByRole (id **or name**), SearchUsers, **SelectUser** (id **or name** — view), **SortUsers** (name/email/status/type × asc/desc), **NavigateToUserRecord** (view), ClearAllFilters, RefreshUserList, ExportUsers |
| Roles | **deep — see the dedicated explorer-settings block below** | FilterRolesByType, SearchRoles, **SelectRole** (id **or name** — view + read-only permission summary), **NavigateToRoleRecord** (view), ClearRoleFilters, RefreshRoles |
| API Keys | key counts, MainTab, SelectedKeyId (never secrets) | SwitchAPIKeysTab, FilterAPIKeysByStatus, SearchAPIKeys, RefreshAPIKeyData |
| Query Browser | SelectedQueryId/Name, SearchText, SelectedCategory, QueryCount (never SQL/results) | SearchQueries, FilterQueriesByStatus, SelectQuery, RefreshQueries |

> **Admin safety rule:** Admin surfaces expose ONLY navigational / filter / search / export / read-only-diagnostic tools to the agent. **Never** wire create/edit/delete (users/roles/keys), schema mutations, GraphQL execution, query-run-with-arbitrary-params, or secret/token values into agent context or tools. Each Admin component carries a documented SAFETY BOUNDARY comment.

#### User Management / Role Management — deep ("hardcore") enrichment, Data-Explorer depth (🔒 READ-ONLY governance surfaces)

> **NOTE — different package.** Unlike the rest of the Admin table, these two surfaces live in **`packages/Angular/Explorer/explorer-settings/src/lib/user-management/` + `.../role-management/`** (NOT `dashboards/`). They are `BaseDashboard` subclasses loaded into Admin's left-nav shell, naturally mode-scoped (each owns its own context + tools, no `lastRegisteredToolMode` flip). The pure, vitest-tested context builders + tolerant id→name→contains resolvers live **LOCAL** to each surface (`user-management-agent-context.ts` / `role-management-agent-context.ts`) — they do NOT use the dashboards' shared `agent-tool-validation.ts`. All published name lists are **bounded** (cap 25) with a companion total-count when truncated; ID-taking Select/Navigate/Filter tools also accept a **name** (exact ID → exact name/email → partial-contains, case-insensitive) and return a tolerant "available names" error on a miss. Context is re-emitted on every meaningful state change (load, filter, search, select, sort). Both use the inherited `this.navigationService` (the explorer-settings `BaseResourceComponent` provides it — never re-injected).

- **User Management** — context: TotalUserCount / FilteredUserCount / ActiveUserCount / InactiveUserCount, StatusFilter, RoleFilterId **+ RoleFilterName**, SearchText, SelectedUserId **+ SelectedUserName** (display name only — never a credential), bounded **VisibleUserNames** / **VisibleUserEmails** / **AvailableRoleNames** / **VisibleColumns** (each with a companion `*Count` when capped). Tools: SwitchUserStatusFilter, FilterUsersByRole (name **or** id; empty string clears), SearchUsers, **SelectUser** (id **or** name/email — view, sets SelectedUser), **SortUsers** (name/email/status/type × asc/desc — read-only display ordering, preserved across filter changes), **NavigateToUserRecord** (id **or** name → `OpenEntityRecord('MJ: Users', …)` for viewing), ClearAllFilters, RefreshUserList, ExportUsers.
- **Role Management** — context: TotalRoleCount / FilteredRoleCount / SystemRoleCount / CustomRoleCount, TypeFilter, SearchText, SelectedRoleId **+ SelectedRoleName**, bounded **VisibleRoleNames** (+count when capped), and a **read-only permission summary** for the selected role (`SelectedRolePermissions` = EntityCount / ReadCount / CreateCount / UpdateCount / DeleteCount — **counts only**, lazily loaded from `MJ: Entity Permissions` and surfaced ONLY when a role is selected, never fabricated zeros). Tools: FilterRolesByType, SearchRoles, **SelectRole** (id **or** name — view + loads the permission summary), **NavigateToRoleRecord** (id **or** name → `OpenEntityRecord('MJ: Roles', …)` for viewing), ClearRoleFilters, RefreshRoles.

> **User/Role Management safety:** 🔒 Both carry a documented `SAFETY BOUNDARY` comment. **DELIBERATELY NOT exposed:** create/edit/delete user, toggle status, bulk enable/disable/delete, **bulk role assignment (a role GRANT)**; create/edit/delete role; and **any permission grant/revoke surface** (the role permission summary is COUNTS ONLY — never a grant control). Context exposes only counts, active filter selections, and on-screen display names/emails — **never passwords, tokens, or secrets**. All Select/Navigate tools open records / select for **viewing** only; the agent filters/searches/selects/navigates, the user commits from the UI. Pure helpers covered by [`user-management-agent-context.test.ts`](../explorer-settings/src/__tests__/user-management-agent-context.test.ts) (18) and [`role-management-agent-context.test.ts`](../explorer-settings/src/__tests__/role-management-agent-context.test.ts) (13) — 31 tests.

**Actions** — deep ("hardcore") enrichment, Data-Explorer depth (🔒 find/filter/sort/navigate/select only — **action execution NOT exposed** to the agent). The pure, vitest-tested id/name resolvers live LOCAL to the app in [`src/Actions/agent-tool-helpers.ts`](src/Actions/agent-tool-helpers.ts) (`findByIdOrError` / `findByNameOrError` / `findByIdOrNameOrError`); the shared validation/cap helpers (`validateEnumParam`, `boundNameList`) come from `src/shared/agent-tool-validation.ts` (unmodified). All published name lists are **bounded** (cap 25). ID-taking navigation tools also accept a **name** (exact ID → exact name → partial-name, case-insensitive) and return a tolerant "available names" error on a miss. The **Monitor** surface uses the **mode-scoped tool pattern** (a `lastRegisteredToolMode` guard + `syncAgentToolsForMode()` re-registering on the list↔detail flip): COMMON tools (search/filter/sort/refresh) are always present; detail-only tools appear only while an execution is selected.
| Surface | Context Fields (deep) | Tools |
|---------|-----------------------|-------|
| Overview | action counts **by status** (active/pending/disabled) + **by type** (AI-generated/custom), execution metrics (total/recent), success rate, category count, search/status/type filters, **VisibleActionNames** + **TopCategoryNames** (bounded), **SelectedActionId+Name** | SearchActions, FilterActionsByStatus/ByType, ClearAllFilters, **SortVisibleActions** (name/status/type/updated × asc/desc), OpenActionDetail (id **or name**), OpenCategoryDetail (id **or name**), OpenExecutionDetail, RefreshOverviewData |
| Explorer | view mode, sort field+direction, **SelectedCategoryId+Name** (resolves special "all"/"uncategorized"), total/filtered/category counts, search + status/type filters, **VisibleActionNames** + **AvailableCategoryNames** (bounded) | SwitchViewMode, SortActionsByField, **SetSortDirection**, SearchActions, ClearExplorerSearch, SelectCategory (id), **SelectCategoryByName**, ToggleStatus/TypeFilter, ClearExplorerFilters, OpenActionRecord (id **or name**), **NavigateToActionCategory** (related-record), RefreshExplorerData |
| Monitor (mode-scoped: **list** ↔ **detail**) | execution metrics (total/failed/running/today/this-week/avg-duration), success rate, filtered count, **VisibleActionNames** (distinct, bounded), search/result/timerange/action filters, **ToolMode**, **SelectedExecutionId + action Name + result** | **COMMON:** SearchExecutions, FilterExecutionsByResult/ByTimeRange, FilterExecutionsByAction (id **or name**), ClearMonitorFilters, RefreshMonitorData · **list:** SelectExecution · **detail:** OpenSelectedExecutionLog, **OpenSelectedExecutionAction** (related-record), ClearExecutionSelection |

> **Actions safety:** `RunAction` / any execute path is intentionally NOT exposed (side effects + agent-supplied params). The agent finds/filters/sorts/navigates/selects; the user runs from the UI dialog. Every Actions component carries a documented `SAFETY BOUNDARY` comment; the Monitor's also documents the mode-scoping. Handlers are tolerant (never throw; return a structured failure on bad input).

**Lists** — deep ("hardcore") enrichment, Data-Explorer depth. Context is built by the pure, vitest-tested helpers in [`src/Lists/lists-agent-context.ts`](src/Lists/lists-agent-context.ts) (`buildListBrowseAgentContext` / `buildListCategoriesAgentContext` / `buildListOperationsAgentContext`, plus `resolveNamedRecord` / `buildNotFoundError` / `capListNames`); the Venn region resolver stays in [`src/Lists/components/lists-operations-region-resolver.ts`](src/Lists/components/lists-operations-region-resolver.ts). All published name lists are **bounded** (cap 25) with a companion total-count when truncated. ID-taking tools also accept a **name** (exact ID → exact name → partial-name, case-insensitive) and return a tolerant "available names" error on a miss.
| Surface | Context Fields (deep) | Tools |
|---------|-----------------------|-------|
| Browse | search, view mode, list counts, **VisibleListNames** (+count when capped), sort, owner/entity/favorites filters, active-filter count | OpenList (id **or name**), SearchLists, FilterLists, ClearFilters, SetViewMode, SortBy, **CreateList** (bounded) |
| Categories | selected category **id+name**, category count, **SelectedCategoryListNames** (member lists of selection), **CategoryNodes** (name/list-count/expanded tree), **ExpandedCategoryNames** | SelectCategory (id **or name**), ExpandCategory (id **or name**), **CreateCategory** (bounded) |
| My Lists | search, view mode, list counts, **VisibleListNames** | OpenList (id **or name**), SearchLists, SetViewMode, **CreateList** (bounded) |
| Operations (Venn) | **Operands** (name/kind/entity), operand counts, max, locked entity, compose op, **AvailableEntityNames**, **AvailableRegions** (label+size), selected region label+size, **PreviewRecordNames** (sample of region records), last operation | AddListOperand (id **or name**), RemoveListOperand, FilterOperandsByEntity, PerformUnion/Intersection/SymmetricDifference, SelectVennRegion (label or operand-set), **CreateListFromRegion**/**AddRegionToList** (append-only) |

> **Lists safety:** create/append (dialog-validated) exposed; **Edit/Delete/Share/Duplicate/ToggleFavorite/ComposeAndSave NOT exposed** (destructive or user-confirm-driven). The two Venn mutation tools (CreateListFromRegion / AddRegionToList) are create-only / append-only and route through the same dialog-validated flows a user would use — they never drop records. Every Lists component carries a documented `SAFETY BOUNDARY` comment enumerating the excluded operations.

**Bulk Operations** — deep ("hardcore") enrichment, Data-Explorer depth (🔒 **dry-run preview only — real apply NOT exposed**). The two surfaces are **separate `BaseResourceComponent` hosts** (Operations / Run History), each loaded into the Bulk Operations left-nav shell — so tools are **naturally mode-scoped by component**: the Operations host only ever registers Operations tools, the Run History host only Run-History tools (no `lastRegisteredToolMode` guard needed — the component boundary does the scoping a single mode-flipping dashboard would do with `syncAgentToolsForMode`). Both hosts are thin ViewChild wrappers over the generic `@memberjunction/ng-record-process-studio` components; they read public state and call public methods only. Context is built by the pure, vitest-tested helpers in [`src/BulkOperations/bulk-operations-agent-helpers.ts`](src/BulkOperations/bulk-operations-agent-helpers.ts) (`buildStudioAgentContext` / `buildHistoryAgentContext`, `resolveRowByIDOrName` exact-ID→exact-name→partial-contains resolver, `resolveRowByID`, `capNames`); the shared validation helpers come from `src/shared/agent-tool-validation.ts` (unmodified). All published lists are **bounded** (cap 25) with a companion total-count when truncated. ID-taking tools also accept a **name** and return a tolerant "available names" error on a miss.
| Surface | Context Fields (deep) | Tools |
|---------|-----------------------|-------|
| Operations | CurrentMode (list/edit), ProcessCount, FilteredProcessCount, SearchQuery, IsRunning, **VisibleProcessNames** (+count when capped), EditingProcessID + resolved **EditingProcessName/Entity/WorkType** (when editing an existing row) | RefreshProcessList, SearchProcesses, CreateNewProcess, EditProcess (id **or name**), BackToList, **PreviewProcessChanges** (dry-run; id **or name**) |
| Run History | IsViewingRunDetail, RunCount, **RecentRuns** (ProcessName · Status · DryRun, bounded +count), **RunStatuses** (distinct), **DryRunCount/RealRunCount**, OpenRun id+Status+**ProcessName**+DryRun flag | RefreshRunHistory, ViewRunDetails (run ID **or** operation name → most-recent), **SelectRun** (alias of ViewRunDetails), BackToRunList |

> **Bulk Ops safety:** the ONLY "run" capability is `PreviewProcessChanges` (opens the runner, which ALWAYS dry-runs first and requires a manual user Apply). **No tool performs a real (non-dry-run) apply** — nothing bypasses the runner's preview → user-confirm flow, and `SaveProcess` / process-definition commits are NOT exposed (the agent may only *open* the editor via CreateNewProcess / EditProcess). Run History is a read-only audit surface — every tool there is refresh / view / select / back; nothing re-runs, applies, retries, or writes. `ViewRunDetails`/`SelectRun` resolving a run by operation name opens the most-recent matching run for *viewing* only. Both hosts carry a `🚨 SAFETY BOUNDARY` class comment enumerating these exclusions. Pure helpers covered by [`src/__tests__/bulk-operations-agent-helpers.test.ts`](src/__tests__/bulk-operations-agent-helpers.test.ts) (27 tests).

### Phase-5 full rollout — remaining apps (all wired with context + tools)

Every app below publishes context (re-emitted on state change) + registers tools via the inherited `this.navigationService` (never re-inject it — that shadows the base and breaks the build). Per-surface specifics live in each component's `SAFETY BOUNDARY` comment; this is the index.

| App | Context (highlights) | Safe tools | 🔒 Excluded / gated |
|-----|----------------------|------------|---------------------|
| **Communication** | **mode-scoped, Data-Explorer depth** — see the dedicated block below | SwitchCommunicationTab, RefreshCommunicationData + per-tab Filter/Search/Select/Open suite | **SendMessage / compose**, template/provider edit, test-send |
| **Integration** | **deep, naturally-separate surfaces — see the dedicated block below** | per-surface tool set (SwitchIntegrationSurface + RefreshIntegrationData everywhere; surface Select/Search/Open-record/Filter suite) | **RunSync / StartSync / RunNow** (live external sync), edit mappings/schedules/creds, create/delete connections |
| **Scheduling** | **mode-scoped, Data-Explorer depth** — see the dedicated block below | per-tab tool set (common SwitchSchedulingTab/RefreshSchedulingData/GetJobDetails + Jobs filter/search/select + Activity filter/search/time-range) | pause/resume (updateJobStatus), delete, create-job, releaseLock |
| **Testing** | **mode-scoped, Data-Explorer depth** — see the dedicated block below | per-tab tool set (common nav/refresh/time-range/lookup + Runs filter/search/**SelectTestRun** + Analytics window + Review **SubmitTestFeedback**) | **StartNewTest / RerunTest** (execute) on every tab |
| **EntityAdmin** | **deep, Data-Explorer depth** — total/filtered entity counts + HasActiveFilters, active SchemaFilter/SearchText/StatusFilter, selected entity **id+registered name+DISPLAY name**+schema+description+field-count + bounded **RelatedEntities** (name·relationship type) + count, filter-panel state, bounded **AvailableEntities** (DISPLAY names) +count, **SchemaGroups** (schema·entity-count) | SelectEntity (id **or** registered/DISPLAY name — e.g. "AI Models" not "MJ: AI Models"), ToggleFilterPanel, ClearEntitySelection, RefreshERD, **FilterEntities** (schema + search; empty clears, neither resets), **NavigateToEntityRecord** (id **or** name → dynamic record view, read-only) | any entity-metadata edit/create/delete, schema mutation |
| **DashboardBrowser** | mode, selected dashboard, counts, category, view mode | SearchDashboards, FilterByCategory, OpenDashboard, SwitchViewMode, RefreshDashboardList, BackToList | create/delete/save/share/move |
| **MCP** | **deep, mode-scoped, Data-Explorer depth** — see the dedicated block below | per-tab tool set (common SwitchMCPTab/SearchMCPTools/RefreshMCPData/ClearMCPFilters + Servers/Connections/Tools/Logs select·open·filter·sort suite) | **create/delete servers+connections**, **MCP tool EXECUTION / test-with-params**, credential/token/OAuth values |
| **Form Builder / Component Studio** | **deep, Data-Explorer depth — see the dedicated block below** | **deepened canvas-edit suite** (19 reversible canvas tools + wholesale UpdateForm) | Save/Publish/persist, delete-whole-form, override-activation |
| **Home** | **deep, Data-Explorer depth** — app counts + bounded **AvailableApps**, pin counts + bounded **PinnedItems** + **PinGroupNames**, **UnreadNotifications** + bounded **NotificationTitles**, recent count + bounded structured **RecentItems** (name·resource-type), edit + add-panel + sidebar state, add-panel search query (all name lists capped 25 with companion `*Count` when truncated) | OpenApp (name, exact/partial), OpenPin (display name, exact/partial), **SearchPins** (read-only — returns matches), **OpenRecent** (display name → recent nav), SearchAddPinPanel, OpenAddPinPanel/Close, ToggleSidebar, TogglePinEditMode | pin create/delete/rename, group mutation, reorder |
| **RealtimeRecordings** (🔒 read-only playback) | **deep, Data-Explorer depth** — session count + **FilteredSessionCount**, search/sort state (SortField/SortDirection), selected session (agent/convo/media/duration/**id**), turn count + detail-loading, bounded **VisibleSessionNames** ("Agent — Conversation" labels) + structured **VisibleSessions** (id·agent·convo·media·duration, +truncation flag) | SelectSession (id **or** name), **PlayRecording** (alias — opens + mounts the synced audio+transcript player), DeselectSession, RefreshSessionList, SearchSessions (updates the visible list), **SortSessions** (date/agent/duration × asc/desc) | **mutate/delete/export** (no such tool exists or may be added) |
| **VersionHistory** | **deep — see the dedicated block below** | Restore: FilterRestoresByStatus, RefreshRestoreHistory, GetRestoreStats, **SelectRestore** · Graph: SelectEntityForDependencyView (id **or name**), FilterEntitiesBySchema, SearchEntities | **Restore/rollback**, label mutation, schema edits |
| **ApplicationRoles** (🔒 deep, READ-ONLY governance) | app-group + assignment counts, unsaved flag, expanded groups, bounded **Applications** summary (name·role-count·expanded), selected app **id+name** + bounded **SelectedApplicationRoles**, AvailableRoleCount (CanAccess/CanAdmin flags NEVER streamed) | ToggleApplicationGroup (id **or name**), GetRoleCountForApplication (id **or name** → count + role names), **SelectApplication** (id **or name** — inspect roles, view-only), RefreshApplicationRoleData, **ExportApplicationRoles** (CSV of the on-screen matrix) | toggle-access/admin, add/remove role, save/discard |
| **Archiving** | **deep — see the dedicated block below** | Config: RefreshArchiveConfig · Runs: FilterArchiveRunsByStatus, RefreshArchiveRunHistory, ViewArchiveRunDetail (id **or name**), **SelectRun** | **run archive / purge**, modify policy, cancel/retry run |
| **DatabaseDesigner** (🔒 read-only schema browse) | **deep, Data-Explorer depth** — total + **FilteredEntityCount**, HasSearch/HasActiveFilters, active SearchText + SchemaFilter, selected entity **id + registered name + DISPLAY name** + schema + table + field-count + bounded **SelectedEntityFields** (name·type·nullable) + **RelatedEntities** (name·relationship type) + count, modify-panel state, bounded **AvailableEntities** (DISPLAY names, e.g. "AI Models" not "MJ: AI Models") +count, **SchemaGroups** (schema·entity-count) | SearchEntities, SelectEntity (id **or** registered/DISPLAY name → view panel), **FilterBySchema** (empty clears; rejects unknown schema), RefreshEntityList, **NavigateToEntityRecord** (id **or** name → `MJ: Entities` record, view) | **all schema mutation** — create/modify/add-field/delete/apply/pipeline |
| **Credentials** (🔒 metadata-only) | active tab, credential/type counts (never secrets) | SwitchCredentialsTab, RefreshCredentialCounts | secret values, create/edit/delete/reveal, type/category mutation |
| **Permissions** (🔒 deep, READ-ONLY governance — 3 surfaces) | **deep — see the dedicated block below** | Resource Access: LookupResourcePermissions (domain id **or name**), ChangeDomain (id **or name**), ListResourceTypesForDomain, SearchResourceGrantees · User Access: SelectUserForPermissionReview (id **or name/email**), SearchAccessibleUsers, TogglePermissionDomainGroup · Audit Log: RunAuditTimelineQuery (domain + user resolved by id/name/email), ResetAuditFilters, SearchAuditEntries | **grant/revoke**, domain mutation, impersonation, audit-record edit, bulk-export effective permissions |
| **DevTools** | **deep, per-inspector (see below)** | EventMonitor (pause/resume/clear/filter-by-type/-component/clear-filters/sort), ClassRegistry (search/filter-by-base/clear), LazyModules (search/refresh), Layout (inspect/refresh) | — |

> **DevTools safety tiers:** EventMonitor / ClassRegistry / LazyModules / Layout = safe (context + tools). **AppState / SettingsExplorer = metadata-only** (counts/keys, **never values** — may hold tokens/secrets; no value-returning tools). **GraphQL Console = intentionally UNWIRED** (no context, no tools — arbitrary query execution is an injection vector).

### Permissions — deep ("hardcore") enrichment, Data-Explorer depth (🔒 READ-ONLY governance, 3 surfaces)

The Permissions admin application's three surfaces (Resource Access, User Access, Audit Log) are **separate `BaseResourceComponent`s** (each a tab in the Permissions app), so they're **naturally mode-scoped** — each owns its own deep context + tools (no `lastRegisteredToolMode` flip). The pure, vitest-tested context builders + the tolerant id→name→contains resolver (`resolvePermissionsCandidate` / `buildPermissionsNotFoundError` / `capPermissionsList`) live **LOCAL** to the app in [`src/Permissions/permissions-agent-context.ts`](src/Permissions/permissions-agent-context.ts); the shared validation helper (`validateStringParam`) comes from `src/shared/agent-tool-validation.ts` (unmodified). All published name lists are **bounded** (cap 25) with a companion `*Count` / `*Truncated` flag when over; ID/name-taking tools accept either an **ID or a display name** (and the user surfaces also an **email**), resolving exact-ID → exact-name → partial-contains, and return a tolerant "available names" error on a miss. Each component keeps the inherited `this.navigationService` + `super.ngOnInit()`/`super.ngOnDestroy()` + `NotifyLoadComplete()`. Context is re-emitted on every meaningful state change (domain/user select, lookup completion, query run, filter reset, group toggle).

- **Resource Access** ([resource-access-resource.component.ts](src/Permissions/resource-access-resource.component.ts)) — context: SelectedDomainName, bounded **AvailableDomains** (+count), supported **ResourceTypes** (+count), ResourceType/ResourceId inputs, LastQueryLabel, GranteeCount + bounded **GranteeNames** (display names, `GranteeNamesTruncated` when over), IsLoading. Tools: LookupResourcePermissions (domain id **or name**), ChangeDomain (id **or name**), ListResourceTypesForDomain (id **or name**), SearchResourceGrantees (substring over the last lookup's grantees).
- **User Access** ([user-access-resource.component.ts](src/Permissions/user-access-resource.component.ts)) — context: SelectedUserId + **SelectedUserName** (display name only, NEVER a credential), bounded **SelectedUserRoles**, **AccessByDomain** (domain · accessible-resource count · expanded · bounded resource names), TotalAccessibleResourceCount, **ExpandedDomains**, bounded **AvailableUsers** (+count). Tools: SelectUserForPermissionReview (id **or name/email**), SearchAccessibleUsers (name/email substring), TogglePermissionDomainGroup.
- **Audit Log** ([audit-log-resource.component.ts](src/Permissions/audit-log-resource.component.ts)) — context: DomainFilter, UserFilter + resolved **UserFilterName**, StartDate/EndDate, **HasActiveFilters**, EntryCount + bounded **RecentEntries** (changed-by-user · domain · change-type — no diff bodies, `RecentEntriesTruncated` when over), bounded **AvailableDomains** / **AvailableUsers** (+counts), IsLoading/HasRunQuery. Tools: RunAuditTimelineQuery (view-only filters; domain + user resolved by id/name/email; dates validated `YYYY-MM-DD` by the pre-existing `parseAuditFilterParams`), ResetAuditFilters, SearchAuditEntries (substring over loaded entries).

> **Permissions safety:** 🔒 all three surfaces are security-sensitive governance reports — every exposed tool is **lookup / filter / select / search** only. **DELIBERATELY NOT exposed:** grant/revoke any access, mutate any permission domain, impersonate a user, create/update/delete an audit record, or bulk-export effective permissions. Context exposes only display names, counts, active filter selections, and **bounded samples** of grantee / resource / entry names — never passwords, tokens, credentials, or change-diff bodies. Each component carries a documented `🚨 SAFETY BOUNDARY` comment; the file header of `permissions-agent-context.ts` restates it. Pure helpers covered by [`src/__tests__/permissions-agent-context.test.ts`](src/__tests__/permissions-agent-context.test.ts) (15, incl. a no-mutation/no-secret assertion) + the pre-existing [`permissions-agent-helpers.test.ts`](src/__tests__/permissions-agent-helpers.test.ts) (9 — audit filter parsing). The Application Roles deep helpers (context + id/name resolver + CSV export) are covered by [`src/__tests__/application-roles-agent-context.test.ts`](src/__tests__/application-roles-agent-context.test.ts) (13, incl. a no-permission-flag-leak assertion).

### Archiving — deep ("hardcore") enrichment, Data-Explorer depth (🔒 READ-ONLY)

The two Archiving surfaces are **separate `BaseResourceComponent`s** (each a thin wrapper over a reusable `@memberjunction/ng-archive-manager` component), so they're **naturally mode-scoped** — each owns its own deep context + tools (no `lastRegisteredToolMode` flip). The pure, vitest-tested context builders + the tolerant id→name→contains run resolver live in [`src/Archiving/archive-agent-context.ts`](src/Archiving/archive-agent-context.ts) (`buildArchiveConfigAgentContext` / `buildArchiveRunsAgentContext`, `computeArchiveRunStatusCounts` / `filterArchiveRunsByStatus`, `resolveArchiveRun`, `capArchiveNames`, `isValidArchiveRunStatusFilter`); the shared validation helpers come from `src/shared/agent-tool-validation.ts` (unmodified). All published name lists are **bounded** (cap 25, `ARCHIVE_NAME_LIST_CAP`) with a companion `*Count` when truncated; the selected run is reported by **id AND name AND status**; ID-taking run tools also accept a **name** (exact ID → exact configuration/policy name → name-contains, case-insensitive) and return a tolerant "available runs" error on a miss. Context is re-emitted on every meaningful state change (load, filter, refresh, run-detail open). The Config wrapper owns its cross-policy counts/names by querying the archive entities directly (the inner admin only loads the selected policy's entities).

| Surface | Context Fields (deep) | Tools |
|---------|-----------------------|-------|
| Config | PolicyCount, ActivePolicyCount, EntitiesUnderArchive, bounded **PolicyNames** + **ArchivedEntityNames** (de-duplicated, +`*Count` when capped), IsLoading | RefreshArchiveConfig |
| Runs | per-status counts (Total/Successful/Failed/Running), StatusFilter, FilteredRunCount, SelectedRun **id+name+status**, bounded **RecentRuns** (id·name·status, +RecentRunCount when capped) | FilterArchiveRunsByStatus, RefreshArchiveRunHistory, ViewArchiveRunDetail (id **or** configuration/policy name), **SelectRun** (alias) |

> **Archiving safety:** 🔒 both carry a documented `SAFETY BOUNDARY` comment. **DELIBERATELY NOT exposed** (irreversible / mutating): **RunArchiveNow / execute**, **PurgeArchivedData**, **CancelRunningArchive**, **RetryFailedArchiveRun**, **ModifyArchivePolicy** and any policy/entity create/edit/delete or save path. ViewArchiveRunDetail / SelectRun open a run's detail drawer for **viewing** only; the agent reads counts/names, filters, refreshes, and inspects — a human commits any real archive action from the UI. **No `DryRun` field is published** — `MJ: Archive Runs` has no dry-run column (that concept lives in the Record-Set-Processing substrate, not archiving), so there is no honest data source for it (documented in the context file alongside the same note for the absent `ScheduledJobCount`/`NextScheduledRun`). Pure helpers covered by [`src/__tests__/archive-agent-context.test.ts`](src/__tests__/archive-agent-context.test.ts) (18 tests).

### VersionHistory — deep ("hardcore") enrichment, Data-Explorer depth (🔒 Restore is READ-ONLY; Graph is browse-only)

The two VersionHistory surfaces are **separate `BaseResourceComponent`s** (Restore history + Dependency Graph), **naturally mode-scoped** — each owns its own deep context + tools. The pure, vitest-tested context builders + resolvers live in [`src/VersionHistory/version-history-restore-agent-context.ts`](src/VersionHistory/version-history-restore-agent-context.ts) (`buildVersionHistoryRestoreAgentContext`, `resolveRestore`, `isValidRestoreStatusFilter`) and [`src/VersionHistory/version-history-graph-agent-context.ts`](src/VersionHistory/version-history-graph-agent-context.ts) (`buildVersionHistoryGraphAgentContext`, `resolveGraphEntity`); the shared `validateStringParam` comes from `src/shared/agent-tool-validation.ts` (unmodified). All published name lists are **bounded** (cap 25) with a companion `*Count` when truncated; selected items are reported by **id AND name**; ID-taking Select tools also accept a **name** (exact ID → exact name → name-contains, case-insensitive) and return a tolerant "available …" error on a miss. Context is re-emitted on every meaningful state change (load, filter, search, select).

| Surface | Context Fields (deep) | Tools |
|---------|-----------------------|-------|
| Restore | Total/Successful/Failed/Partial restore counts, StatusFilter, FilteredRestoreCount, SelectedRestore **id+name**, bounded **RecentRestores** (id·name·status, +RecentRestoreCount when capped) | FilterRestoresByStatus, RefreshRestoreHistory, GetRestoreStats, **SelectRestore** (id **or** name — expands a history row for viewing) |
| Graph | SelectedEntity **id+name** + its **dependency summary** (schema · ReferencedByCount · DependsOnCount · bounded neighbour names, surfaced ONLY when an entity is selected — never fabricated zeros), TotalEntities, EntitiesWithDependents, TotalRelationships, SearchText, SchemaFilter, VisibleEntityCount, bounded **VisibleEntities** + **AvailableSchemas** | SelectEntityForDependencyView (id **or** name), FilterEntitiesBySchema, SearchEntities |

> **VersionHistory safety:** 🔒 both carry a documented `SAFETY BOUNDARY` comment. **Restore** — the actual **restore / rollback** operation is DESTRUCTIVE (overwrites live records from a historical version) and is **NEVER exposed**, nor is any **label mutation or deletion**; SelectRestore only EXPANDS a history row for inspection. **Graph** is a pure browse/filter/inspect view over entity-relationship metadata — **NO mutation of any kind** (no schema edits, no entity changes, no restore, no deletion); every tool only changes what the user is LOOKING at. Pure helpers covered by [`src/__tests__/version-history-agent-context.test.ts`](src/__tests__/version-history-agent-context.test.ts) (20 tests).

#### DevTools inspectors — deep ("hardcore") enrichment, Data-Explorer depth (7 inspectors, 3 safety tiers)

The 7 Developer-Tools inspectors (each a separate `BaseResourceComponent`, naturally mode-scoped — they own their own context + tools, no `lastRegisteredToolMode` flip) were deepened from baseline to Data-Explorer depth. The pure, vitest-tested context builders live **LOCAL** to DevTools in [`src/DevTools/dev-tools-agent-context.ts`](src/DevTools/dev-tools-agent-context.ts) (one `build*AgentContext` per inspector + the local `boundDevToolsNames` / `DEV_TOOLS_NAME_LIST_CAP` cap helper — kept LOCAL so DevTools has zero cross-package coupling). The shared validation helpers (`validateStringParam`, `validateEnumParam`) come from `src/shared/agent-tool-validation.ts` (**unmodified**). All published name lists are **bounded** (cap 25) with a companion `*Count`/`*Total` field only when truncated. Context is re-emitted on every meaningful state change (capture pause/clear, filter, search, sort, section switch, refresh, live rate tick). All inspectors use the inherited `this.navigationService` (never re-injected).

| Inspector | Tier | Context Fields (deep) | Tools |
|-----------|------|-----------------------|-------|
| **Event Monitor** | 🟢 safe | EventCount, BufferedCount, FilteredCount, EventsPerSecond, Paused, text/type/component/code filters + HasActiveFilters, sort field+dir, bounded **KnownEventTypes** / **KnownComponents** / **KnownEventCodes** (+count when capped), bounded **RecentEvents** (Type · Component · redacted payload-shape Summary — never raw values) | PauseEventMonitor, ResumeEventMonitor, ClearEventLog, FilterEventsByType, **FilterEventsByComponent**, **ClearEventFilters**, SetEventMonitorSort |
| **Class Registry** | 🟢 safe | TotalClassCount, BaseClassCount, OverrideCount, SearchTerm + HasActiveSearch, FilterByBase, VisibleGroupCount, bounded **VisibleBaseClasses** (+count when capped) | SearchClassRegistry, FilterByBaseClass (exact name), ClearClassRegistryFilters |
| **Lazy Modules** | 🟢 safe | Available, Total/Loaded/Pending modules, Filter, SearchQuery + HasActiveSearch, VisibleModuleCount, bounded **VisibleModules** labels (+count when capped) | SearchLazyModules, RefreshModuleStatus *(Force Load stays user-only — preload is a user action)* |
| **Layout Inspector** | 🟢 safe | SelectedElement(+label), ElementCount, **AvailableSections** (section ids), SnapshotSize | RefreshLayoutSnapshot, InspectElement (switch section) |
| **App State Inspector** | 🟠 **metadata-only** | StateSize, KeyCount, ActiveSection(+label), **AvailableSections**, bounded **TopLevelKeys** (structural field names like "Email"/"Roles" — 🔒 **never their values**) | **none** (no value-returning op) |
| **Settings Explorer** | 🟠 **metadata-only** | SettingCount, User/InstanceSettingCount, Scope, SearchTerm + HasActiveSearch, FilteredCount, bounded **SettingKeys** (identifiers like "mj.formBuilder.…" — 🔒 **never the `.rawValue`**; +total when capped) | **none** (no value-returning op) |
| **GraphQL Console** | 🔴 **UNWIRED** | — (no context) | — (no tools) |

> **DevTools safety (per-tier):** 🔒 Each inspector carries a documented `SAFETY BOUNDARY` class comment + the shared file-level boundary in `dev-tools-agent-context.ts`. **Safe tier** (EventMonitor/ClassRegistry/LazyModules/Layout) reports only counts + the user's own filter/search/sort/selection + bounded NON-secret NAMES (event types, class names, module labels, section ids); EventMonitor's RecentEvents `Summary` is the component's already-redacted payload-shape preview (keys/length), never raw arg values. **Metadata-only tier** (AppState/Settings) exposes counts + section/scope labels + **KEY NAMES** (state field identifiers / setting keys are structural identifiers, NOT secrets) but **NEVER the underlying VALUES** (which may hold auth tokens, provider URLs, API keys, PII), and registers **no value-returning tools**. **GraphQL Console stays intentionally UNWIRED** — arbitrary query execution is an injection vector, so it has no agent context and no tools. Pure helpers covered by [`dev-tools-agent-context.test.ts`](src/__tests__/dev-tools-agent-context.test.ts) (19 tests, including the closed-shape 🔒 assertions that pin AppState/Settings to metadata-only keys).

### Communication — deep ("hardcore") enrichment + the mode-scoped tool pattern

The Communication dashboard goes to Data-Explorer depth. The active tab decides which deep context slice is published AND which surface-scoped tools are registered — same `lastRegisteredToolMode` + `syncAgentToolsForMode()` guard as Data Explorer, here keyed on the tab (`monitor`/`logs`/`providers`/`templates`/`runs`/`settings`). The COMMON tools (`SwitchCommunicationTab`, `RefreshCommunicationData`) are always present; the surface tools swap on every tab flip, so a logs-only tool is never visible while Providers is open. Context is built by the pure, vitest-tested helpers in [`src/Communication/communication-agent-context.ts`](src/Communication/communication-agent-context.ts) (`buildCommunicationAgentContext` + per-surface shapers, `resolveCommunicationItem` id→name→contains resolver, `capCommunicationList`); the dashboard harvests only the ACTIVE, mounted child's state (`{ Surface: 'none' }` when unmounted — never fabricated zeros). All published lists are **bounded** (cap 25) with a companion total-count when truncated; the selected row is reported by **id AND name**; ID-taking Select/Open tools also accept a **name** and return a tolerant error on a miss.

| Surface | Context Fields (deep) | Tools (read-only) |
|---------|-----------------------|-------------------|
| Monitor | TotalSent, DeliveryRate, Pending, Failed, ProviderHealthCount + **ProviderHealthNames**, **ChannelNames**, **RecentActivity** | *(read-only — common tools only)* |
| Logs | LogCount, FilteredLogCount, LogStatusFilter, LogSearchText, SelectedLog id+name, **VisibleLogs** (+count when capped) | FilterLogsBy (status), SearchLogs, SelectLog (id **or** display value), OpenLog (selected/by ref) |
| Providers | ProviderCount, ActiveProviderCount, SelectedProvider id+name, **ProviderNames** | SelectProvider (name **or** id), OpenProvider (selected/by ref — view, not edit) |
| Templates | TemplateCount, FilteredTemplateCount, TemplateCategoryFilter, TemplateSearchText, SelectedTemplate id+name, **AvailableCategories**, **VisibleTemplates** | FilterTemplatesByCategory, SearchTemplates, SelectTemplate (name **or** id), OpenTemplate (selected/by ref — view, not edit) |
| Runs | RunCount, ActiveRunCount, CompletedRunCount, RunSuccessRate, SelectedRun id+name, **VisibleRuns** | SelectRun (id **or** label), OpenRun (selected/by ref) |

> **Communication safety:** all surface tools are **read / filter / search / select / navigate** only. `Open*` opens the record in a tab for viewing (it does NOT edit). **Intentionally NOT exposed:** message **send / compose** (the new-message resource / `onSend` — irreversible outbound side effect), **template / provider create-edit-delete**, and any **test-send / "send sample"**. Context exposes only aggregate counts, active filters/search, and visible **display** names — never message bodies, recipient lists, provider credentials, or template content. The `SAFETY BOUNDARY` comment in `communication-dashboard.component.ts` enumerates the exclusions. Pure helpers are covered by [`src/__tests__/communication-agent-context.test.ts`](src/__tests__/communication-agent-context.test.ts) (21 tests).

### Integration — deep ("hardcore") enrichment + the naturally-separate-surface pattern

The Integration dashboard goes to Data-Explorer depth. **It does NOT use the mode-flip pattern** (`lastRegisteredToolMode` / `syncAgentToolsForMode()`): the four surfaces (Overview, Connections, Activity, Schedules) are **separate `BaseResourceComponent`s** — one mounted component per nav item, not tabs in a single host — so each is *intrinsically* mode-scoped. Each surface publishes its own deep context slice and registers its own tool set on `ngAfterViewInit`, re-emitting context on every meaningful state change (load, selection, filter, search). This is the **Lists pattern** (each separate surface owns its context + tools), not the Data-Explorer / Communication / Testing flip. Context is built by the pure, vitest-tested helpers in [`src/Integration/integration-agent-context.ts`](src/Integration/integration-agent-context.ts) (`buildOverviewAgentContext` / `buildConnectionsAgentContext` / `buildActivityAgentContext` / `buildSchedulesAgentContext` + the shared KPI strip, `resolveIntegrationSurface` for nav, `resolveIntegrationRecord` id→name→contains resolver, `buildIntegrationNotFoundError`, `capIntegrationNames`). All published lists are **bounded** (cap 25) with a companion total-count when truncated; selected rows are reported by **id AND name**; ID-taking Select/Open tools also accept a **name** (exact ID → exact name → partial-name) and return a tolerant "available names" error on a miss. Every surface publishes the shared **KPI strip** (TotalIntegrations, ActiveSyncs, RecordsSyncedToday, SyncErrorRate, PipelineCount, ScheduledSyncCount) plus its deep slice.

| Surface | Context Fields (deep) | Tools (read-only) |
|---------|-----------------------|-------------------|
| Overview | KPI strip + SuccessRate, health breakdown (Healthy/Warning/Error/Inactive), AverageSyncDurationMs, **VisibleIntegrationNames** (+count), ActivityFeedCount, **RecentActivity** (integration·status·when) | SwitchIntegrationSurface, OpenIntegrationRecord (id **or** name), RefreshIntegrationData |
| Connections | KPI strip + ConnectionCount, ActiveConnectionCount, **VisibleConnectionNames** (+count), SelectedConnection id+name, (when selected) SelectedEntityMapCount + DetailSearchTerm | SwitchIntegrationSurface, SelectIntegrationConnection (id **or** name), SearchConnectionEntityMaps, OpenIntegrationRecord (id **or** name — view), CloseConnectionDetail, RefreshIntegrationData |
| Activity | KPI strip + StatusFilter, DateFilter, IntegrationFilter (name), SearchQuery, Filtered/Total run counts, Successful/Failed counts, TotalRecordsProcessed, **VisibleRuns** (id+label+status+records+when, capped), SelectedRun id+name, **AvailableIntegrationNames** | SwitchIntegrationSurface, FilterActivityByStatus / ByTimeRange / ByIntegration (id **or** name **or** "all"), SearchActivity, ClearActivityFilters, SelectActivityRun (id **or** label — read-only detail panel), OpenActivityRunRecord (id **or** label — view), RefreshIntegrationData |
| Schedules | KPI strip + ScheduleCount, Enabled/Locked counts, cadence breakdown (Interval/Cron/Manual), **VisibleSchedules** (name·next-run, type, enabled, locked, capped) | SwitchIntegrationSurface, OpenScheduleRecord (id **or** name — view), RefreshIntegrationData |

> **Integration safety:** 🔒 **`RunSync` triggers a LIVE external data sync against a third-party system — it is NEVER exposed, nor is any sync-trigger** (Connections `RunSync`/`StartSyncWithDirection`, Schedules `RunNow`, Overview `RunSync`). Also intentionally NOT exposed: **edit-mapping** (CreateEntityMap / CycleSyncDirection / ToggleEntityMapEnabled / AutoMap / CreateTables / FindAndAddNewTables), **update-schedule** (SaveSchedule / ToggleScheduleEnabled / SetScheduleType / SetInterval / SetCronExpression — which write the linked ScheduledJob), **change-credentials**, and **create/delete connections**. All exposed tools are **read / filter / search / select / navigate** only. `Open*Record` opens the record in a tab for **viewing** (not editing); `SelectActivityRun` opens the read-only detail panel; `SelectIntegrationConnection` opens the entity-map detail view (read-only). Context exposes only aggregate counts, active filters/search, visible **display** names, and run/schedule summaries — never credentials, watermark secrets, or field-mapping internals. Each Integration component carries a documented `SAFETY BOUNDARY` comment enumerating the exclusions. Pure helpers are covered by [`src/__tests__/integration-agent-context.test.ts`](src/__tests__/integration-agent-context.test.ts) (22 tests).

### Testing — deep ("hardcore") enrichment + the mode-scoped tool pattern

The Testing dashboard goes to Data-Explorer depth. The active tab (`dashboard`/`runs`/`analytics`/`review`) decides which deep context slice is published AND which surface-scoped tools are registered — same `lastRegisteredToolMode` + `syncAgentToolsForMode()` guard as Data Explorer. COMMON tools (`SwitchTestingTab`, `RefreshTestingData`, `FilterTestsByTimeRange`, `GetTestRunDetails`) are always present; the per-tab tools swap on every tab flip, so a Runs-only `SelectTestRun` is never visible while Analytics is open. The always-on KPI slice (active-run count + names, total/passed/failed/skipped, active-test count, pass rate + trend, avg duration, cost, pending-review) is published on every tab; the ACTIVE tab adds its detailed slice. Context is built by the pure, vitest-tested helpers in [`src/Testing/testing-agent-context.ts`](src/Testing/testing-agent-context.ts) (`buildTestingAgentContext` + per-surface shapers, `resolveTestRunByReference` id→name→contains resolver, `capList`); the dashboard harvests each child's reported state (`runsState`/`analyticsState`/`reviewState`) and publishes only the active tab's slice. All published lists are **bounded** (cap 25 for run names, 5 for analytics breakdowns) with a truncation flag; the selected run is reported by **id AND name**.

The agent-driven **run selection** flows through the additive `RunSelectionIntent` stream on `TestingInstrumentationService` (`setRunSelectionIntent(runId, open)` → `runSelectionIntent$`, with a monotonic nonce so re-selecting the same run replays). The dashboard owns no `SelectedRun` UI state — the Runs component (which does) consumes the intent and opens the detail panel (and, when `open`, the run's record). Filter/search intents reuse the existing `RunsFilterIntent` stream.

| Surface (tab) | Context Fields (deep) | Tools |
|---------------|-----------------------|-------|
| Dashboard | KPI slice only (ActiveRunCount + **ActiveRunNames**, TotalTestsRun, PassedCount, FailureCount, SkippedCount, ActiveTestCount, PassRate, PassRateTrend, AverageRunDuration, TotalTestCost, PendingReviewCount) | *(common only)* |
| Runs | + StatusFilter, TimeRange, SearchText, VisibleRunCount, **VisibleRunNames** (+truncation flag), **SelectedRunId+SelectedRunName**, DetailPanelOpen | FilterTestsByStatus, SearchTests, **SelectTestRun** (id; `open` to also open the record — read-only nav) |
| Analytics | + SelectedDays, VersionCount, **TopFailingTests / SlowestTests / MostExpensiveTests** (bounded breakdowns) | SetAnalyticsTimeRange |
| Review | + View (queue/history), PendingCount, ReviewedCount, AverageHumanRating, AgreementRate, HistorySearchText | **SubmitTestFeedback** (bounded 1-10 rating + correctness + comments), **SelectTestRun** |

> **Testing safety:** 🔒 test **EXECUTION is NEVER exposed on any tab** — `StartNewTest` / `RerunTest` have real side effects (cost, agent runs, state changes) and remain deliberate UI actions. The exposed tools are all navigation / filter / search / read-only / **SelectTestRun** (read-only nav: opens the detail panel; `open` opens the run record) / **SubmitTestFeedback** (bounded evaluation write on an existing run — part of the normal review flow, not an execution). The `SAFETY BOUNDARY` comment in `testing-dashboard.component.ts` (on `syncAgentToolsForMode`) enumerates this. Pure helpers are covered by [`src/__tests__/testing-agent-context.test.ts`](src/__tests__/testing-agent-context.test.ts) (22 tests).

### Scheduling — deep ("hardcore") enrichment + the mode-scoped tool pattern

The top-level **Scheduling** dashboard goes to Data-Explorer depth. The active tab (`dashboard`/`jobs`/`activity`) decides which deep context slice is published AND which surface-scoped tools are registered — same `lastRegisteredToolMode` + `syncAgentToolsForMode()` guard as Data Explorer / Testing. The always-on **KPI slice** (per-status job counts Active/Paused/Disabled/Pending/Expired, alert count, locked-job + running counts, overall success rate, bounded job-type breakdown with names) is published on every tab; the ACTIVE tab adds its detailed slice. Context is built by the pure, vitest-tested helpers in [`src/Scheduling/scheduling-agent-context.ts`](src/Scheduling/scheduling-agent-context.ts) (`buildSchedulingAgentContext` + KPI/Jobs/Activity slice shapers, `resolveSchedulingItem` id→name→contains resolver, `buildSchedulingNotFoundError`, `capSchedulingList`); the dashboard harvests each ACTIVE child's reported state and publishes only that tab's slice. All published lists are **bounded** (cap 25) with a companion truncation flag / total-count when truncated; the selected job is reported by **id AND name**; ID-taking tools also accept a **name** (exact id → exact name → partial-name, case-insensitive) and return a tolerant "available jobs" error on a miss.

| Surface (tab) | Context Fields (deep) | Tools |
|---------------|-----------------------|-------|
| Dashboard | KPI slice only (TotalJobs, ActiveJobCount/PausedJobCount/DisabledJobCount/PendingJobCount/ExpiredJobCount, AlertCount, LockedJobCount, RunningCount, SuccessRate, JobTypeCount + **JobTypeNames**) | *(common only)* |
| Jobs | + JobsSearchTerm, StatusFilter, TypeFilter, VisibleJobCount, **VisibleJobNames** (+truncation flag), **SelectedJobId+SelectedJobName** | FilterJobsByStatus, FilterJobsByType, SearchJobs, **SelectJob** (id **or** name — opens read-only editor slideout), **OpenJobRecord** (id **or** name — opens the entity record for viewing) |
| Activity | + ActivitySearchTerm, ActivityStatusFilter, ActivityJobNameFilter, ActivityTimeRange, VisibleExecutionCount, **VisibleExecutionJobNames** (+truncation flag), ActivityJobNameCount, **ActivityJobNames** | FilterActivityByStatus, FilterActivityByJob, SearchActivity, SetActivityTimeRange (24h/7d/30d/90d) |

COMMON tools (always present): **SwitchSchedulingTab**, **RefreshSchedulingData**, **GetJobDetails** (id **or** name — read-only single-job details).

> **Scheduling safety:** 🔒 the exposed tools are all **navigation / filter / search / read-only / select** only. `SelectJob`/`OpenJobRecord` open a job for **viewing** (slideout / entity tab) — they do NOT save or mutate. **Intentionally NOT exposed** (operational mutations): **pause/resume** (`updateJobStatus` / `ToggleJobStatus`), **delete** (`deleteJob` / `ConfirmDelete`), **create** (`saveJob` / `OpenCreateSlideout`), and **lock release** (`releaseLock`). Context exposes only aggregate counts, active filters/search, and visible **display** names — never lock tokens or raw job-configuration blobs. The `SAFETY BOUNDARY` comment in `scheduling-dashboard.component.ts` (on the agent section + `syncAgentToolsForMode`) enumerates this. Pure helpers are covered by [`src/__tests__/scheduling-agent-context.test.ts`](src/__tests__/scheduling-agent-context.test.ts) (28 tests).

### MCP — deep ("hardcore") enrichment + the mode-scoped tool pattern

The MCP (Model Context Protocol) dashboard goes to Data-Explorer depth across its four tabs (`servers`/`connections`/`tools`/`logs`). The active tab decides which deep context slice is published AND which surface-scoped tools are registered — the same `lastRegisteredToolMode` + `syncAgentToolsForMode()` guard as Data Explorer / Testing, keyed on the tab. COMMON tools (`SwitchMCPTab`, `SearchMCPTools`, `RefreshMCPData`, `ClearMCPFilters`) are always present; the per-tab tools swap on every tab flip, so a logs-only `SortLogs` is never visible while Servers is open. Context = the always-on top-level slice (active tab, server/connection/tool/exec counts, search, tools view mode) merged with the ACTIVE tab's deep slice. Context is built by the pure, vitest-tested helpers in [`src/MCP/mcp-agent-context.ts`](src/MCP/mcp-agent-context.ts) (`buildMCPAgentContextFull` + per-tab `buildMCPServersContext`/`buildMCPConnectionsContext`/`buildMCPToolsContext`/`buildMCPLogsContext`, the `resolveMCPItem` id→name→contains resolver, `buildMCPNotFoundError`, `capMCPNames`); the shared validation helpers (`validateEnumParam`, `validateStringParam`) come from `src/shared/agent-tool-validation.ts` (unmodified — MCP helpers are LOCAL). All published name lists are **bounded** (cap 25) with a companion `<key>Count` when truncated; the selected row is reported by **id AND name**; ID-taking Select/Open/Filter tools also accept a **name** (exact ID → exact name → partial-contains, case-insensitive) and return a tolerant "available names" error on a miss. `Open*Record` opens the entity record in a tab via `NavigationService.OpenEntityRecord` for **viewing** (never edits).

| Surface (tab) | Context Fields (deep) | Tools |
|---------------|-----------------------|-------|
| Servers | ServerCount, FilteredServerCount, ActiveServerCount, ServerStatusFilter, **VisibleServerNames** (+count when capped), SelectedServer **id+name** | FilterServersByStatus, **SelectServer** (id **or name**), **OpenServerRecord** (selected/by ref — view) |
| Connections | ConnectionCount, FilteredConnectionCount, ConnectionStatusFilter, **VisibleConnectionNames**, SelectedConnection **id+name** | FilterConnectionsByStatus, **SelectConnection** (id **or name**), **OpenConnectionRecord** (view), **NavigateToConnectionServer** (related-record → parent server, view) |
| Tools | ToolCount, FilteredToolCount, ToolsViewMode, ToolStatusFilter, ServerFilter **id+name**, CategoryFilter, FavoritesOnly, RecentOnly, FavoriteToolCount, **VisibleToolNames** / **AvailableServerNames** / **AvailableCategoryNames** (each bounded), SelectedTool **id+name** | FilterToolsByStatus, **FilterMCPToolsByServer** (id **or name**), FilterToolsByCategory, ToggleToolsFavoritesOnly, SetToolsViewMode, **ToggleMCPToolFavorite** (id **or name**), **SelectTool** (id **or name**), **OpenToolRecord** (view) |
| Logs | ExecutionCount, FilteredExecutionCount, FailedExecutionCount, LogStatusFilter, SortColumn+Direction, **VisibleLogLabels** (tool-name · status — NEVER args/result), SelectedLog **id+name**, DetailPanelOpen | FilterLogsByStatus, **SortLogs** (7 columns), **SelectLog** (id or tool-name → opens detail panel), **OpenLogRecord** (view) |

> **MCP safety:** 🔒 MCP is a **credential-bearing surface** — servers carry OAuth client secrets, connections carry bearer tokens, and OAuth token / client-registration / credential entities are in scope. Every exposed tool is **read / filter / sort / select / navigate / favorite-toggle / refresh** only; `Open*Record` opens a record in a tab for **viewing** (it does NOT edit). **Intentionally NOT exposed:** server/connection **create / edit / delete**; **MCP tool EXECUTION / test-with-params** (`openTestToolDialog` / the run path — untrusted LLM-supplied args against real, side-effecting external tools — there is NO execute tool on any tab); and any **secret/credential value** (server URL/command, bearer token, OAuth client secret, encrypted credential, or tool input/output payload). Context exposes only aggregate + per-tab counts, active filters/search, and visible **display** names (log labels are tool-name + status only) — never secrets, tool args, or tool results. The `SAFETY BOUNDARY` comment in `mcp-dashboard.component.ts` (on the agent section) and the file header of `mcp-agent-context.ts` enumerate the exclusions. Pure helpers are covered by [`src/__tests__/mcp-agent-context.test.ts`](src/__tests__/mcp-agent-context.test.ts) (28 tests) + the existing index test (8).

### Data Explorer — deep ("hardcore") enrichment + the mode-scoped tool pattern

The Data Explorer app's three sections (Data / Queries / Dashboards) go deeper than the baseline rollout:

- **Mode-scoped tools** (reusable pattern): a surface with two distinct contexts registers a DIFFERENT tool set per context, re-registering on the transition (a `lastRegisteredToolMode` guard + a `syncAgentToolsForMode()` called wherever context is re-emitted; `SetAgentClientTools` replaces the set, the streamed manifest follows). **Data** scopes `home` (entity browser) vs `entity` (record view) — at home the agent literally can't see record tools and vice-versa; **Dashboards** scopes `list` vs `open`. Copy this for any other two-mode surface.
- **Data — RECORD-VIEW tools** (entity selected): SelectView, ChangeViewType, FilterRecords (smart-filter), ClearRecordFilter, OpenRecord, CreateNewRecord, NavigateToRelated, **ExportView** (csv/excel/json via the view workspace → grid `Export()`), **OpenViewProperties**, **NextPage/PreviousPage/GoToPage/SetPageSize/SetSort** (live grid control via additive public API on `@memberjunction/ng-entity-viewer`). Context adds CurrentPage/TotalPages, SortColumn/Direction, RelatedEntities. **ENTITY-BROWSER tools** (home): OpenEntity, SearchEntities, SetEntityBrowserMode, ToggleEntityFavorite, ExpandAppGroup/CollapseAppGroup. **COMMON**: BackToEntityBrowser. *(SetVisibleColumns deferred — dynamically-mounted grid has no stable column setter; VisibleColumns still published.)*
- **Queries — 🟢 stored-query EXECUTION** (the high-value add): `RunStoredQuery` (name-or-id, typed Parameters, rows hard-capped ~200/default 50), `GetQueryMetadata` (params + field names), `PageQueryResults`. **Safety:** stored queries only (pre-vetted SELECTs), permission re-checked via `UserCanRun`, provider threaded via `this.RunQueryToUse`, **raw/ad-hoc SQL never set, SQL text never returned, results never in published context**. Catalog tools unchanged; create/edit/delete still excluded.
- **Dashboards — opened-dashboard awareness**: when a dashboard is open, context carries `OpenedDashboardPanels` (panel titles/types) + name/id/editing/canEdit; tools `GetDashboardPanels`, `GetDashboardDetail`, `GetCategoryHierarchy`, `GetDashboardShares` (all read-only). All mutations still excluded.

See **[packages/AI/Agents/AGENT_CONTEXT_GUIDE.md](../../../AI/Agents/AGENT_CONTEXT_GUIDE.md)** for the full architecture guide.

### Form Builder / Component Studio — deep ("hardcore") canvas-edit enrichment

The Form Builder cockpit ([`FormBuilder/form-builder-resource.component.ts`](src/FormBuilder/form-builder-resource.component.ts)) and Component Studio's Form Builder tab ([`ComponentStudio/component-studio-dashboard.component.ts`](src/ComponentStudio/component-studio-dashboard.component.ts)) are **two surfaces editing the SAME `FormCanvasModel`** through the SAME shared transform + client-tool factory ([`ComponentStudio/services/canvas-edit-transforms.ts`](src/ComponentStudio/services/canvas-edit-transforms.ts)). They were deepened to Data-Explorer depth. The pure, immutable transforms + the bounded context builder + the tolerant id→label→contains resolvers live in that **one shared** transforms file (so the two surfaces never drift); the per-component wiring is a thin `CanvasEditHost` adapter (`GetCanvas`/`ApplyCanvas`/`NewElementId`/`NewSectionId` + Form Builder's optional pane actions). The shared validation/cap helpers in `src/shared/agent-tool-validation.ts` are **unmodified** — the canvas transforms are self-contained pure functions with their own local coercion + bounded list caps (`CANVAS_CONTEXT_LIST_CAP = 25`).

**Deep context** (`buildCanvasStateSummary`, re-emitted on EVERY canvas change + selection change): the full bounded `CanvasSummary` — EntityName, FormTitle, section/element/field counts, a **Sections[]** summary (id·title·collapsible·columns·element-count·**fieldCount**), an **Elements[]** summary (every placed element addressable by **id AND display label**: type·label·fieldName·sectionId·required·span), **PlacedFieldNames**, **AvailableFieldNames** (curated entity fields NOT yet placed — the AddField candidate set, with `*Truncated`/`*Count`), and the resolved **SelectedElement** / **SelectedSection** by NAME (not just id). Form Builder layers on `SelectedElementName`/`SelectedSectionName`, derived non-persisting **ValidationIssues** (empty form, empty section, field placed twice — bounded), `PreviewError`, plus the existing `Code`/`SchemaMarkdown`/`RelatedEntitiesMarkdown`/override identity. Component Studio publishes the same `CanvasSummary` + `IsDirty`/`ActiveTab`/selection ids (re-emitted via `reemitFormAgentContext`, gated on `formAgentContextActive` so a canvas edit before "Open in Chat" doesn't register context/tools).

**Deepened canvas-edit tool suite** (all 🔒 reversible canvas-DEFINITION edits — id-taking tools also accept a **label/title**, exact id → exact label → partial-contains, and return a tolerant "Available: …" error on a miss):
- *fields:* AddField, RemoveField, SetFieldLabel, ToggleFieldRequired, SetFieldSpan, **SetFieldType** (field/static-text/spacer/computed, validated), **SetFieldHelpText** (set/clear helper), **MoveFieldToSection** (by id/label → section by id/title, optional index), **DuplicateField** (clone in place)
- *sections:* AddSection, RemoveSection, SetSectionTitle, ReorderFields, **SetSectionCollapsible** (toggle/explicit), **SetSectionColumns** (1/2), **SetSectionOrder** (move to a 0-based index)
- *Form Builder only (view-only nav):* PreviewForm, ViewFormCode, ViewFormLayout (Component Studio omits these — it navigates via its tab system, the factory drops them when the host doesn't provide them)
- *both:* the wholesale UpdateForm (whole-canvas replace)

> **Form Builder / Component Studio safety:** 🔒 the suite exposes ONLY **reversible in-memory canvas-DEFINITION edits** + Form Builder's **view-only pane switches**. **Save / Publish / persist-to-DB / delete-the-whole-form / EntityFormOverride activation** are deliberately NOT exposed — they're irreversible and stay human-confirmed in the UI. The agent rearranges the canvas freely; committing it to the database is always a human action. The `🔒 SAFETY BOUNDARY` comment at the top of `canvas-edit-transforms.ts` enumerates this and the rule "do NOT add a Save/Publish/DeleteForm tool to `buildCanvasEditClientTools`". Pure transforms + resolvers + the context builder are covered by [`src/__tests__/canvas-edit-transforms.test.ts`](src/__tests__/canvas-edit-transforms.test.ts) (81 tests, including a safety test asserting no save/publish/persist/deleteform/activate/override tool name ever appears).

### Knowledge Hub — Clusters / Visualize / Analytics deep ("hardcore") enrichment

These three Knowledge Hub surfaces are **separate `BaseResourceComponent`s** (not tabs in one host), so they're **naturally mode-scoped** — each owns its own deep context + tools (no `lastRegisteredToolMode` flip needed). Pure, vitest-tested helpers live **LOCAL** to each component folder; the shared validation/cap helpers (`validateEnumParam`, `validateStringParam`) come from `src/shared/agent-tool-validation.ts` (unmodified). All published name/summary lists are **bounded** (cap 25) with a companion total-count when truncated; selected items are reported by **id AND name**; ID-taking tools also accept a **name** (exact id → exact name → partial-contains, case-insensitive) and return a tolerant "available names" error on a miss. Handlers are tolerant (`{ Success, Data?, ErrorMessage? }`, never throw). Re-emit on every meaningful state change (run complete, mode/tab switch, filter, selection, drilldown open/close, recompute). Pure helpers covered by [`src/__tests__/knowledge-hub-agent-context.test.ts`](src/__tests__/knowledge-hub-agent-context.test.ts) (31 tests).

| Surface | Context Fields (deep) | Tools |
|---------|-----------------------|-------|
| **Clusters** ([clusters/cluster-agent-context.ts](src/KnowledgeHub/components/clusters/cluster-agent-context.ts)) | IsVisualizationLoaded, VisualizationTitle, IsRunning, RunError, ConfigEntityName/Algorithm/Dimensions, ClusterCount, TotalPoints, **Clusters** (id+label+point-count, bounded), SilhouetteScore, **AvailableEntities** (sources), SavedVisualizationCount, **SavedVisualizations** (bounded), ActiveSaved **id+name** | RegenerateClusters, ResetClusterAnalysis, **SetClusterSourceEntity** (name), **SetClusterAlgorithm** (kmeans/dbscan), **SetClusterDimensions** (2/3), **OpenSavedVisualization** (id **or** name), ListSavedVisualizations |
| **Visualize** ([visualize/visualize-agent-context.ts](src/KnowledgeHub/components/visualize/visualize-agent-context.ts)) | ActiveVisualizationMode, AvailableVisualizationModes, DrilldownVisible/Loading, DrilldownTitle/Subtitle, **DrilldownRecords** (id+title+subtitle, bounded) + count | SwitchVisualizationMode (clusters/tagcloud), **OpenDrilldownRecord** (id **or** title — view), CloseDrilldown, ListDrilldownRecords |
| **Analytics** ([analytics/analytics-agent-context.ts](src/KnowledgeHub/components/analytics/analytics-agent-context.ts)) | **always-on:** ActiveTab, DateRange, EntityFilter + **AvailableEntityFilters**, IsLoading, DrillDownOpen+Target, **KPIs** (bounded), PipelineStatus · **tags:** TopTags + CoOccurrencePairs (+LastComputed) · **sources:** SourceComparison + **AvailableSourceNames** + SelectedSourceName · **quality:** QualityScore + ConfidenceStats · **cost:** CostKPIs | SwitchAnalyticsTab (overview/tags/sources/pipeline/quality/cost), SetAnalyticsDateRange, **SetAnalyticsEntityFilter** (name), **SelectAnalyticsSource** (name → Sources tab), **OpenAnalyticsDrillDown** / **CloseAnalyticsDrillDown**, ExportAnalyticsCSV (top-tags/kpis/cost-usage), **RecomputeAnalyticsCoOccurrence** (idempotent aggregate refresh), RefreshAnalyticsData, ListAnalyticsSources |

> **Knowledge Hub deep-enrichment safety:** all three are **read / filter / search / select / navigate / idempotent-recompute** only.
> - **Clusters** 🔒 cluster recompute (RegenerateClusters / ResetClusterAnalysis / SetClusterDimensions re-projection) is idempotent and OK; **DELETE a saved visualization (`OnDeleteSaved`) and SAVE (`OnSaveVisualization`, writes a User Setting) are NOT exposed**. SetClusterSourceEntity/Algorithm only stage config — the user/agent then runs.
> - **Visualize** (host) 🔒 only mode-switch + open-a-listed-record (view) + close-drilldown — **nothing mutates / deletes / re-tags**. The embedded Clusters child registers its own tools when standalone; here it's `Embedded`, so cluster recompute is reached through the child's UI, not duplicated on the host.
> - **Analytics** 🔒 read-only reporting surface — CSV export is a local download of already-visible aggregate rows (no record bodies); RecomputeAnalyticsCoOccurrence only re-derives an aggregate table. **No content/tag/source/run mutation exists to gate.**
> Each component carries a documented `🚨 SAFETY BOUNDARY` comment enumerating the exclusions.
