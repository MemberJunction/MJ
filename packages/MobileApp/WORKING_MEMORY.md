# MJ Mobile App — Build Working Memory

> 🚨🚨 **KEY DIRECTIVE (2026-05-22, user, MUST stay top of mind through any compaction):**
> Autonomously complete **100% of the mobile app plan** (`plans/mobile-app-react-native/README.md`)
> stepwise, end to end. Stand up MJAPI against a clean **MJ_5_36_0** (5.36) database. **Verify every
> feature live in the iOS Simulator via computer-use** (screenshots + interaction) — each must be
> "amazing and perfect." Use **sub-agents** to conserve context. **DO NOT STOP and DO NOT ask the
> user for help again** until every item in the plan is complete + verified. User is away. If blocked
> (e.g., auth), self-serve a solution (local dev auth, etc.). Check `/tmp/mj-jwt.txt` for a JWT the
> user may have left. 🚨🚨

> **Purpose:** Persistent task list + status + resume guide for the autonomous build of the MJ
> React Native mobile app. If a Claude Code session is interrupted, a new session should read
> this file first, then continue from the "Next action" pointer below.
>
> **Keep this file updated** as work progresses: flip checkboxes, append to the Work Log, and
> always keep "Next action" accurate.

**Last updated:** 2026-05-22
**Branch:** `an-mobile-app-dev` (tracks `origin/an-mobile-app-dev`) · PR #2617
**Repo root:** `/Users/amith/develop/MJ` · App: `packages/MobileApp/`

---

## Mission

Implement **100% of the mobile app plan** (`plans/mobile-app-react-native/README.md`, 764 lines).
Phase 1 was already "code-complete"; this effort closes the **Phase 1 gaps** first, then builds
**Phase 2 and Phase 3** fully. User granted full machine control and standing approval to
commit/push at clean milestones.

## Environment state (done)
- ✅ Merged `origin/next` into branch (0 behind next). Merged pickup-guide commit.
- ✅ Fresh SQL Server DB `MJ_5_36_0` created (Docker container `sql`, localhost:1433, sa;
  sqlcmd at `/opt/mssql-tools18/bin/sqlcmd` inside container). After reboot: `docker start sql`.
- ✅ `.env` (root, symlinked from packages/MJAPI/.env) `DB_DATABASE` → `MJ_5_36_0`. Backup `.env.bak`. (.env is gitignored.)
- ✅ `mj migrate` → 118 migrations applied (schema/metadata at 5.36.0).
- ✅ `npm install` + `npm run build` (203/203 tasks) clean. expo/expo-router/react-native installed; react pinned 19.1.0.
- ✅ MobileApp baseline `npx tsc --noEmit` → 0 errors.

## Toolchain status
- ✅ **Xcode 26.5 installed** (2026-05-22) at `/Applications/Xcode.app` (build 17F42). xip expanded from `~/Downloads/Xcode_26.5_Apple_silicon.xip`. User ran the sudo steps: `xcode-select -s`, `xcodebuild -license accept`, `xcodebuild -runFirstLaunch`. `xcodebuild -version` works.
- ⏳ **iOS Simulator runtime downloading** via `xcodebuild -downloadPlatform iOS` (background; ~7GB). `xcrun simctl list runtimes` was EMPTY before download. Modern Xcode ships runtimes separately from the base app.
- ⚠️ **CocoaPods: brew is BROKEN** for this (internal Ruby error pouring `ca-certificates` while building the `ruby` dep — crashes both `brew install cocoapods` and Expo's fallback). watchman also not installed via brew. **WORKAROUND THAT WORKS:** macOS system Ruby is 2.6.10 (too old — latest cocoapods deps `ffi`/`securerandom` need Ruby ≥3.1). Used Homebrew's bundled **portable Ruby 4.0.2** at `/opt/homebrew/Library/Homebrew/vendor/portable-ruby/4.0.2_1/bin` to `gem install --user-install --no-document cocoapods` → CocoaPods 1.16.2 at `~/.gem/ruby/4.0.0/bin/pod`. **ALSO REQUIRED:** portable Ruby is 4.0.2 (Ruby ≥3.4 removed the `kconv` stdlib that CocoaPods 1.16.2 needs → `pod install` fails with `cannot load such file -- kconv`). Fix: `gem install --user-install --no-document nkf` (the `nkf` gem ships `kconv.rb`). After that, `pod install` works (88 deps / 87 pods installed cleanly for this app). To build, prepend PATH: `export PATH="$HOME/.gem/ruby/4.0.0/bin:/opt/homebrew/Library/Homebrew/vendor/portable-ruby/4.0.2_1/bin:$PATH"` before `npx expo run:ios`. (NOTE: portable-ruby path can change on `brew update` — re-resolve `.../portable-ruby/*/bin`.) watchman still not installed but Metro works without it. `ios/` is generated by prebuild (gitignored).
- Global `mj` CLI is 5.23.0 (repo 5.36.0) — migrations applied fine regardless.

## Testing status (2026-05-22)
- ✅ markdown-core: 54 unit tests (engine parseToHtml/parseToTokens, config toggles, injected highlightFn, svg tokenizer, collapsible HTML structure, helpers). `cd packages/MarkdownCore && npm test`.
- ✅ ng-markdown: 12 tests (jsdom env) for web DOM utilities (sanitizeSvgContent, code-copy add/remove, collapsible DOM helpers). Added `jsdom` devDep + jsdom vitest env. `cd packages/Angular/Generic/markdown && npm test`. Both packages added to root `vitest.config.ts` projects.
- MobileApp RN MarkdownView: no RN unit-test harness exists in the app (jest-expo/@testing-library/react-native not set up); its verification is part of the native iOS computer-use QA, not vitest.

## 🟢 AUTHENTICATED APP LIVE AGAINST MJAPI (2026-05-22) — major unlock
- MJAPI on **:4001** (added GRAPHQL_PORT=4001 to MJAPI/.env), DB MJ_5_36_0, Docker `sql` up.
- AUTH: ROPG disabled for Auth0 client; `mjmobile://auth` NOT a registered callback. Solution that works: auth-code+PKCE via browser using MJExplorer's web callback `http://localhost:4200`, headless creds da-robot-tester@bluecypress.io. Minted id_token → injected via `Env.devAuthToken` in src/config/env.ts (LOCAL ONLY — revert before commit; ~24h expiry, re-mint with the token sub-agent recipe). Seed Owner user email renamed → da-robot-tester@bluecypress.io so token maps to a full-access user. MJAPI restarted to pick up rename.
- App now CONNECTS: logs `[MJProvider] setupGraphQLClient ok`, lands on **Conversations** screen (empty state correct on clean DB). App↔MJAPI↔DB end-to-end works.
- **Two real RN-compat bugs fixed in @memberjunction/core (KEEP/commit these — genuine fixes):**
  1. `crypto.getRandomValues` missing in Hermes (uuid). Fix: `src/polyfills.ts` polyfills global.crypto.getRandomValues via expo-crypto (already linked, no rebuild); imported FIRST in app/_layout.tsx.
  2. `runningOnNode()` in `packages/MJCore/src/generic/logging.ts` used `process.versions !== null` → threw on `process.versions.node` in RN (process.versions undefined). Fixed to `!= null`. Rebuilt MJCore.
- **GOTCHA (cost time):** MobileApp had a STALE NESTED copy `packages/MobileApp/node_modules/@memberjunction/{core,core-entities,global,graphql-dataprovider}` (real dirs from the 5.34.1 era) that Metro resolved BEFORE the root workspace symlinks → fixes didn't apply. FIX: bumped app deps 5.34.1→5.36.0, then `rm -rf packages/MobileApp/node_modules/@memberjunction` (root has all 233 as workspace symlinks), then `npx expo start --clear`. After ANY workspace package rebuild, Metro needs `--clear` to pick it up.
- DEV WORKFLOW: Metro = `npx expo start` (port 8081) in MobileApp with PATH including ruby/cocoapods (only needed for native rebuild). Relaunch app: `xcrun simctl terminate booted org.memberjunction.mobile; xcrun simctl launch booted org.memberjunction.mobile`. Screenshot: `xcrun simctl io booted screenshot /tmp/x.png` then Read it. Navigate: `xcrun simctl openurl booted "org.memberjunction.mobile:///<route>"`. NO tap injection (TCC blocks CGEvent; only Automation granted) — use deep links + dev harness routes for interactions.

## CHAT HERO FEATURE — findings (2026-05-22, via dev harness app/devchat.tsx)
- Data flow WORKS: createConversation + sendMessage create Conversation + ConversationDetail; `RunAIAgentFromConversationDetail` dispatches the agent (fireAndForget) and MJAPI runs it.
- **Skip (default agent) FAILS**: MJAPI logs `[SkipSDK] Sending request to Skip API: undefined → TypeError: Invalid URL`. Skip is an EXTERNAL service; this clean env has no Skip API URL configured. The error is written back to the conversation detail (Status=Error, Message="Invalid URL"). → To use Skip, user must configure Skip API in MJAPI. For QA, route to a NATIVE agent.
- **Native agents WORK**: routed to "Demo Minimal Loop Agent" (ID 2E081955-B2FD-4E13-81DF-5DC7C59126BE) → "AI AGENT RUN COMPLETED (2210ms)", produced a real markdown response (## heading, bullet list, **bold**, ```ts block). LLM keys present: AI_VENDOR_API_KEY__GeminiLLM, __CerebrasLLM.
- **OPEN BUG 1 (WS streaming)**: client throws an UNHANDLED `[object Object]` (redbox in dev) from the `void sendMessage` WebSocket-subscription path even when the agent completes server-side (result still lands in DB via fireAndForget). Likely a ws://localhost:4001/graphql subscription error object. Non-fatal to result delivery but needs fixing for live progress + to remove the redbox. Get the real error via a global ErrorUtils handler / stringify in GraphQLDataProvider AI subscription path.
- **OPEN BUG 2 (agent response role)**: the agent's response rendered as RAW markdown in a USER bubble because the stored response detail came back Role='User' (adapt.ts: Role==='User'→kind user→parseUserMessage; only 'AI'/'Error'→kind agent→MarkdownView). Need to confirm whether fireAndForget/Demo agent stores response with Role='AI'+AgentID, or adapt mapping needs adjusting. MarkdownView IS correctly wired to the agent branch (proven via /markdown-preview).
- VERIFIED: chat thread renders (header, recents strip chips, conversation, message bubble); MarkdownView renders perfectly on its own (markdown-preview). Markdown-in-chat will work once a response has Role='AI'.
- AGENT INVOCATION ALIGNED TO ng-conversations (2026-05-22): Studied the real reference — `ng-chat` is a dumb UI; `@memberjunction/ng-conversations` (conversation-agent.service.ts + message-input.component.ts) is the real flow. It ALSO creates the user ConversationDetail client-side then calls `provider.AI.RunAIAgentFromConversationDetail` (the same helper mobile used) — so mobile's detail-creation was already correct. The REAL gap: mobile wasn't running the ambient **Sage** orchestrator (Name==='Sage', seeded ID 3AB78346-897F-4238-AA6A-F10A131CC691) nor passing the Data payload. FIXED `src/data/services/agents.ts` sendMessage: default to Sage when no agentId override, and pass `data: { conversationId, latestMessageId: detail.ID, ALL_AVAILABLE_AGENTS: [top-level active agents except Sage] }` — faithful to ng-conversations. tsc clean.
- ✅ **CHAT NOW WORKS END-TO-END (2026-05-22)** — Sage replies, markdown renders via MarkdownView, working indicator shows. Three fixes:
  1. **THE key bug**: `RunAIAgentFromConversationDetail` writes the response INTO the `conversationDetailId` you pass. ng-conversations (message-input.component.ts:1011-1020) passes the **AI placeholder detail's ID** (Role='AI', In-Progress), NOT the user message. Mobile was passing the user detail → response landed on the user row (Role='User', raw text). FIX in `sendMessage`: create an AI placeholder ConversationDetail (Role='AI', Status='In-Progress', ParentID=userDetail, AgentID=target) and pass `conversationDetailId: aiDetail.ID`. Agent reads the prompt via history + `data.latestMessageId`. Now response → Role='AI' detail → renders via MarkdownView with agent attribution.
  2. **WS unreliable on RN**: `PushStatusUpdates` (graphql-ws) errors → `fireAndForgetHelper` now has an error handler (added) so it's not a fatal redbox; and `sendMessage` returns success=true (submitted), with the chat thread (`app/chat/[id].tsx` handleSend) POLLING `getConversationDetailStatus(aiMessageId)` every 2.5s (≤60s) + refresh() until the AI detail finalizes (Complete/Error). This is the WS-independent result delivery (Angular has a 30s polling fallback too).
  3. Suppressed dev-only LogBox noise (Error running AI agent / PushStatusUpdates / FireAndForget) via LogBox.ignoreLogs in polyfills.ts.
  - Working indicator: the in-progress Role='AI' bubble renders "Working…" (adapt.ts treats 'AI'/'Error' as agent rows; MessageRenderer shows spinner while Status='In-Progress'). Default agent = **Sage** when no agentId.
- NOTE: Skip API still not configured (Sage answers simple things directly; delegating to Skip would error). Native agents + Sage-direct replies work. Test harness: `app/devchat.tsx` (waits for ready → creates conv → opens `/chat/[id]?autosend=<prompt>` which auto-runs the real send flow). The `?autosend=` thread param is a small deep-link feature added for QA.
- ✅ FIRST-MESSAGE PROGRESS FIX (2026-05-22): the first message used to be fired from new-conversation (fire-and-forget) then navigate, so the thread showed no indicator until a 2nd message. NOW `new-conversation.tsx start()` does createConversation → `router.replace('/chat/[id]?autosend=<body>')`; the thread's handleSend (working indicator + poll) runs the send. The `?autosend` thread param handles this (and devchat uses the same path). Verified: working bubble on first msg → Sage markdown reply.
- ✅ SCROLL FIX: chat thread ScrollView now has a ref + `onContentSizeChange → scrollToEnd` (auto-scroll to newest, Angular QoL), `keyboardShouldPersistTaps='handled'`, visible scroll indicator; and MarkdownView's horizontal ScrollViews (code/tables) got `directionalLockEnabled` + `nestedScrollEnabled` so they don't capture vertical pan (the likely "can't scroll" cause when a reply has a code block/table).
- STILL OPEN (lower priority): make graphql-ws actually connect on RN (then live progress streams instead of polling); "Invalid Date"/agent-count cosmetics in conversation list; clean up debris conversations (DELETE needs `SET QUOTED_IDENTIFIER ON` + FK order); profile default-agent no longer applied to first message (thread always uses Sage default — wire getDefaultAgentId into thread handleSend if that feature is wanted).

## Native iOS run + QA (2026-05-22) — VALIDATED LIVE
- ✅ **App BUILDS + RUNS on the iOS 26.5 simulator** (`MJ-iPhone`, device A1E82DBD-49CE-4D26-B8F2-F3C1951B9136). `npx expo run:ios` → Build Succeeded (0 errors), installed + launched. Metro bundled 2082 modules. Logs confirm native modules load (MMKV `mj-mobile-prefs` instance created live = P1.5 code running).
- ✅ Login screen renders correctly (MemberJunction wordmark, Auth0 / Microsoft / Developer-options). Login needs a real JWT (Developer options → paste-JWT sheet, stored in expo-secure-store) or OAuth; conversations/agents need MJAPI reachable at `Env.graphqlUrl` + DB up.
- ✅ **MarkdownView QA'd ON-DEVICE** via a dev-only route `app/markdown-preview.tsx` (renders a sample, no auth/backend needed). Deep link: `xcrun simctl openurl booted "org.memberjunction.mobile:///markdown-preview"`. Verified rendering of: h1/h2, bold/italic/strikethrough/inline-code/links/hard-break, code block w/ language label + Copy button (formatLanguageName + expo-clipboard), bulleted/nested/ordered/TASK lists (☑/☐), bordered table w/ bold header, blockquote, and ```svg``` → real SVG via react-native-svg SvgXml. Screenshots at /tmp/mj-md.png + /tmp/mj-md2.png.
- COMPUTER-USE method: `xcrun simctl io booted screenshot <png>` + Read the image; navigation via `xcrun simctl openurl`. TAP injection NOT available (idb/cliclick need broken brew; no PyObjC/Quartz). So interactive taps (e.g., toggling profile prefs, tapping buttons) can't be automated yet — would need cliclick binary (curl from GitHub) + Simulator window coord mapping, or idb.
- (Resolved non-bug) Suspected an italic edge case in the preview intro, but verified marked emits a correct `em` token for `*same engine*` after a `br` with `breaks:true`, and the renderer handles `em` uniformly — it was a misread of italic glyphs in the downscaled screenshot. No fix needed.
- NOTE: `app/markdown-preview.tsx` is a DEV-only screen (not linked in-app). Remove before any production cut, or keep for QA.

### Deferred until AFTER reboot + Xcode install
- `brew install cocoapods watchman` (or let `npm run ios` auto-install pods)
- `npm run ios` — first build of native shell (3–6 min)
- Live Auth0 sign-in (needs `mjmobile://auth` added to Auth0 dev tenant callback URLs — see plan §3)
- On-device QA of every screen; native-module verification (voice/push/biometric)

### Doable NOW (no Xcode needed) — all TypeScript
All client TSX + all MJAPI server-side endpoints (pure Node, buildable/testable). Validate with
`cd packages/MobileApp && npx tsc --noEmit` and `npm run build` in affected packages.

---

## TASK LIST

Legend: [ ] todo · [~] in progress · [x] done

### Phase 1 gaps (do first)
- [x] **P1.1 — Apollo/MJ auth reconciliation. DONE (2026-05-22).** Removed the vestigial Apollo provider: dropped `<ApolloProviderRoot>` wrapper + import from `app/_layout.tsx`; deleted `src/providers/apollo-provider.tsx`; removed `@apollo/client` from `package.json` (kept `graphql` — graphql-request peer); `npm install` at root removed @apollo/client + 12 transitive deps; `npx tsc --noEmit` → 0 errors. Data path is graphql-request via `GraphQLDataProvider`; no consumers of Apollo remained (confirmed by grep).
- [x] **P1.2 — `@memberjunction/markdown-core` extraction. DONE (2026-05-22).** DECISIONS (2026-05-22, user-approved): (a) package name = `@memberjunction/markdown-core` at `packages/MarkdownCore` (top-level `packages/*` workspace glob). (b) Core supports BOTH a `parseToHtml` path (web) AND a `parseToTokens` AST path (RN). (c) ng-markdown STAYS on the HTML-string + DOM post-processing path for now (zero web-regression risk) — it just sources marked config/extensions/types from core. (d) Open a GitHub issue referencing PR #2617 tracking the longer-term migration of ng-markdown to the AST path + regression tests (copy buttons, collapsible nesting, mermaid, alerts, heading IDs, smartypants).
  - SOURCE MAP (from `packages/Angular/Generic/markdown`, `@memberjunction/ng-markdown` v5.36.0): It is HTML-string based (`marked.parse()` → `[innerHTML]` → DOM post-processing), NOT AST. Pure/liftable: `MarkdownService` (only `@Injectable` is Angular), `extensions/{svg-renderer,collapsible-headings,code-copy}.extension.ts`, `types/markdown.types.ts`, helpers (`formatLanguageName`, `isSvgContent`, `sanitizeSvgContent`). Angular/DOM-bound (STAYS in ng-markdown): `MarkdownComponent` (DomSanitizer, @Input/@Output, ChangeDetectorRef, ElementRef, MutationObserver theme-watch), mermaid rendering (needs browser DOM), and the DOM post-processing that injects copy buttons + wires collapsible toggles. marked plugins: marked, marked-alert, marked-gfm-heading-id, marked-highlight, marked-smartypants, prismjs (mermaid stays web-only).
  - PLAN: markdown-core deps = marked + marked-* plugins (NO prismjs, NO mermaid — highlighter is INJECTED via `highlightFn`). Expose `MarkdownEngine` with `configureMarked(config, {highlightFn})`, `parseToHtml`, `parseToTokens`. RN renderer in MobileApp uses parseToTokens → View/Text/Pressable; code via react-native-syntax-highlighter (Prism), code-copy via expo-clipboard, svg via react-native-svg; mermaid deferred on mobile.
  - PROGRESS (2026-05-22):
    - ✅ markdown-core CREATED at `packages/MarkdownCore` (`@memberjunction/markdown-core` 5.36.0). DOM/Prism/mermaid-free. Files: types/markdown.types.ts, helpers/{language,escape}.ts, extensions/{svg-renderer(factory+isSvgContent), collapsible-headings(factory+restructure)}.extension.ts, engine/markdown-engine.ts (configureMarked+parseToHtml+parseToTokens+normalizeHtmlBlockIndentation), index.ts. Added to root `vitest.config.ts` projects + has local `vitest.config.ts`. Build clean, 17 unit tests pass.
    - ✅ ng-markdown REFACTORED to consume core (HTML path preserved). markdown.service.ts now holds a `MarkdownEngine`, injects Prism highlightFn, keeps DOM methods (mermaid, copy buttons, unwrapMiscodedHtml) + Prism language imports. Component changed ONE import line (types from core). Extension files trimmed to web-DOM-only (svg→sanitizeSvgContent, collapsible→4 DOM helpers, code-copy→imports formatLanguageName from core). public-api.ts drops type/factory re-exports (rule #5). package.json: dropped marked + 4 marked-* (now transitive via core), added @memberjunction/markdown-core. ngc build clean. Verified NO consumer imports any moved symbol (only MarkdownModule/MarkdownService used externally).
    - ✅ GitHub issue #2665 created (https://github.com/MemberJunction/MJ/issues/2665) — tracks ng-markdown→AST migration + full regression-test checklist, references PR #2617.
    - ✅ RN renderer DONE. `src/components/markdown/MarkdownView.tsx` consumes markdown-core `parseToTokens` and renders the AST → View/Text/Pressable. Covers headings, paragraphs, lists (incl. task lists), blockquotes, tables, code blocks (copy via expo-clipboard ~8.0.8), inline code/bold/italic/strikethrough/links, hr, and ```svg``` (react-native-svg SvgXml). Wired into chat (`app/chat/[id].tsx` MessageRenderer — replaced the naive `renderMarkdownInline` placeholder). Added deps to MobileApp: @memberjunction/markdown-core 5.36.0 (workspace symlink), expo-clipboard ~8.0.8, marked ^17.0.1 (direct type import; rule #5 forbids re-export from core). `npx tsc --noEmit` clean.
    - DEFERRED in RN renderer (needs simulator/Xcode QA): syntax highlighting in code blocks (planned react-native-syntax-highlighter/Prism — left as plain monospace for now), and mermaid (web-only/DOM). Noted in MarkdownView.tsx header comment.
  - P1.2 STATUS: ✅ COMPLETE (all 5 sub-tasks). Full chain verified green: markdown-core build + 17 tests pass; ng-markdown ngc build clean; MobileApp tsc clean. RUNTIME/bundle verification of RN renderer still pending simulator (marked v17 ESM under Metro/Hermes is the main thing to confirm on-device).
- [ ] **P1.3 — Interactive-component artifacts via `@memberjunction/react-runtime`.** RN integration: `ComponentManager.LoadComponent(spec)` → render `<Compiled {...props}/>` in RN container; `ReactRootManager` shim (RN owns root); web-primitive shim (div→View, span→Text, button→Pressable, style normalization); lazy-load runtime (Babel standalone ~2MB). Replace the "view on desktop" stub in `app/artifact/[id].tsx`. Do the §4.3 investigation tasks (Hermes + Babel standalone, JSX runtime, bundle impact).
- [ ] **P1.4 — Real dashboard part rendering.** Native renderers for common DashboardPart types (KPI grid, line/bar chart via victory-native or react-native-svg, list). Complex/desktop-only parts → "Open on desktop" placeholder card. Replace best-effort placeholder in `app/explorer/dashboard/[id].tsx`.
- [ ] **P1.5 — Profile toggles wiring.** Wire what Phase 1 allows: Appearance (Light/Dark/System theme via theme provider), default-agent picker persistence, persist prefs to MMKV. Leave voice/push/biometric toggles to feed Phase 2 features.

### Phase 2
- [ ] **P2.1 — Voice STT.** expo-av Audio.Recording → upload to new MJAPI Whisper transcription endpoint → create `Conversation Detail`. Wire voice-mode.tsx (replace visual scaffold/hardcoded transcript).
- [ ] **P2.2 — Voice TTS.** Agent response playback via expo-speech (system TTS) or ElevenLabs via MJAPI.
- [ ] **P2.3 — Push notifications.** expo-notifications client registration + token storage; new MJAPI server endpoint to register device tokens and send via APNs/FCM (Expo push). Wire profile toggle.
- [ ] **P2.4 — Biometric lock.** expo-local-authentication app-unlock gate on cold start/resume; wire profile toggle; store preference.
- [ ] **P2.5 — Record editing/creation.** Editable record detail using BaseEntity.Save(); start with safe field types; validation + error surfacing. Make Explorer record detail no longer read-only.

### Phase 3
- [ ] **P3.1 — Photo/file capture & attachments** (expo-image-picker / expo-document-picker → upload).
- [ ] **P3.2 — Offline mutation queue** with sync-on-reconnect (MMKV/SQLite-backed queue).
- [ ] **P3.3 — Android verification** (Gradle/AVD; RN code shared, mostly verification).

### Cross-cutting / closeout
- [ ] Keep `npx tsc --noEmit` clean in MobileApp after every change; build affected workspace packages.
- [ ] Update PR #2617 description to reflect real scope (currently stale) — only with explicit approval.
- [ ] Post-Xcode: full simulator QA pass against plan mockups (`plans/mobile-app-react-native/index.html`).

---

## NEXT ACTION
P1.1 and P1.2 are DONE. Start **P1.3 — Interactive-component artifacts via `@memberjunction/react-runtime`**
(replace the "view on desktop" stub in `app/artifact/[id].tsx`). Do the §4.3 investigation first
(Hermes + Babel standalone, JSX runtime, bundle impact) before committing to an approach. If P1.3's
runtime questions are blocked without a simulator, P1.4 (dashboard part rendering) and P1.5 (profile
toggles) are also pure-TS and doable now.

Also pending (not blocking): commit the P1.1 + P1.2 work (user has standing approval to commit at
clean milestones, but confirm). Uncommitted as of this note.

(Post-reboot env note 2026-05-22: Docker Desktop NOT running — run `docker start sql` (after
starting Docker Desktop) before any migration/server/DB work. Still only Xcode Command Line Tools;
full Xcode + CocoaPods + watchman still pending — native/simulator runtime QA remains blocked. The
RN MarkdownView in particular needs an on-device check that marked v17 ESM bundles under Metro/Hermes.)

---

## WORK LOG
- 2026-05-22 — Env setup complete (merge next, fresh MJ_5_36_0 DB + migrate, build, install). Created this working-memory file. Beginning Phase 1 gaps.
- 2026-05-22 — P1.1 investigation: Apollo client is vestigial (no consumers; data path uses graphql-request via GraphQLDataProvider). Decided to remove it. Edits NOT yet applied — interrupted for restart. No code changed yet; only this file + memory written.
- 2026-05-22 (post-reboot) — P1.1 COMPLETE. Applied all 5 steps: removed Apollo provider wrapper+import from `app/_layout.tsx`, deleted `src/providers/apollo-provider.tsx`, removed `@apollo/client` from package.json, `npm install` at root (−13 packages), `npx tsc --noEmit` → 0 errors. Confirmed no other Apollo consumers via grep. Next: P1.2 markdown-core extraction. (Docker not yet running; Xcode still CLT-only.)
