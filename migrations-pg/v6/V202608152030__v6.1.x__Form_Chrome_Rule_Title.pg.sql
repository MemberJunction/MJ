-- ============================================================================
-- MemberJunction PostgreSQL Migration — V202608152030__v6.1.x__Form_Chrome_Rule_Title.sql
-- Generated by the split-and-regenerate pipeline (AST dialect transpile of
-- hand-written DDL; CodeGen objects regenerated natively; metadata re-seeded).
-- ============================================================================

CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE SCHEMA IF NOT EXISTS __mj;
SET search_path TO __mj, public;
SET standard_conforming_strings = on;

ALTER TABLE __mj."FormChromeRule"
ADD COLUMN "Title" VARCHAR(100) NULL /* ============================================================================
   MJ: Form Chrome Rules — additive Title

   Site admin can rename a rail / accordion section without depending on the
   OpenApp's DisplayName. Keyed by RelatedEntityID or ContributionKey, so an
   upgrade that changes "Payments" → "Payment-o" does not overwrite "Pmts".

   Nullable. Empty / omitted Title keeps the L1 DisplayName (or humanized
   entity name). CodeGen owns EntityField metadata after this column exists.
   ============================================================================ */;

COMMENT ON COLUMN __mj."FormChromeRule"."Title" IS 'Optional admin display title for this section. Null keeps the relationship DisplayName or contribution name. Survives OpenApp upgrades because the row is keyed by RelatedEntityID / ContributionKey, not by the previous label.';
