-- ============================================================================
-- MemberJunction PostgreSQL Migration — V202608082130__v6.1.x__Clear_EntityAction_RelatedNameField.sql
-- Generated by the split-and-regenerate pipeline (AST dialect transpile of
-- hand-written DDL; CodeGen objects regenerated natively; metadata re-seeded).
-- ============================================================================

CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE SCHEMA IF NOT EXISTS __mj;
SET search_path TO __mj, public;
SET standard_conforming_strings = on;

-- ╔══ CONVERSION GAPS — resolve before relying on this migration ══╗
-- UNHANDLED BY THE AST TRANSPILER (4 statement(s)):
--   [1] (DECLARE) DECLARE @EntityActionConsumers TABLE (EntityID UNIQUEIDENTIFIER PRIMARY KEY) /*
--   [2] (INSERT) INSERT INTO @EntityActionConsumers (EntityID) VALUES ('3E248F34-2837-EF11-86D4-6
--   [3] (STOP) /* 1. Stop the request. Without this, the next CodeGen run recreates the orphan
--   [4] (REMOVE) /* 2. Remove the orphan virtual fields already created. Scoped to virtual fields
--   Each statement above was REPORTED, not silently dropped — port it manually.
-- ╚════════════════════════════════════════════════════════════════╝

/* MJ: Entity Action Params */;

-- ============================================================================
-- HAND-PORTED BODY
--
-- The AST transpiler emitted nothing executable for this migration: its whole body is a T-SQL
-- table variable plus two JOIN-to-table-variable DML statements, none of which it handles. The
-- generated file was a header and one stray comment.
--
-- The defect being fixed is metadata-level, not SQL-Server-specific, so PostgreSQL needs it too:
-- four entities set IncludeRelatedEntityNameFieldInBaseView on their EntityActionID FK, but the
-- target (MJ: Entity Actions) is a pure junction with no name field. CodeGen therefore announces an
-- `EntityAction` column in metadata that it cannot emit into the base view, which breaks field
-- ordering and fails IT50 / codegen-determinism.CD3 on any database built from migrations alone.
--
-- Ported as plain IN-lists rather than a temp table — a four-row key set needs no relation. Note
-- the boolean literals: IsVirtual and IncludeRelatedEntityNameFieldInBaseView are BIT on SQL Server
-- but genuine BOOLEAN in the PG schema, so the SS `= 1` / `= 0` comparisons must become TRUE/FALSE
-- or the statement fails outright.
-- ============================================================================

-- 1. Stop the request. Without this, the next CodeGen run recreates the orphan field.
UPDATE __mj."EntityField"
   SET "IncludeRelatedEntityNameFieldInBaseView" = FALSE,
       "__mj_UpdatedAt" = NOW()
 WHERE "Name" = 'EntityActionID'
   AND "IncludeRelatedEntityNameFieldInBaseView" = TRUE
   AND "EntityID" IN (
        '3E248F34-2837-EF11-86D4-6045BDEE16E6',  -- MJ: Action Execution Logs
        '39248F34-2837-EF11-86D4-6045BDEE16E6',  -- MJ: Entity Action Filters
        '35248F34-2837-EF11-86D4-6045BDEE16E6',  -- MJ: Entity Action Invocations
        '56248F34-2837-EF11-86D4-6045BDEE16E6'   -- MJ: Entity Action Params
   );

-- 2. Remove the orphan virtual fields already created. Scoped to virtual fields named
--    'EntityAction' on those four entities only — a real, view-backed column is never IsVirtual,
--    so this cannot remove a working field.
DELETE FROM __mj."EntityField"
 WHERE "Name" = 'EntityAction'
   AND "IsVirtual" = TRUE
   AND "EntityID" IN (
        '3E248F34-2837-EF11-86D4-6045BDEE16E6',
        '39248F34-2837-EF11-86D4-6045BDEE16E6',
        '35248F34-2837-EF11-86D4-6045BDEE16E6',
        '56248F34-2837-EF11-86D4-6045BDEE16E6'
   );
