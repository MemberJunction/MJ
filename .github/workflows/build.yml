name: Build all packages for testing

on:
  # Line branches run this gate too (process doc 5.2 rule 4): lts/* is what a publish
  # ships from and what customers pin to, so a break discovered after merge is worse
  # here than on next. Without this the whole workflow was inert on every lts/* PR.
  workflow_dispatch:
  push:
    branches:
      - next
      - 'lts/**'
    paths:
      - 'package-lock.json'
      - 'packages/**'

jobs:
  build-only:
    name: Build new versions of NPM packages
    timeout-minutes: 30
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.VERSION }}
    env:
      BRANCH: ${{ github.base_ref || github.ref_name }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
          fetch-tags: true

      - name: Set up node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Validate package repository.url for npm provenance
        run: ./.github/scripts/validate-package-repository.sh

      - name: Install dependencies
        run: npm ci

      - name: Build packages
        run: npm run build
