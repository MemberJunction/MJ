name: Check Dependencies

on:
  pull_request:
    branches:
      - next
    paths:
      - 'packages/**/*.ts'
      - 'packages/**/*.tsx'
      - 'packages/**/package.json'
      - 'package.json'
      - 'package-lock.json'

jobs:
  check-dependencies:
    name: Detect missing and unused dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run Knip dependency check
        id: knip
        run: |
          echo "Running Knip to check for missing dependencies..."
          # Force no color output and use compact reporter for consistent parsing
          if NO_COLOR=1 npx knip --include unlisted --reporter compact > knip-report.txt 2>&1; then
            echo "✅ No missing dependencies found"
            echo "status=success" >> $GITHUB_OUTPUT
            echo "has_issues=false" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Missing dependencies detected"
            echo "status=warning" >> $GITHUB_OUTPUT
            # Check if there are actual missing dependencies (not just config hints)
            if grep -E "^[a-zA-Z@]" knip-report.txt | head -1 | grep -vE "(Configuration hints|index\.js)"; then
              echo "has_issues=true" >> $GITHUB_OUTPUT
            else
              echo "has_issues=false" >> $GITHUB_OUTPUT
            fi
          fi

          # Display the report
          cat knip-report.txt

      - name: Format report
        if: always()
        run: node scripts/format-knip-report.mjs knip-report.txt > formatted-report.md

      - name: Comment PR with results
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('formatted-report.md', 'utf8');

            // Find existing comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('<!-- dependency-check-report -->')
            );

            if (botComment) {
              // Update existing comment
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              // Create new comment
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Upload Knip report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: knip-report
          path: knip-report.txt
          retention-days: 30

      - name: Fail if missing dependencies found
        if: steps.knip.outputs.has_issues == 'true'
        run: |
          echo "❌ Missing dependencies detected! Please run 'npm run deps:fix-missing' locally to fix."
          echo ""
          echo "See the PR comment above for details on which dependencies are missing."
          exit 1
